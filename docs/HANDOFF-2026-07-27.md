# Session Handoff — 27 July 2026

> **⚠️ This file is DISPOSABLE and dated. Delete it once `wp-16b-guards` is committed and merged.** It exists because one long session produced a lot of uncommitted state, and a fresh session needs to pick it up without re-deriving it. **`CLAUDE.md`, `docs/TEAM-MEETING.md` and `docs/SRS-v2.md` remain the sources of truth** — everything below is either already written into them or is explicitly flagged as *not yet done*.
>
> **Owner of everything here: Patrick Rucker.** Presentation: **31 July 2026 — four days.**

---

## 1. Where the repo actually is

**Branch `wp-16b-guards`, off `dev`. NOTHING IS COMMITTED.** `dev` is clean and at `be53a32` (the packet 11 merge), in sync with `origin`.

**Modified (uncommitted):**

| File | What changed |
|---|---|
| `CLAUDE.md` | Header + §9 rewritten for today; **§4.12(a) corrected** (two routes for guard authorship); Patrick's queue row re-ordered; OPEN-31 pointer added |
| `docs/SRS-v2.md` | **→ v2.33.** v2.32 = FR-WER-04 narrowed + FR-REC-11 clause. v2.33 = NFR-MNT-07/09 guard-authorship correction. OPEN-29 closed, OPEN-30 added in Appendix C, FR-REC-11/12 traceability row rewritten |
| `docs/TEAM-MEETING.md` | Six new decision-log rows; OPEN-08/11 closed, OPEN-10 retargeted, OPEN-28 corrected, OPEN-29 closed, **OPEN-30/31/32 opened**; build-order gains row `5a` |
| `docs/REPLAY.md` | Corrected: not all of `scripts/` is transcribed verbatim |
| `prompts/00-prompt-collection-summary.md` | 16b marked written; **17d added**; coverage rows updated |
| `prompts/wearable/09-recommendation-rules-RED.md` | Footnote marking its FR-REC-11 quote superseded (historical quote left intact) |

**New (untracked):**
- `prompts/verification/16b-guards-network-extensibility.md` — the FR-LIB-02 + FR-REC-11 guards
- `prompts/verification/17d-wire-recommendation-path.md` — ⚠️ **needs rescoping, see §3**
- `docs/HANDOFF-2026-07-27.md` — this file

**`npm run verify` has NOT been run since these edits.** They are documents and prompts only, so it should be green, but nobody has checked.

---

## 2. ⛔ THREE THINGS ARE FLAGGED BUT NOT DONE

**These are the first things a new session should handle. All three were found late in the session and none were implemented.**

### 2.1 ✅ **DONE — SRS v2.34 applied.** Correct the v2.32 wording, which contradicts FR-WEL-01

> **Applied in full on 27 Jul, uncommitted, on this branch.** `WellnessView.tsx` was **read** this time (not grepped) and confirms the analysis below: the current-date list is `Object.entries(metrics.metrics).map(...)`, and the two literal names are inside the `useMemo` building the FR-WEL-04 seven-day series. Edited: SRS (cover token 2.30 → **2.34**, new revision row, FR-WER-04's note, FR-REC-11's parenthetical, the FR-REC-11/12 traceability row, OPEN-29 in Appendix C), `docs/TEAM-MEETING.md` (new decision-log row + OPEN-29's issue row), `CLAUDE.md` (header + §9), packet 16b (three places), packet 09's footnote. **The v2.32 revision-history row and the 27 Jul OPEN-29 decision-log row are left unedited with pointers to v2.34** — they record what was decided that day.

**v2.32 narrowed FR-WER-04** (removing "the dashboard" from its protected surfaces) and its explanatory note says:

> *"**The wellness view is not obliged to display it.**"*

**That sentence is wrong.** **FR-WEL-01** (Essential, D) already states the opposite, verbatim:

> *"Display **every metric in the Daily Metric Set** for the current date… The view shall render whatever the set contains **rather than a hard-coded list**, so a metric added under FR-WER-04 appears without changing this view."*

**FR-WEL-01 was missed during the v2.32 cross-reference hunt** (a §0 rule 3 failure).

**The outcome of v2.32 still stands, but the justification must change:**
- The generic-rendering obligation **lives in FR-WEL-01**, verified by **demonstration (D)** — FR-WER-04 did not need to duplicate it as an (I) obligation.
- **`WellnessView.tsx` already satisfies FR-WEL-01**: line ~90 does `Object.entries(metrics.metrics).map(...)` — the current-date list *is* generic.
- The hard-coded `'sleepScore'` / `'activeCalories'` at lines ~57–58 build the **7-day history chart**, which is **FR-WEL-04** — *"Display sleep score and active calories over at least the previous seven days"* — **a requirement that names both metrics explicitly.** Hard-coding is correct there.
- ⚠️ **The original OPEN-29 analysis reasoned from a `grep` for metric names rather than reading the file**, and so described FR-WEL-04's named chart as if it were the whole view.

**To do:** SRS **v2.34** replacing the "not obliged to display it" sentence with the FR-WEL-01/FR-WEL-04 division; update OPEN-29's closure record, `CLAUDE.md`'s header/§9 summary, and packet 16b's quoted clarification. **The guard's `web/src/**` exclusion and "no work for Ryan" are both unchanged** — only the reasoning moves.

**Nothing downstream has gone wrong:** the Slack message sent to Ryan told him to do nothing, which is still correct.

### 2.2 ✅ **DONE — 17d rescoped.** It overlapped `origin/wp-14a-wellness-backend`

> **Rescoped 27 Jul by reading the branch.** The overlap was **larger** than the table below records: `wp-14a` also already wires `MetricStore` + both catalogs + a composed `Catalog` in `index.ts` (most of 17d's composition root), and its `GET /wellness` **already answers** the per-user-engine design question 17d posed as open. All cut. **`wp-14a` is now a hard prerequisite — the packet stops if it is not merged.** What survives: the `WorkoutSource` adapter (OPEN-28), the `RecommendationScheduler` factory, `POST /recommendations/apply-workout`, and the §6 transcript. Decision-log row recorded.

**`wp-14a` (Ryan's, unmerged) already contains work 17d specifies:**

| Already on `wp-14a` | 17d currently also specifies |
|---|---|
| **`POST /wearable/metrics`** — FR-WER-07 injection | `POST /metrics` — **a duplicate** |
| **`GET /wellness`** — builds `RecommendationEngine` **per request** with `CaloriesToTargetRule(user.baselineCalories)` | 17d poses this as an open design question — **`wp-14a` already answers it, correctly** |
| **`baselineCalories` captured at registration**, validated (FR-REC-09 / UC-01) | 17d's escalation clause guesses this "may live on `wp-14a`" — **it does** |

**Rescoped, 17d's unique content is:** wiring `RecommendationScheduler` (the apply-workout path — FR-REC-02/03/04), the `WorkoutSource` adapter + `Catalog` alias (OPEN-28), and the catalog wiring. **Smaller than authored.**

⚠️ **Hard dependency: `wp-14a` must merge to `dev` before 17d runs**, or the two collide in `app.ts` and `index.ts`.

### 2.3 The Garmin export has no ingestion path — unowned, no packet

> ✅ **NOW TRACKED as OPEN-33** in `docs/TEAM-MEETING.md`'s open-issues table and in `CLAUDE.md` §9 (27 Jul), so it no longer depends on this disposable file surviving. **Ownership is still undecided** — that is the first step and it needs a decision-log row. **Still to do: the ownership call, then author + run the packet.**

**`GarminExportAdapter` is referenced nowhere outside `server/src/wearable/` — on `dev`, on `wp-14a`, on any branch.** No route, no CLI, no script. The adapter that parses the export exists and is frozen-tested (packet 08, frozen `1199d80`); **nothing calls it.**

**Three Essential requirements have no live path:**
- **FR-WER-08** (E, D) — ingest real health data exported from the user's wearable account
- **FR-WER-09** (E, T) — ingestion is idempotent; re-ingesting a date updates rather than duplicates
- **FR-WER-10** (E, D) — at least one **real** metric from a **real** device drives a real recommendation *(this was the **midpoint** gate)*

**⚠️ Ownership was being checked when the session ended and was NOT resolved.** By role it is Ryan's (Data & Wearable Integration Lead), but **it appears in no queue and no packet** — the build order runs 08 → 09 → 10 → OPEN-04 → 11 → 14/15 and never includes it. **Patrick has said he will likely take it** given Ryan's availability. **Confirm the reassignment, record it in the decision log, then author the packet.**

**Scoping note for whoever writes it:** the pieces exist — `GarminExportAdapter` parses, `MetricStore.ingest` persists idempotently (upsert on `(userId, date, name)`). The missing part is *the thing that reads the export file and connects them*. ⛔ **§8.4: the real export is a teammate's actual health data and must NOT enter the repo** — so this reads a local path (or an upload), and fixtures stay *derived*, not copied.

---

## 3. What was decided this session (all written into the docs already)

1. **OPEN-29 closed** — the wellness chart naming its two series is presentation. FR-WER-04 narrowed at **v2.32**; the dashboard is removed from its protected surfaces, and FR-REC-11's verification clause follows. ⚠️ **Reasoning needs the §2.1 correction.**
2. **Packet 16b authored** — FR-LIB-02 + FR-REC-11 guards, plus FR-WER-04/NFR-MNT-06. Names four legitimate constructs a naive pattern fires on (`WearableAdapter.fetch()` is a *method*; `readFileSync` in `WorkoutCatalog` is vendored seed data — **disk is not network**; the FR-LIB-10 attribution URL; metric names in `MetricStore.ts`'s *comment*). Requires **eleven must-fail transcripts**.
3. **`stripCommentsAndStrings` gets extracted** verbatim to `scripts/lib/strip-code.mjs`, imported by all four guards, **with 16a's four must-fail exercises re-run** — because breaking a guard is *silent*.
4. **`CLAUDE.md` §4.12(a) + SRS NFR-MNT-07/09 corrected (v2.33)** — guards may be authored by **transcription** *or* **generation from a human-authored spec with every check watched to fail**. The document had claimed only the first, while 16a had used the second since 24 July. **NFR-MNT-07 gains: a check never observed to fail does not satisfy it.**
5. **Queue re-ordered — 17d before 16b** (OPEN-30): the recommendation path is wired into nothing, so §6 cannot be demonstrated on the running System. 16b guards a path nobody can execute.
6. **Open-issue sweep** — OPEN-08 closed (done since 12 Jul, row stale), OPEN-11 closed as *superseded* by the 26 Jul no-Conditional-work decision, OPEN-10 retargeted to presentation prep, **OPEN-31 opened** (the P08 freeze-provenance confirmation had lived only as a `⚠️` inside a status paragraph).
7. **Second read of Ryan's 15a RED suite → OPEN-32**, four findings, **sent to Ryan on Slack**. The gate call stands; two findings are cheaper before tonight's freeze.

---

## 4. Open issues, current

| ID | What | Owner | Status |
|---|---|---|---|
| **OPEN-30** | Recommendation path wired into nothing — §6 undemonstrable | Patrick | 🔴 Packet 17d, **needs rescoping (§2.2)** |
| **OPEN-32** | Four findings on the 15a RED suite; two are pre-freeze | Patrick (1,2) · Ryan (3,4) | 🔴 **Sent to Ryan; he freezes tonight** |
| **OPEN-31** | Confirm the P08 freeze-provenance correction (`40a3447`→`1199d80`) | Patrick (§4.12 owner) | 🔴 Two minutes |
| **OPEN-28** | Two `Catalog` interfaces | Patrick | 🟡 Approach decided; closes with 17d |
| **OPEN-10** | Questions for the professor | Patrick | 🟡 Retargeted — presentation prep, not a blocker |

**Not in the table but outstanding:** **Miguel was never told about `source?`.** `TaskRepository.createTask` gained an optional `source?: TaskSource` param — his module. The 17b decision-log row says he *"should ack before this branch merges to `dev`"*; **it merged on 26 July anyway** (on `dev` at `f1e0ccd`). Harmless (optional, defaults `'USER'`) but a stated gate that got bypassed. One Slack line.

---

## 5. Ordered plan — "the app must function first"

**Patrick's stated priority (27 Jul): finish building the application, including test suites, before any rehearsal work.** Rehearsals are explicitly deferred.

1. **Commit + merge `wp-16b-guards`** — run `npm run verify` first; read the diff. *(Nothing reaches teammates until this happens; Ryan is working tonight against a `dev` without the v2.32/2.33 changes.)*
2. **Apply the §2.1 correction (SRS v2.34)** — small, and it stops a contradiction shipping in the submission document.
3. **Ryan merges `wp-14a` to `dev`** — unblocks 17d and puts the whole wellness section (FR-WEL-01…05) on `dev`.
4. **Rescope and run 17d** (§2.2) — fresh session. **FR-REC-04 is §5's load-bearing requirement.**
5. **Author + run the Garmin ingestion packet** (§2.3) — confirm ownership first.
6. **Ryan: 15a freeze → 15b GREEN (analytics endpoint) → 15c (analytics view).**
7. **Run 16b** — fresh session; closes NFR-MNT-07.
8. **OPEN-31**, and the Miguel heads-up.
9. *Then* testing/rehearsals.

---

## 6. Two questions the user asked that were NOT answered

**Carry these into the new session.**

1. **"Is the Garmin export task assigned to Ryan?"** — Being verified when the session ended. See §2.3: by role yes, by queue **no** — it is in no packet and no owner's list.
2. **"We need to find testing suites and possible demo software — I'm not sure what that is or what it's used for."** — **Not answered at all.** Worth noting for whoever picks it up: the project **already has a substantial test stack** (Jest, `ts-jest`, `fast-check` for property tests, `supertest` for HTTP, `mongodb-memory-server`, ~453 tests, engine at 100% coverage), so "find testing suites" may largely be done. The genuine gaps to discuss are **browser-level end-to-end testing** (which would be new tooling four days out — a scope risk worth weighing against its benefit) and what "demo software" should mean in practice: most likely **a seed script that puts the System into a known demo state in one command** (making a rehearsal repeatable and a failed demo resettable), plus possibly **a screen recording as a fallback** if a live demo fails. **None of this has been discussed or decided — treat it as an open question, not a plan.**

---

## 7. Facts a fresh session will need

- **Frozen suites (seven):** `engine/test` `ac06e70` · `server/test/reschedule` `c30e784` · `server/test/wearable` `1199d80` · `server/test/recommendation` `59f4e8a` · `server/test/catalog` `4b1fd4e` · `server/test/acceptance` `8809158` · `server/test/replacement` `47bb301`. **`server/test/analytics` (15a) is NOT yet frozen** — Ryan does that tonight.
- **SRS versions:** `dev` has 2.30. `wp-16b-guards` has **2.32 + 2.33**. **`wp-15-analytics` claims 2.31** (unmerged). Expect a revision-history conflict when these meet — **text only, no code**. Next free number after this branch: **2.34**.
- **⛔ `server/test/acceptance/support/harness.ts` (frozen `8809158`) imports the name `Catalog` from `RecommendationScheduler`.** Widening or renaming that interface **without keeping `export type Catalog = ...` as an alias** breaks a frozen suite at compile time.
- **`npm run verify` on `dev`: 453 tests green**, engine 100%.
- Unmerged branches: `wp-14a-wellness-backend` (wellness + profile capture, green and mergeable), `wp-15-analytics` (off it; 15a RED, by-design red), `wp-16b-guards` (this work), plus older merged-through branches.
- **Read `CLAUDE.md` §0, §0.1, §4.6, §4.12 and §8 before doing anything.** Especially: **an agent does not commit unless a human asks**, and **no AI credit lines in commit messages, ever**.
