# Team Meeting — Agenda, Questions, and Decision Log

**Project:** Adaptive Habit, Schedule & Wellness System
**Team:** Patrick Rucker · Miguel Alvarez · Ryan Woosley

> **How to use this file.** The top section is the agenda for the next meeting. Below it is a **permanent decision log** — every decision the team makes gets a row, with the date and the reasoning. The reasoning matters more than the decision: six months from now, and three weeks from now, the *why* is what stops you re-arguing something you already settled.

---

# 🔨 THE BUILD ORDER — *adopted 22 July 2026. This is what we are working to.*

> **⚠️ The dated schedule further down this file (“The Schedule We’re Committing To”, 12 Jul) is SUPERSEDED.** The team stopped working to per-day deadlines on 22 Jul. It is kept as a record of what was committed at Meeting 1 — **do not read it as the current plan.** The presentation date (**31 July**) is unchanged and is still the only hard date.
>
> **Order here is by dependency, not by calendar.** Each owner works their queue top to bottom and starts the next item when the one above it lands.

**Standing rules that apply to every item below** *(they are not repeated per row)*: one packet = one fresh agent session · **the owner authors their own packet before running it** (copy `prompts/_TEMPLATE.md`, quote the SRS verbatim, include the *files you must NOT touch* list) · a 🔴 RED packet and its 🟢 GREEN partner are never the same session, and **the freeze commit sits between them** · merge to `main` as each item lands (`CLAUDE.md` §7.1) · **an agent does not commit unless a human asks** (§8.5).

### Patrick Rucker — Scheduling Algorithm Lead

| # | Item | Done when |
|---|---|---|
| 1 | Run `prompts/engine/04-engine-tests-RED.md` → **human gate** (§6 of the workflow doc, with one other person) → freeze commit | Suite **fails**. A green suite means the agent wrote the engine and the packet is compromised. |
| 2 | Run `prompts/engine/05-engine-implementation-GREEN.md` | Engine coverage **≥90%** (NFR-MNT-01) · property test **≥1,000 cases** (NFR-COR-01) · `engine/package.json` dependencies block **empty** · `git diff --stat engine/test/ <RED-sha>..HEAD` **empty** |
| 3 | Author **06** (reschedule RED) → run → gate → freeze | All three triggers covered: missed (FR-RSC-01), declared skip (FR-RSC-08), displacement (FR-RSC-02) — plus the correction path (FR-RSC-09) |
| 4 | Run **07** (reschedule GREEN) | The service **re-invokes the engine**. No second placement function anywhere (FR-RSC-03). |
| 5 | ✅ **DONE 24 Jul (merged to `dev`)** — Author + run **16a**, the guards whose subjects exist **now**: FR-RSC-03 and FR-SCH-05. Both run in `verify` + CI; re-validated clean against `dev` with 12/13 present, one vocabulary false positive narrowed. `docs/P16A-REPORT.md`. | FR-RSC-03 is the most exposed requirement in the repo; guard it before packet 12 lands new `server/src/` code. |
| 5b | *(after 10/11)* Author + run **16b** — FR-LIB-02 and FR-REC-11. | These need the recommendation path and libraries to exist before there is anything to guard. |
| 6 | Author + run **17a RED** — the acceptance suite, **including FR-REC-04's**, frozen **before any wiring exists**. Needs **09 RED** first, for the recommendation shape. | **FR-REC-04 is Core and verified by TEST.** A test written by whoever wires it encodes what the wiring does. |
| 7 | **Human gate → freeze → run 17b GREEN**: the FR-REC-04 wiring and integration, against a suite it may not edit; then **≥3 full demo rehearsals** | The demo sequence below runs end to end **with the network disabled** |

### Ryan Woosley — Data & Wearable Integration Lead

| # | Item | Done when |
|---|---|---|
| 1 | ✅ **DONE 22 Jul — OPEN-01 closed, and OPEN-02 closed with it.** Both metrics exist as published fields: **`activeKilocalories`** (UDS file) and **`sleepScores.overallScore`** (sleep file), joined on `calendarDate`. **Nothing below is blocked any more.** | ✅ Field names recorded in OPEN-01. The real export **stays out of the repo** — fixtures are *derived* from it (§8.4), and note the height/weight/VO2Max files are **more** sensitive than the scores. |
| 1b | **OPEN-20 — settle the absent-vs-measured-zero rule for calories** *(new, from reading the export)* | The team has adopted a stated rule for when `activeKilocalories: 0.0` means *unavailable* rather than *zero*. **It goes in packet 08 as a constraint; an agent must not infer it.** |
| 2 | ✅ **08** — adapter + Daily Metric Set | **DONE 25 Jul, merged to `dev`.** One document per metric per date (DR-05); absent vs measured zero distinguishable via `totalSteps` presence (OPEN-20, FR-WER-06/NFR-ROB-01); `bmrKilocalories` untouched (CON-05/§4.9). |
| 3 | ✅ **09** (rules RED) | **DONE 25 Jul.** Frozen at `59f4e8a` (27 tests); boundaries 49/50/74/75 asserted by literal value; run in a fresh window, second-human gated, committed. |
| 4 | ✅ **10** (rules GREEN) | **DONE 25 Jul, merged to `dev`, `verify` green (294 tests).** `SleepToIntensityRule` + `CaloriesToTargetRule` + content-agnostic `RecommendationEngine`. ⚠️ **FR-WER-10 is code-complete, NOT yet demonstrated** — the (D) requirement needs the real-export → recommendation path *shown*, not a unit suite; the FR-REC-04 wiring is Patrick's packet 17. |
| 5 | ✅ **Packet 11 DONE 27 Jul — RED (`4b1fd4e`, 134 tests) + 11b GREEN (`a40d46d`) merged to `dev`, `verify` 453 green.** Catalog + seeded workout/meal libraries behind the FR-LIB-01 seam; OPEN-04 closed. **Next: frontend views 14/15** (OPEN-09). ⚠️ The libraries exist but are not yet wired into the recommendation path — that is **OPEN-28** (Patrick's 17b port), the last step for the real §6 demo | Libraries seeded locally, **with calorie counts** — done | 
| 6 | frontend views **14** and **15** *(OPEN-09, agreed 23 Jul)* | ⬜ Next |

### Miguel Alvarez — Frontend & Backend Lead

| # | Item | Done when |
|---|---|---|
| 1 | ✅ Author + run **12** — accounts, tasks, commitments, persistence | **Done, 22 Jul — `docs/P12-REPORT.md`.** OPEN-12 answered before the packet was authored (Mongo `_id`, boundary ownership — SRS v2.18). FR-CAL-05 resolved as vacuous under manual-entry-only scope, not a runtime guard — the first-drafted guard didn't survive contact with `RescheduleService`'s internals; see the report. OPEN-17 (FR-SCH-10's day-planning path) closed via `TaskRepository.tasksForDate` + `sweepElapsed` wiring, no new placement code. `npm run verify`: 233 tests green. |
| 2 | ✅ **13** — schedule dashboard | **Done, 23 Jul — `docs/P13-REPORT.md`.** Built on `wp-13-frontend-dashboard` off `wp-12-backend-api`. **Merged to `dev` 24 Jul** (along with `wp-replay-guards` and a cleanup pass — OPEN-25, OPEN-17; see the 24 Jul decision-log row). `npm run verify` on `dev`: 243 tests green. |
| ~~3~~ | ~~**14**, **15**~~ — **reassigned to Ryan, OPEN-09 agreed 23 Jul.** Miguel's queue ends at 13 unless something else comes up. | |
| 4 | ✅ **FR-CAL-07** — the `.ics` calendar export, on `wp-cal-export`. **Done, 24 Jul.** Started ahead of the "not until Essential is verified" line above — recorded and reasoned in the decision log, since it's Conditional, scoped, and doesn't touch anyone else's queue. | `npm run verify`: 263 tests green. Not done: an actual import into a real calendar app (see the decision-log row). |

### 🔗 Cross-person dependencies — the only places one queue blocks another

| Sync point | Needs |
|---|---|
| Gate on **04** | Patrick **+ one other person**, same day as the RED run |
| Gate on **06** | Patrick + one other |
| Gate on **09** | Ryan + one other |
| Packet **16** | **07 and 10 both landed** |
| **FR-REC-04 wiring** | **05 + 10 + 12** — the seam where the System stops being a scheduler and a fitness app sharing a login. **The load-bearing requirement; protect it above everything else.** |
| Packet **17** | everything |

### ✅ Resolved 23 Jul — the rebalance

**Ryan's queue ran dry after packet 11; Miguel had the backend plus three views alone.** OPEN-09's original fix — *Patrick takes the backend after the engine* — **no longer worked**, because packets 06, 07, 16 and 17 fill Patrick's queue. **Direction set 22 Jul, agreed 23 Jul: Ryan takes views 14 and 15 once his wearable chain is green; Miguel keeps 12 and the Core dashboard (13).** *(OPEN-09, closed.)*

---

# ✅ MEETING 1 — 12 July 2026 — **HELD. OUTCOMES BELOW.**

## What we decided

| # | Decision |
|---|---|
| **Roles** | **Patrick Rucker → Scheduling Algorithm Lead.** **Ryan Woosley → Data & Wearable Integration Lead.** **Miguel Alvarez → Frontend & Backend Lead.** |
| **Garmin export** | **Ryan owns it.** ✅ *(Pulled and read 22 July — OPEN-01 and OPEN-02 both closed. Two files matter; field names are recorded in OPEN-01.)* |
| **Database** | **MongoDB, not PostgreSQL.** |
| **Calendar** | **Not importing.** Instead we will **export an `.ics` file** the user can import into Google Calendar. |
| **Repo** | **Patrick creates it.** |
| **Working agreements** | **Agreed** — daily integration, one human owner per module who can explain it aloud, nobody merges code they haven't read. |
| **Scheduling / cadence** | Each member manages their own working schedule; no fixed standup. |

## ⚠️ Still open after Meeting 1 — carry to Meeting 2

- [ ] **Scope cuts were NOT ratified** — the team didn't recall what they were. **They are re-stated below. Confirm them.**
- [ ] **FR-REC-04** — did everyone confirm they understand why it's untouchable?
- [ ] **Workload imbalance** — Miguel has frontend **and** backend. See the risk note below. **This needs 10 minutes.**
- [ ] Who owns the demo environment (making sure it actually runs on 31 July)?
- [ ] Demo rehearsal dates (SRS assumes ≥3 full run-throughs, 28–30 July)?
- [ ] Availability during graduation week — anyone with conflicts?
- [ ] Questions for the professor (see below).

---

## 🔁 THE SCOPE CUTS — *re-stated, still needing ratification*

Two requirements were demoted from **Essential** to **Conditional** to protect the 31 July date:

| Requirement | What it is | Why it's safe to cut |
|---|---|---|
| **FR-REC-10** | The **weekly** meal plan, with meal-prep windows placed on the schedule | **FR-REC-08 stays Essential** — active calories drive a daily calorie target, and meals are recommended against it. **That is the adaptive claim.** Weekly *planning* just rides on top of it. |
| **NFR-MNT-02** | 70% test coverage across the **whole system** | Frontend coverage is expensive and reveals little. **NFR-MNT-01 stays Essential: 90% coverage on the scheduling engine** — the number that actually matters, because the engine is where correctness lives. |

**Also confirm these are NOT started until all Essential work is verified:** FR-SCH-07/08 (priority displacement), FR-WER-11 (live wearable API), FR-REC-12 (extensibility proof), FR-LIB-09 (external content API), **FR-CAL-07 (the `.ics` export)**.

- [ ] **Ratified by the team** ☐ yes ☐ objections: ______________________

---

## 🔴 RISK RAISED AFTER THE MEETING: workload imbalance

**Miguel has the frontend *and* the entire backend API.** The frontend alone was already the most likely thing to run out of time — three views, one with a calendar-style time axis. The backend API is what every other module plugs into.

Meanwhile the **scheduling engine is small and deep** — probably ~3 days — and then Patrick has spare capacity for two weeks.

**Suggested rebalance:**

| Person | Suggested |
|---|---|
| **Patrick** | Scheduling engine + rescheduling triggers → **then the backend API** |
| **Ryan** | Garmin ingest + metric adapter + recommendation rules + workout/meal libraries |
| **Miguel** | **Frontend only** — three views, starting 17 July against a mocked API |

**This is worth ten minutes.** If Miguel is the critical path and he's also the only person who can unblock everyone else, a single bad week sinks the demo.

---

## ❓ QUESTIONS FOR THE PROFESSOR

*Raised 12 July. These are course-policy questions, not technical ones, and we should not codify answers before asking.*

1. **Is AI-generated prompting acceptable**, or does he expect the human direction of agents to be human-authored?
2. **Does he expect tests to be human-written?** This one has teeth: our SRS commits to **90% engine coverage (NFR-MNT-01)** and **property-based testing of the core invariant (NFR-COR-01)**. If an agent writes both the tests and the implementation it tests, those numbers stop meaning what they appear to mean — and a software engineering professor is likely to care about that distinction.
3. Any expectation on deliverable format for the SRS (PDF? Word? page numbers in the Index)?

---
---

# MEETING 2 — *date TBD*

**Purpose:** Ratify the scope cuts, resolve the workload imbalance, review the Garmin export findings.

**Bring:** The SRS (`SRS-v2.md`). Everyone should have skimmed §1.2 (Scope), §2.7.1 (what we're cutting), and §3.3 (the use cases).

---

## ⏱ The 60-Second Status Update (say this first)

> We have a complete SRS — ~84 numbered, verifiable requirements, use cases, UML diagrams, and wireframes, written to IEEE 830-1998 and matching the format of the professor's examples. **We also have a written development method (`AGENTIC-TDD-WORKFLOW.md`) and the first three work packets ready to run (`prompts/`).**
>
> **We have 19 days until we present on 31 July, and the midpoint go/no-go gate is in FOUR.** The documents are done. **What's blocking us is three decisions, and one small piece of work nobody has done yet** — nobody has looked at a real Garmin export.

---

## 🚨 IF WE ONLY GET THROUGH FOUR THINGS TONIGHT, MAKE IT THESE

There is more agenda below than there is evening. **These four block all other work — do not leave without them.** Everything else can be settled in Slack tomorrow.

| # | The decision | Why it can't wait |
|---|---|---|
| **1** | **Roles** (Q1) | **Every work packet in `prompts/` assigns its owner by role.** Until roles exist, literally nobody can start. It is OPEN-08 and it was due today. |
| **2** | **Who pulls the Garmin export — and when** (Q4) | **The 16 July gate depends on it and it has not been started.** It is small work. It is on the critical path. It should be done before Monday. |
| **3** | **Who creates the repo, tonight or tomorrow** (Q10) | Everything downstream is blocked on packets 01–03. |
| **4** | **Ratify the scope cuts** (Q2) | The entire point of writing them down is that **nobody is surprised on 29 July.** |

**Everything else — the workflow doc, the contract, PR conventions — is *reading*, not *deciding*.** Assign it and move on.

---

## 🔴 DECISIONS WE MUST MAKE TONIGHT

These block other work. Don't leave without them.

### Q1. Who takes which role?

The SRS is built around three module boundaries so we can work in parallel without colliding. **Whoever owns a module owns its section of the SRS and must be able to explain its code aloud.**

| Role | Owns | Hardest part |
|---|---|---|
| **Scheduling Algorithm Lead** | The engine (a pure function), rescheduling triggers, the test suite | The engine **is** the grade. It's also the most testable thing in the project. |
| **Data & Wearable Integration Lead** | Garmin ingest, the metric adapter, the recommendation rules, the workout/meal libraries, the DB schema | Owns the **only external unknown** in the project (the Garmin export). |
| **Frontend & Analytics Lead** | Schedule dashboard, wellness view, analytics view | **The most likely thing to run out of time.** Three views with a calendar-style time axis is real work. |

- [ ] **Patrick Rucker →** ______________________
- [ ] **Miguel Alvarez →** ______________________
- [ ] **Ryan Woosley →** ______________________

**Ask the team:** *"Who's strongest at algorithms and testing? Who wants the frontend, knowing it's the biggest time sink? Whoever takes Data owns the one thing we can't predict."*

---

### Q2. Do we ratify the scope cuts?

I've already marked two requirements **Conditional** in the SRS to protect the timeline. **The team needs to actually agree to this out loud** — the whole point of writing it down is that nobody is surprised on 29 July.

| Requirement | Cut to Conditional | Why it's safe to cut |
|---|---|---|
| **FR-REC-10** — weekly meal plan + scheduled meal-prep windows | ☐ agree ☐ disagree | FR-REC-08 already gives us wearable-driven meal recommendations against a daily calorie target. That **is** the adaptive claim. Weekly planning rides on top of it. |
| **NFR-MNT-02** — 70% test coverage across the whole system | ☐ agree ☐ disagree | Frontend coverage is expensive and reveals little. **We keep 90% coverage on the engine** — that's the number that actually matters. |

**Also confirm we do NOT start these until everything Essential is done and verified:**
- ☐ **FR-SCH-07/08** — priority-based task displacement. *This is the most intellectually interesting thing in the project and the fastest way to lose a week we don't have.*
- ☐ **FR-WER-11** — live wearable API
- ☐ **FR-REC-12** — the extensibility proof
- ☐ **FR-LIB-09** — external recipe/exercise API

**Ask the team:** *"Does anyone object to cutting these? If we're going to fight about it, let's fight now, not on the 29th."*

---

### Q3. Does everyone understand what FR-REC-04 is, and why it's untouchable?

**FR-REC-04:** *a recommended workout is created as a task and placed by the same engine, subject to the same conflict detection and rescheduling as any user-created task.*

This is the **load-bearing requirement of the whole project.** It's what makes us one integrated system instead of a scheduler and a fitness app sharing a login.

> **If everything else shipped and FR-REC-04 didn't, we would have failed at our own thesis while appearing to have built the whole product.**

**Ask the team:** *"Can each of us say, in our own words, why cutting this would sink the project? If not, we don't understand our own thesis yet."*

- [ ] Everyone can articulate it

---

### Q4. Who pulls the Garmin export, and when? ⚠️ **CRITICAL PATH**

✅ **ANSWERED 22 July — Ryan pulled it and read it. This section is kept for the record; the live detail is in OPEN-01.**

- **Sleep score** *(FR-REC-01)* — ✅ published, `sleepScores.overallScore`, integer 0–100. **No derivation formula needed, so OPEN-02 closed unopened.**
- **Active calories** *(FR-REC-08)* — ✅ `activeKilocalories`, separated from BMR by Garmin.
- **The meal library's calorie assumptions** are still open — that is **OPEN-04**, and it is independent of the export.

⚠️ **One thing the export could not supply: there is no 2026 sleep score anywhere in it.** The sleep-driven demonstration therefore runs on the 20-day 2023–24 overlap — see the mixed-vintage decision-log row of 22 Jul, which also records why waiting for a fresh export and why swapping to Body Battery were both rejected.

---

## 🟡 DECISIONS WE SHOULD MAKE TONIGHT

### Q5. Calendar source — Google Calendar integration, or manual entry?

**Recommendation: BOTH, layered. Manual entry is the Essential path; Google Calendar is a Conditional upgrade we build only after everything Essential is done and verified.** *(SRS: SI-05 already says exactly this. OPEN-03.)*

**Google Calendar is genuinely feasible — it does NOT have Garmin's approval problem.** You create your own Google Cloud project, enable the Calendar API, and generate OAuth credentials yourself. **No partner review, no waiting on a human.** Calendar scopes are "sensitive," which normally triggers verification — but **you skip verification entirely by leaving the app in "Testing" mode and adding our three Google accounts as test users** (cap: 100). That works the same day.

**Cost: ~1–2 days** with agentic help. The OAuth flow and `events.list` are easy, and mapping events → busy intervals is trivial **because the engine already takes busy intervals and doesn't care where they came from.**

**⚠️ Four gotchas — the last two are where time actually disappears:**

1. **Refresh tokens expire after 7 days in Testing mode.** Survivable, but if we authorize on the 20th and demo on the 31st without re-authorizing, **it will be broken on stage.** *(Verify against current Google docs.)*
2. **Pass `singleEvents=true`** so Google expands recurring events for us. Without it we're parsing RRULE ourselves — a swamp that eats weeks.
3. **All-day events are a trap.** They return a `date`, not a `dateTime`. Handled naively, a "Mom's Birthday" all-day event marks the **entire schedulable day busy and the scheduler can place nothing.** We need an explicit rule — almost certainly *"ignore all-day events; they aren't time commitments."*
4. **Timezones.** Events carry timezone info; our schedulable day is local. Get this wrong and placements land an hour off.

**The argument FOR doing it:** our original pitch claimed technical depth partly from *"working with external APIs, handling authentication and data retrieval."* **When Garmin's live API came off the critical path, we quietly lost that story.** Google Calendar restores it cheaply — a real OAuth 2.0 flow against a real third-party API is a genuinely different skill from parsing an export file, and it's the kind of thing a grader notices.

**The argument for keeping manual entry as the Essential path:** our acceptance demo currently **runs with the network disabled** — a property we engineered on purpose. We should not casually give it up. **Demo with seeded commitments; *show* the Google import as a live feature if it's working.** If campus wifi dies on the 31st, we lose a feature, not the demonstration.

**Ask the team:** *"Does anyone want to own this as a stretch goal? It's real OAuth experience and it makes the demo feel real — but it does not start until the Essential set is verified."*

- [ ] **Manual entry = the Essential path** ☐ agreed
- [ ] **Google Calendar = Conditional stretch goal** ☐ agreed ☐ skip entirely
- [ ] **If yes, owner:** ______________________  **Not started before:** ______________

### Q6. Do we ratify the tech stack?

React + TypeScript (frontend) · Node.js + TypeScript (backend) · PostgreSQL.

**Rationale:** one language across the whole stack means all three of us can read and review the entire codebase — which matters enormously on a 3-person team with 19 days. TypeScript rather than plain JavaScript because the scheduling engine is interval math over multi-field records, exactly where a type system catches bugs we'd otherwise pay for in debugging time.

- [ ] **Agreed** ☐ **Objections:** ______________________

### Q7. How we work with AI agents — **this is now written down; do not re-derive it in the room**

**Read `AGENTIC-TDD-WORKFLOW.md` before tomorrow.** It is self-contained and assumes you've seen nothing. The two sections that matter: **§2** (why — *read the code block in §2.1, it's the whole argument in one example*) and **§6** (the checklist you will personally run).

**The 90-second version, so we can all nod and move on:**

- **The SRS is the prompt.** We don't rewrite requirements — we quote them verbatim into work packets (`prompts/`).
- **The agent that writes the tests is NEVER the agent that writes the code.** Different chat session, and **the implementer may not edit a test — ever.** If it can't pass one, it stops and a human decides whether the test or the code is wrong.
- **Between them sits a human who reads the tests against the SRS.** Not *"do they pass"* — ***"are they right."*** That review is the bottleneck of this project.
- **The contract** (`shared/src/contract.ts`, ~60 lines of types) is written **once, by a human**, and everything is built against it. **Nobody's agent redefines a type.**
- **TDD is scoped, not universal.** We TDD the engine, rules, catalog, analytics. **We do NOT TDD the frontend** — and we already decided that without noticing, when we demoted NFR-MNT-02 for *"frontend coverage is expensive and reveals little."*

**Still-standing rules from before:** integrate daily · every module has **one human owner who can explain it aloud** (if nobody can, **it isn't done**, even if tests pass) · **nobody merges code they haven't read.**

**Ask the team:** *"Does anyone disagree with the test-writer / code-writer split? It's the one thing everything else hangs on."*

- [ ] All three have read the workflow doc ☐ (due 13 Jul)
- [ ] Nobody objects to the RED/GREEN split ☐

---

### Q8. ⚠️ Are we going to make the 16 July midpoint gate? **Be honest.**

**The gate is FR-WER-10: one real metric, from a real device, driving one real recommendation.** It is four days away.

The path to it is **export → adapter → Daily Metric Set → rule → decision.** Note what is *not* on that path: **the engine.** So the Data Lead **does not wait for the engine** — they start the moment the contract is ratified, in parallel.

**What has to happen, and by when:**

| | By | Who |
|---|---|---|
| ~~Pull the Garmin export and **look at the actual fields** (OPEN-01)~~ | ✅ **Done 22 Jul** | **Ryan** |
| Contract ratified + repo up (packets 01–03) | 13 Jul | |
| DB schema — **one row per metric per date**  (packet 08) | 15 Jul | Data Lead |
| Sleep-score → intensity rule (packets 09–10) | 15 Jul | Data Lead |

**Ask the team:** *"Is this real, or are we going to walk into the 16th and hand-wave? **If we're going to miss it, I'd rather know tonight than on Thursday.**"*

- [ ] We believe the gate is achievable ☐ yes ☐ **no — and here's what we cut:** ______________

---

### Q9. Dev logistics for agentic work — **not yet decided, and they'll bite**

- [ ] **Does everyone actually have Claude Code (or equivalent) working?** *This is a three-person agentic project. If one of us doesn't have the tool running tonight, that person is blocked and we don't find out until the 15th.*
- [ ] **Branch per work packet, PR to main?** *A packet is already one reviewable diff with one owner — it maps onto a PR exactly. And it's how we honor "nobody merges code they haven't read."*
- [ ] **Who reviews whose PRs?** *The human gate (§6) needs a second pair of eyes on the engine tests specifically.*
- [ ] **Where do the prompt files live?** *Proposal: `prompts/` goes in the repo, so packets are versioned alongside the code they produced. A grader can see exactly what we asked for.*

### Q10. Logistics

- [ ] **Who creates the repo?** ______________________
- [ ] **How often do we sync?** ______________________
- [ ] **Who owns the demo environment** (making sure it actually runs on 31 July)? ______________________
- [ ] **Demo rehearsal dates** — the SRS assumes **at least three full end-to-end rehearsals** between 28–30 July. Agreed? ______________________
- [ ] **Reality check: is everyone actually available?** It's graduation week. Who has conflicts? ______________________

---

## 📅 The Schedule We're Committing To

> **⛔ SUPERSEDED 22 July 2026 — this is a historical record, not the plan.** The team stopped working to per-day deadlines; work is now sequenced by dependency in **THE BUILD ORDER** at the top of this file. **31 July (presentation) is unchanged and is still the only hard date.** Kept here because the dates below are what was actually committed to at Meeting 1, and deleting them would erase the evidence of how far the plan moved.

| Dates | Milestone | Owner |
|---|---|---|
| **12–15 Jul** | Repo, DB schema, **scheduling engine + full test suite**. Garmin export pulled and inspected. | |
| **16 Jul — MIDPOINT** | **GO/NO-GO GATE:** engine done and property-tested; **one real Garmin metric in the database driving one real recommendation.** *(SRS: FR-WER-10)* | |
| **17–21 Jul** | Backend API, task CRUD, reschedule triggers, recommendation rules, libraries seeded. | |
| **17 Jul** | ⚠️ **Frontend starts NOW, against a mocked API.** Do not wait for the backend. | |
| **22–27 Jul** | Frontend: all three views. | |
| **28–30 Jul** | Integration, bug fixing, **≥3 full demo rehearsals.** | |
| **31 Jul** | **PRESENT.** | |

---

## 🎬 The Demo We're Building Toward

Everything above serves this one sequence. It's worth having all three of us able to describe it:

> Inject a sleep score of **40** and **850 active calories** for today. The scheduled high-intensity 3-mile run is **automatically replaced** by a recovery session. The day's calorie target rises from 2,000 to **2,850**. The recovery session is placed onto the **real calendar, by the same engine that places everything else.** Then we drop a fixed commitment on top of it — and it **automatically re-places itself**, telling the user why.

**It requires no network, no device, and no third party. It cannot fail for a reason outside our control.** That was a deliberate design goal, not an accident.

---
---

# 📖 DECISION LOG

*Every decision, with its date and reasoning. Add a row whenever we settle something. **The reasoning is the valuable part** — it's what stops us re-arguing settled questions.*

| Date | Decision | Reasoning | Decided by |
|---|---|---|---|
| **28 Jul** | **Packet 15c (the analytics frontend view) authored — `prompts/frontend/15c-frontend-analytics-view.md`. Two omissions from the §3.7.3 wireframe are made explicit and binding: (1) the COMPLETION-TREND chart is out (already known); (2) NEW — the "Last 30 days ▾" PERIOD SELECTOR is out too.** 15c is a display-only BUILD packet (no frozen suite — FR-ANL-04/UI-04 are (D)) that renders `GET /analytics` and adds no backend, mirroring 14b. It builds the per-habit streak/completion-rate table + the FR-ANL-03 explainer note only. **The period selector is deferred because 15b's `GET /analytics` computes over a FIXED server-chosen trailing-365-day window (`from`/`to`/`asOf`) — a user-selectable period would require a 15b backend change, out of bounds for a display-only view packet.** The view instead states the window the server returned. One display-side rule pinned in the packet: a habit with `scheduled === 0` (no elapsed occurrences yet) renders its rate as **"—", not "0%"** — the display mirror of FR-ANL-02's elapsed-denominator rule, since 0% would falsely assert the user failed every occurrence. | FR-ANL-05 (trend chart) is Conditional and the 26 Jul freeze put every remaining Conditional definitively out (§6) — already logged in the 27 Jul five-item plan (row above). The period selector was NOT previously recorded and is the wireframe trap a future builder would most likely fall into: it looks like a trivial dropdown but the backend serves one fixed window, so adding it silently requires reopening 15b (a frozen-computation packet). Recording it stops that discovery mid-build. The "—"-not-"0%" rule is recorded because it is invisible-if-wrong — 0% is a plausible-looking bug that contradicts the very denominator rule ratified at the 15a gate (v2.31). | **Ryan Woosley** *(owner) · directed with Claude* |
| **28 Jul** | **FR-ANL-01's streak is over a habit's resolved OCCURRENCES, not calendar days — the SRS is NOT amended (OPEN-30 closed "do what the SRS says").** A weekly (e.g. Monday-only) habit completed three Mondays running has a streak of **3**, not 1. Pinned by a new 15a test; the wording-change proposal drafted the previous turn is withdrawn. | The literal "consecutive completed *days*" looked like calendar days, but **ratified FR-ANL-07 (v2.31) already forbids that reading**: a `SUPERSEDED` day between two completions must not break a streak (the pinned SUPERSEDED test asserts streak 2), which a strict calendar-day count gives as 1. Once a *System-excused* day cannot break a streak, a *never-scheduled* day cannot either — the alternative makes the System replacing a workout better for the streak than a rest day, which is incoherent. So the SRS read **whole** compels the occurrence reading with no text change; amending FR-ANL-01 was unnecessary. Reading a single requirement in isolation (FR-ANL-01 alone) would have produced the wrong answer *and* silently broken another requirement — the §0-rule-3 cross-reference discipline resolves the ambiguity, not just contradictions. | **Ryan Woosley** *(owner) · directed with Claude* |
| **27 Jul** | **Two FR-ANL rules ratified at packet 15a's RED gate → SRS v2.31; the RED freeze will encode ratified rules, not a run's guess.** The 15a RED suite (streak + completion rate) had to decide two cases the SRS left open (checked against the SRS on Ryan's ask rather than pinned by fiat — the OPEN-13/§0.1 pattern). **GATE-1 — `SUPERSEDED` in analytics:** a workout occurrence the System **replaced** with a warranted-tier recommendation (FR-REC-02) counts as **neither completed nor missed** — excluded from both terms of FR-ANL-02 and not a streak-breaker (new **FR-ANL-07**). *Rejected: counting it a miss* — that is the exact defect FR-ANL-03's rationale forbids ("a System that broke the user's streak for using its own recovery mechanism would defeat its own purpose"); the supersession is the System's action, not the user's. *Rejected: counting it completed* — the superseded occurrence was not itself done; the replacement is a separate task with its own record. Deliberate asymmetry with FR-ANL-06: a **user-declared** skip never made up **is** a miss (the user chose not to do it); a **System-driven** supersession is not. **GATE-2 — the FR-ANL-02 denominator:** an *occurrence scheduled* is one whose window has **elapsed** in the period; a future `PLANNED` occurrence is in neither term, so a habit is not penalised for a day that has not happened. *Basis: FR-RSC-01/FR-RSC-10 infer a miss only on elapse+retrieval, so an un-elapsed occurrence has no outcome yet* — the SRS never connected that model to the rate; this does. **The SRS supported both directions** (v2.28 kept `SUPERSEDED`/`CANCELLED` distinct precisely because "FR-ANL … may read differently" — §3.6/DR-06/§5) **but stated neither rule** — so both were ratified as decisions and written into FR-ANL (v2.31: FR-ANL-02 amended, FR-ANL-07 added, UC-11 + traceability + index cross-refs). **Now the 15a freeze locks a ratified rule** (§0.1). | An unratified RED run would have frozen a guess into the spec — the same trap OPEN-13 (tie-break) and FR-LIB-08 (relaxation order) were. Ratifying against the SRS's own principles (FR-ANL-03 for GATE-1, FR-RSC-01/10 for GATE-2) keeps the rules the team's, not one session's, and threads the cross-references (§0 rule 3) so the document stays internally consistent. Recorded as decisions because both are invisible-if-wrong: a mis-folded status or an over-eager denominator changes a number no test would otherwise catch. | **Ryan Woosley** *(owner; 15a RED gate) · Claude (surfaced the two escalations)* |
| **27 Jul** | **FR-REC-09's baseline calorie target — and dietary preferences (FR-REC-05) — are captured at REGISTRATION, per UC-01, which the packet-12/13 registration under-built.** Checking the SRS instead of guessing (Ryan's ask) settled what looked like an open fork: FR-REC-09 *(Essential, D, SRS:1384)* says the user *records* a baseline but not where — **UC-01's Basic Path step 3 says where**: "User sets wake time, sleep time, dietary preferences, workout preferences, **and baseline calorie target**," its post-conditions record all of them at account creation, and the ERD carries `baseline_calorie_target` as a **user field** (SRS:853). UC-08 then makes it a *pre-condition* ("the user has recorded a baseline calorie target"). **But the built registration (packet 12/13) captures only wake/sleep (FR-USR-07)** — so baseline calories AND dietary preferences are both unbuilt at the exact point UC-01 says they're captured, and `MetricStore`/`CaloriesToTargetRule` have no real user baseline to read, while `MealCatalog.findMeals(target, dietaryPrefs, …)` has no stored prefs to honor (UC-08 step 3). **Decision:** packet 14a extends `UserRecord` + `POST /auth/register` (+ `GET /user/me`) + the `AuthScreen` form to capture **`baselineCalories`** and **`dietaryPreferences`** (`DietaryFlag[]`), completing UC-01 **minus workout preferences** — deferred because they only drive FR-LIB-08 equipment relaxation, which the wellness view does not demonstrate. Touches Miguel's auth + packet-13 form → flag Miguel before merge (per the 27 Jul ownership decision, "Ryan builds it, flags Miguel"). | The apparent fork (registration vs. settings vs. hardcode) was not a fork — UC-01, the ERD, and UC-08 already specify registration-time capture, and §4.9/CON-05 forbid the hardcode alternative (the System *asks*, never estimates BMR). This is §0 rule 3 in action: an under-built use case left the code contradicting UC-01/UC-08/the ERD, invisible until a reader chased the cross-refs. Capturing dietary prefs in the same pass (cheap while in the form) lets FR-WEL-03's meal plan honor preferences per UC-08 rather than running unrestricted; workout prefs are deferred because nothing in scope reads them. | **Ryan Woosley** *(owner) · Claude (found the UC-01 cross-ref gap)* |
| **27 Jul** | **Ryan's remaining queue (the wellness + analytics views, packets 14/15) is expanded into five work items, because the views have NO backend to render — unlike packet 13, which sat on packet 12's API.** The frontend surface for FR-WEL/FR-ANL does not exist over HTTP: `MetricStore` persists the Daily Metric Set but no route ingests or serves it; `RecommendationScheduler`/catalogs are server-local, unwired; and analytics (streaks, completion rates) has **no code at all**. Adopted sequence — **(14a)** wellness backend endpoints [BUILD, wiring]: serve today's `DailyMetricSet` + ≥7-day history + today's recommendation (workout + 2 alternatives) + meal plan with the calorie target's baseline/activity split, plus a metric-ingestion path — composes existing `MetricStore`/`RecommendationScheduler`/`RecommendationEngine`/catalogs; **(14b)** wellness view [BUILD, frontend] (FR-WEL-01…05, UI-03); **(15a)** analytics computation [**RED**] frozen tests for FR-ANL-01/02/03/06; **(15b)** analytics computation [**GREEN**] + `GET /analytics`; **(15c)** analytics view [BUILD, frontend] (FR-ANL-04, UI-04). **Three sub-decisions:** analytics is its own RED/GREEN pair (FR-ANL-01/02/03/06 are **(T)** with load-bearing edge cases — a rescheduled-then-completed occurrence counts complete FR-ANL-03, a skipped-never-completed one counts as a miss FR-ANL-06, and `CANCELLED`/`SUPERSEDED` placements must not be miscounted — so §4.6 forbids one agent writing both the calc and its test); Ryan owns the backend endpoints too (in Miguel's `server/src/api` surface) and flags Miguel before merge; and **FR-ANL-05 (the completion-trend chart) is Conditional → out** per the 26 Jul scope freeze, so 15c shows streak + completion rate per habit only, no trend. Starting with 14a (first backend, no RED-gate dependency, unblocks UI-03 and pins the contract the view consumes). | The honest picture is that "two frontend packets" hid a backend gap; recording the five-item shape stops a future session discovering mid-build that the wellness view has nothing to fetch (the packet-13 trap of "never saw real data"). Splitting analytics into RED/GREEN honours §4.6 for a **(T)** requirement whose edge cases are exactly where a single-agent test would encode what the code does rather than what FR-ANL-03/06 say. Ryan keeping the backend keeps the 14/15 chain single-owner (§7.2) at the cost of a heads-up to Miguel — cheaper than a cross-person dependency on the critical path. The analytics logic is designed as a **pure** fold over `Placement[]` (mirroring the engine's purity, §4.3), so the (T) claim is testable with zero DB. | **Ryan Woosley** *(owner) · directed with Claude* |
| **27 Jul** | **Packet 11b GREEN done and merged — the catalog + seeded libraries. 134/134 frozen tests green, `npm run verify` 453 green end to end, engine 100%; `wp-11-libraries` merged to `dev` and pushed.** GREEN commit `a40d46d`; diff confined to `server/src/catalog/` (no test/fixture/`Catalog.ts`/contract/manifest touched — `git diff --stat 4b1fd4e -- server/test/catalog` empty). `resolveIntensityTier` (decision B, total); `WorkoutCatalog` seeds a **vendored** `free-exercise-db` subset read from disk (`node:fs`, never fetched — FR-LIB-02, ahead of 16b) and applies the hard ceiling + soft equipment relaxation; `MealCatalog` + a hand-authored `mealLibrary.ts` (a 9×3 all-five-flag calorie backbone + 16 variety dishes) satisfy FR-LIB-07's 32-combo × 11-target ±10% sweep with the decision-D relaxation order; `attribution.ts` + `data/ATTRIBUTION.md` record `free-exercise-db`/The Unlicense (FR-LIB-10). **Two load-bearing calls:** (1) the MODERATE seed is curated to real-equipment exercises, because a bodyweight MODERATE workout satisfies any preference vacuously (`[] ⊆ anything`) and would stop the equipment-relaxation test ever triggering; (2) the all-five-flag backbone works because a dietary query is a subset check, so one maximally-flagged meal covers all 32 combinations. **`RELAXED_TOLERANCE = 0.25` is an unpinned implementation constant** — no test fixes the widen factor (only the order/reporting and that an impossible target stays unsatisfiable), so it is not a documented requirement. **OPEN-04 fully CLOSED** (the meal library was its last open half). **OPEN-28 (catalog ↔ `RecommendationScheduler` wiring) left untouched — the remaining work for the real §6 demo is wiring, not content.** Report: `docs/P11B-GREEN-REPORT.md`. | The libraries are the last *content* the §6 demonstration needs — FR-REC-03 can now draw three real at-tier workouts and FR-LIB-07 covers every dietary combination against any FR-REC-08 target. The vendored-not-fetched discipline is deliberate: it makes FR-LIB-02's "offline recommendation path" true by construction (the file is committed, read from disk), which is exactly what 16b will inspect. The MODERATE curation and the backbone are recorded because both are non-obvious properties of the *seed data* that specific frozen tests depend on — a future re-seed that ignores them silently reddens the suite. `RELAXED_TOLERANCE` is called out so it is never mistaken for a pinned value. | **Ryan Woosley** *(GREEN; the RED suite was a separate session, §4.6)* |
| **27 Jul** | **Packet 11 RED (catalog + seeded libraries) run, gated, and FROZEN at `4b1fd4e` on `wp-11-libraries` — 134 tests RED, all failing because a throwing stub is not implemented (0 compile errors, 0 assertion diffs); typecheck + lint clean.** Suite `server/test/catalog/` (4 suites + `_support.ts`), stubs `server/src/catalog/` (5 files, throwing / interface only). Pins FR-LIB-04 total tier mapping + FR-LIB-03 per-tier durations per seeded workout, FR-LIB-05 (≥5/tier) + FR-REC-03 (3 distinct at-tier), FR-LIB-07 ±10% day-plan coverage swept over the calorie range × every dietary combo, FR-REC-05/FR-LIB-08 dietary + intensity-ceiling as **exhaustive negatives** (all 2⁵ combos × full range, zero violations), the FR-LIB-08 relaxation order (meal type before calorie tolerance) + "say so plainly", and FR-LIB-10 provenance. **Gate: Ryan (second human). The RED test suite was written by a separate fresh agent session (§4.6); the packet prompt was authored in a different session; Ryan reviewed and cleared the freeze.** **Five escalations adjudicated, all ratified:** (1) the seed loader returns a server-local `SeededWorkout {workout, source}` (not a contract change) so FR-LIB-04's per-item "resolves to exactly one tier" is assertable — `findWorkouts` still returns bare `Workout`, so FR-LIB-01's no-leak holds; (2) **ratified constants** — calorie sweep **[1500, 4000] step 250** (the SRS never bounded FR-REC-08's target range; UC-08's 2,850 sits inside) and day-plan split **0.30/0.35/0.35** (provably sound: fractions sum to 1, so per-slot ±10% ⇒ day ±10% by construction); (3) FR-LIB-03 `equipment` is **carried but may be empty** (bodyweight) — the SRS says "carry", not "non-empty", and a sentinel would fabricate data (SRS-faithful deviation from the packet wording); (4) FR-LIB-07 judged **feasible, did not halt** — subset semantics mean a maximally-flagged meal satisfies all 32 combos, so ~21 densely spaced all-free meals cover every combination (flagged as an 11b authoring risk: author *variety*); (5) one **tautological test removed** (it asserted a test-side oracle, would pass in GREEN untouched) and replaced with a catalog-level closed-band assertion, the exact at-boundary seed meal left to 11b (needs seed data RED must not author). **`guard:tests-frozen` green across all seven frozen packets. ⚠️ This is a RED branch — `npm run verify` fails by design (the 134 catalog tests are red); it carries 11b GREEN too and the pair merges green (§8.5).** **NOT pushed, NOT merged.** New OPEN-28 (17b↔11 `Catalog` reconciliation). Report path: this row + the packet. | FR-LIB-04/05/07/08 and FR-REC-05 are **(T)** and §4.6 forbids one agent writing both the test and the code, so the suite is frozen clean-room before 11b exists. The ratified constants matter because an unstated sweep range / split would let one frozen run silently author the FR-REC-08 range the whole meal side is tested against (§0.1) — ratifying them makes them the team's, not a run's. Escalation 4 is the packet's planted hard call: halting would have been wrong because FR-LIB-07 *is* satisfiable (the strictest combo is the load-bearer, not a blocker), and saying so with the reasoning is what stops 11b re-deriving it. Removing the tautological test (esc. 5) preserves the RED invariant that every test fails because a *stub* throws — a test that green-passes against its own helper pins nothing. | **Ryan Woosley** *(gate; RED authored by a separate session, §4.6)* |
| **27 Jul** | **OPEN-04, exercise-dataset half RESOLVED — `yuhonas/free-exercise-db` selected; license The Unlicense (SPDX `Unlicense`), public domain, verified against the repo's own GitHub license metadata, not from memory.** Clears **FR-LIB-10**: The Unlicense permits storage, redistribution, and commercial use with **no attribution and no share-alike** — the exact clause recipe APIs and CC-BY-SA sources fail. Local seed, no runtime call (§4.2). **Fields verified from the live `dist/exercises.json`:** `name, force, level, mechanic, equipment, primaryMuscles, secondaryMuscles, instructions, category, images, id`, with `category ∈ {strength, stretching, plyometrics, cardio, powerlifting, strongman}` and `level ∈ {beginner, intermediate, expert}`. **Concrete FR-LIB-04 tier mapping adopted (documented, deterministic, total over every category×level):** (1) `category == stretching` → **LOW** (mobility/recovery); (2) else `category == plyometrics` → **HIGH** (plyometric work); (3) else by level — `beginner`→LOW, `intermediate`→MODERATE, `expert`→HIGH. This covers strength/cardio/powerlifting/strongman via level and satisfies FR-LIB-04's characteristics table (LOW = stretching + beginner; MODERATE = intermediate; HIGH = plyometric + expert). FR-LIB-05 (≥5/tier) is trivially met by ~800 exercises. **Duration rule for the FR-LIB-03 gap — the dataset has no per-exercise duration — adopt a fixed per-tier default: LOW 20 / MODERATE 30 / HIGH 45 minutes**, documented and deterministic like the tier mapping. *Rejected: (a) switching datasets — a single exercise's "typical duration" is a policy choice, not observable data, so every per-exercise source has the identical gap; the only sources that carry a session duration are whole-**routine** datasets, which lack per-movement `category`/`level` (breaking FR-LIB-04) and carry worse licenses (wger's exercise DB is CC-BY-SA — share-alike copyleft; ExRx/commercial restrict redistribution — the FR-LIB-10 trap). (b) per-category durations — more lifelike, bigger table to document/test, no demo value, since duration only sets the scheduler's slot length.* **The concrete mapping and the duration rule are transcribed verbatim into packet 11 when it is authored** (§0.1/§4.11 — a documented team decision, never one frozen run's arbitrary choice). ⚠️ **OPEN-04's meal-library half stays OPEN**: meals are hand-authored (§4.2 — no dataset, so no FR-LIB-10 question), with a **calorie count** (FR-REC-08's target) and dietary flags, and their calorie distribution must satisfy FR-LIB-07's ±10% sweep — authoring work folded into packet 11. | Verifying the license from the repo's actual metadata rather than memory *is* FR-LIB-10 ("read the terms before importing, not after"); The Unlicense is the strongest possible answer (public domain, zero downstream obligations), which is why it beats otherwise-attractive sources like wger that carry CC-BY-SA's share-alike even though they share the duration gap. The tier mapping keys off exactly the two fields FR-LIB-04 names, so the dataset was almost certainly what FR-LIB-04 was written around; making the rule **total** (a level default with two category overrides) is what lets FR-LIB-04's verification clause — "every seeded workout resolves to exactly one tier" — actually pass. Duration is a policy, not a fact, so no dataset choice removes the gap; documenting the rule (rather than letting the seeding agent pick) is §0.1 applied to seed data — otherwise one run's arbitrary durations silently become the spec. | **Ryan Woosley** |
| **27 Jul** | **FR-LIB-08's "documented, fixed" relaxation order is now DOCUMENTED (SRS v2.29) — surfaced while authoring packet 11, before its RED freeze.** FR-LIB-08 has always required the catalog to relax constraints "in a documented, fixed order" but the SRS never stated the order — an unstated requirement that packet 11's RED suite would otherwise freeze from the RED agent's guess (the OPEN-13 trap, §0.1). **The two HARD constraints never relax, in any order, for any reason: dietary preference and intensity ceiling** (FR-REC-05, FR-LIB-08 — recommending against a stated allergy or above the warranted tier is out of bounds). **The SOFT constraints relax in this fixed order: (1) meal type → (2) equipment / workout preference → (3) widen the ±10% calorie tolerance**; the catalog reports which it relaxed (FR-LIB-08), and where even full relaxation yields no candidate it **says so plainly** rather than returning a hard-constraint violation. *Rejected: widening the calorie tolerance first — the daily calorie target is the wearable-driven claim FR-REC-08 exists to make, so it is preserved longest; a snack served where a lunch was asked is a smaller compromise than a number materially off a target the whole meal side is built around.* Transcribed verbatim into packet 11 (RED). | An unstated "documented" order is a requirement that one frozen run silently authors — exactly what §0.1/§4.11 exist to prevent, and the same class of gap OPEN-13 was for the tie-break. Ratifying it as a team decision before the freeze makes it binding because the team agreed, not because a run did it. Preserving calories last follows from FR-REC-08 being the adaptive thesis on the meal side; meal-type/equipment are preference niceties whose relaxation costs the user far less than missing their energy target. The hard constraints are stated as never-relaxing so no relaxation *ordering* can ever be read as licence to touch them. | **Ryan Woosley** |
| **26 Jul** | **Packet 17c GREEN done on `wp-open27-replacement-durability` — OPEN-27 fixed, the replacement is now durable. `npm run verify` 319 green, engine 100%.** Three changes, no test touched: **(1)** `SUPERSEDED` added to `PlacementStatus` in `shared/src/contract.ts` — the human-authored amendment from packet 17c, transcribed **verbatim** (§4.7: a GREEN agent may not invent a `PlacementStatus` member). **(2)** `RecommendationScheduler.applyWorkoutRecommendation` vacates the replaced above-tier run as `SUPERSEDED` instead of `CANCELLED`. **(3)** `RescheduleService.sweepElapsed`'s re-attempt branch now `continue`s past a task whose date-occurrence is `SUPERSEDED` — so FR-RSC-05's self-heal never reaches a deliberately-replaced run, and the frozen 17c durability test (no resurrection across two idempotent sweeps) goes green. `SUPERSEDED` is deliberately **kept out of `OCCUPIES_TIME`** in both `RescheduleService` and `RecommendationScheduler` (and `app.ts`'s `OCCUPYING`), so it frees the slot for the recovery session exactly as `CANCELLED` did — the vacate still works, only the re-attempt eligibility changed. **Other `PlacementStatus` consumers checked and unchanged:** `app.ts` `EXPORTABLE_STATUSES` already excludes `SUPERSEDED` (a superseded occurrence never happened — correctly not exported, like `CANCELLED`); `TaskRepository.plannedDatesForTask` queries `status: 'PLANNED'` only, so a superseded date is not treated as planned; `OccurrenceBlock.tsx` compares statuses and needs no change to type-check. No exhaustive `switch`/`never` on `PlacementStatus` exists, so widening the union broke nothing. **FR-RSC-03 held** — `guard:single-placement` green, no second placement function introduced. **Diff: 3 files (`contract.ts` + 2 `src`), 0 test files**; `git diff` of every frozen dir (04/06/08/09/17a/17c) empty. **SRS → v2.28** (§3.6 `PlacementStatus` enumeration + §5/DR-06 add `SUPERSEDED`). OPEN-27 closed. ✅ **Merged to `dev` and pushed to `origin`** (§8.5 gate met — owner satisfied, verify green; `dev` already carried 17a/17b at tip `1a0864e`, so this was a clean fast-descendant merge of the 17c RED+GREEN pair). | The fix is exactly the two exclusions `SUPERSEDED` was defined for (busy-set and re-attempt), landing at the single point in `sweepElapsed` where FR-RSC-05's re-attempt is decided — narrower and more honest than a set-aside marker, and it reuses the domain's existing "terminal occurrence status" shape rather than adding parallel machinery. Vacating as `SUPERSEDED` rather than `CANCELLED` also un-overloads `CANCELLED` back to its FR-RSC-09-withdrawal-only meaning, which FR-ANL and the dashboard may read differently. The test asserts only the OUTCOME (mechanism-agnostic), so this GREEN is one valid realization, not the spec — re-running it against a different correct fix would still pass. | **Patrick Rucker** *(GREEN session; separate from the RED author — §4.6)* |
| **26 Jul** | **OPEN-27's fix is design option (b): a new terminal `PlacementStatus` value `SUPERSEDED` — a CONTRACT change (§4.7 team decision), authored as packet `prompts/verification/17c-replacement-durability.md`.** A replaced workout's occurrence is marked `SUPERSEDED` (not `CANCELLED`) — terminal like `MISSED`/`SKIPPED`, excluded from the busy set AND from `sweepElapsed`'s re-attempt eligibility, which is what stops the replaced run resurrecting; per-occurrence, so a recurring task's other dates are untouched. *Rejected: (a) a separate set-aside marker (more machinery than a status the domain already models the same way); (c) modelling the replacement as an edit of the user's own workout — it contradicts 17a's frozen `result.task.source === 'SYSTEM'` assertion and fights FR-REC-04's "created as a task."* The `SUPERSEDED` addition is **human-authored and transcribed verbatim into 17c** (an agent may not add a `PlacementStatus` member, §4.7); the SRS revision for it lands with 17c GREEN (§3.6/§5/DR-06 cross-refs). 17c runs as **RED (durability test, its own frozen dir `server/test/replacement/`, so 17a's `8809158` is not re-frozen) → second-human gate → GREEN** — the durability test asserts the OUTCOME (no resurrection), mechanism-agnostic. | `CANCELLED` was a semantic overload (the contract defines it as an FR-RSC-09 withdrawal), and reusing it would blur two different terminal meanings that FR-ANL and the dashboard may read differently. A dedicated status matches how the domain already records terminal occurrence states and makes "this occurrence was replaced" explicit and greppable. Option (c) was the tempting shortcut but the frozen suite already ratified `SYSTEM` authorship, so it would cost a re-freeze to fight a requirement rather than serve it. Giving 17c its own frozen dir avoids re-freezing a suite frozen hours earlier. | **Patrick Rucker** |
| **26 Jul** | **Scope frozen for the presentation: NO Conditional work will be undertaken.** The presentation is imminent and the remaining Essential work leaves no room for Conditional extras. Only **Essential** requirements are conditions of acceptance (§6, Appendix D). **Definitively NOT started:** FR-REC-10 (weekly meal plan), FR-REC-12 (extensibility demo), FR-WER-11 (live API — closes OPEN-05 unattempted, "having lost nothing"), FR-SCH-07/08 (priority displacement — closes OPEN-06), FR-LIB-09 (external content API), FR-ANL-05 (trend chart), NFR-MNT-02 (70% whole-system coverage), NFR-MNT-09 (reproducibility demo). ⚠️ **The two Conditional items already SHIPPED stay — do not rip them out:** FR-CAL-07 (`.ics` export) and FR-DSH-08 (month navigator) are merged to `dev`, tested, and working; cutting *future* Conditional work does not un-build sunk, passing work. **Remaining work is now all Essential**, and the schedule linchpin is **Ryan's packet 11 (libraries)** — the real §6 demo needs real workout options (FR-REC-03) and the meal/calorie side (FR-REC-08, FR-WEL-03), which the acceptance suite currently only fakes with a `Catalog` double. | Only Essential requirements are conditions of acceptance, so cutting Conditional work cannot cause an acceptance failure (§2.7.1, Appendix D) — it is the scope discipline §6 and OPEN-05/06 were written to enable. Recording it as a decision (rather than letting each Conditional quietly not-happen) keeps the cut deliberate and stops a future session from "finishing" a Conditional under time pressure. The two shipped Conditionals stay because deleting working, tested code to honour a forward-looking cut would be pure loss. | **Patrick Rucker** |
| **26 Jul** | **Packet 17b GREEN done and committed on `wp-17-acceptance` — the FR-REC-04 wiring. Acceptance suite 8/8; `npm run verify` 318 green, engine 100%.** `server/src/recommendation/RecommendationScheduler.ts` now implements the two methods (throwing stubs replaced): `recommendationsFor` reads the day's injected metrics via `MetricStore` and delegates to the content-agnostic `RecommendationEngine`; `applyWorkoutRecommendation` finds the above-tier scheduled workout (FR-REC-02, with FR-REC-07's completed/begun guardrails), draws three warranted-tier options from the `Catalog` (FR-REC-03), creates a **`SYSTEM`/`WORKOUT`/`FLEXIBLE`** task inheriting the replaced run's window+duration (FR-REC-04/FR-TSK-01), vacates the replaced run, and **places the recovery session by re-invoking the injected `findCandidateSlots` verbatim** (FR-SCH-01, FR-RSC-03 — `guard:single-placement` green, no second placement fn). The "defended identically to a user task" clause is the *absence* of code: an ordinary `PLANNED` row the existing `RescheduleService` re-places/moves/withdraws (the suite drives it directly). **OPEN-24 fixed**: `sweepElapsed`'s re-attempt branch now stores an honest `firstPlacementReason` on a first-ever placement instead of the false "no room earlier today." **Diff: 4 `src` files, 0 test files** — no frozen test touched (`git diff --stat 8809158 -- server/test engine/test` empty); frozen suites all still pass when run. ⚠️ **Cross-owner: `TaskRepository.createTask` gained an optional `source?` param (defaults `'USER'`) — a persistence change in Miguel's `server/src/db/`, NOT a placement path. Miguel to be given a heads-up and should ack before this branch merges to `dev`.** ⚠️ **Known gap → OPEN-27 (below), demo-blocking:** the vacated (now-`CANCELLED`) *replaced* workout has no `PLANNED` row, so `sweepElapsed` would re-attempt it on the next retrieval — the replacement does not survive a sweep. Not caught by 17a's suite (a coverage hole), surfaced honestly by 17b; needs a design decision before rehearsals. | 17b's job was to satisfy the frozen 17a suite, which it does (8/8, clean boundary), so it is a correct, committable increment. The durability gap is a *separate* 17a coverage hole, not a 17b failure — the same lesson as the v2.22 replay finding (a suite is only as strong as what it pins), and it is tracked as OPEN-27 rather than bolted onto this GREEN diff because the fix is a genuine design decision (a durable set-aside mechanism) that deserves its own clean-room RED test, not behaviour invented by the GREEN session. FR-REC-04's *wiring* is done and tested; the *replacement* is not demo-safe across a sweep until OPEN-27 lands, so §9 does not claim FR-REC-04 demonstrated. | **Patrick Rucker** *(self-gated)* |
| **26 Jul** | **Packet 17a authored, run (RED), self-gated, and FROZEN on `wp-17-acceptance` (off `dev`) — the acceptance suite pinning FR-REC-04 and the §6 demonstration.** *(Committed later the same day: `8809158` RED freeze, `aaefbd1` manifest, `d0538d8` docs — see the 17b row above.)* `prompts/verification/17a-acceptance-suite-RED.md`; the wiring surface `server/src/recommendation/RecommendationScheduler.ts` (throwing `Error('17b')` stubs only); the suite `server/test/acceptance/` (8 tests, one dedicated dir per the 25 Jul E3 rule); report `docs/P17A-RED-REPORT.md`. **RED verified:** 6 fail / 2 pass — the 5 §6-spine tests fail at the `Error('17b')` stub (the FR-REC-04 centerpiece: a fixed commitment dropped on a recommended workout re-places it via FR-RSC-02, Appendix A's 17:00→17:45); the 6th (OPEN-24) fails at an assertion against *existing* code (a real bug 17b fixes). typecheck/lint clean, all four prior freeze guards + single-placement + engine-purity green, no frozen file or contract touched. **Frozen with NO provenance sha** — `npm run freeze` auto-adopted dev-tip `ca587b2` (untracked files are invisible to `git diff HEAD`), but `ca587b2` does not contain these files, so the false sha was cleared by hand (`shaPendingCommit` note in `scripts/frozen-tests.json`); it backfills via `--sha <freeze-commit>` when Patrick commits — the identical P08 provenance situation. **Gate: self-gate by Patrick (owner), teammates unavailable and having given explicit green light** — waived on the record like packet 06, but on availability+authorization, not inexperience. **Five escalations adjudicated:** (1) `RecommendationScheduler` kept as a dedicated collaborator and ratified into §3.6 (v2.27) — not folded onto the content-agnostic, frozen-suite-bearing `RecommendationEngine`; (2) `Catalog` seam accepted minimal, packet 11 owns the full signature; (3) SYSTEM-task creation gap flagged for 17b (needs a `source:'SYSTEM'` path in Miguel's `db/`, not a second placement fn); (4) the §6↔FR-RSC-09 contradiction fixed by amending §6 (v2.27) so the withdrawal is shown on a *miss*, not the skip; (5) **the two OPEN-22(2) tests are already GREEN and are KEPT as regression guards — establishing the convention that a RED freeze MAY contain a green regression guard** where it pins shipped-but-unfrozen behaviour a requirement names (here, SRS v2.20's re-attempt provenance, the OPEN-21 fix). | FR-REC-04 is Core and verified by TEST, and a test written by whoever writes the wiring encodes what the wiring does, not what the requirement says — so it is frozen in a clean-room RED session before 17b exists (the whole reason 17 was split, 22 Jul). Escalation 5 is the sharp one: OPEN-22 is *assigned* to 17a's gate, 17a's clean-room is the correct author (packet 06 could not construct the case — before packet 12 nothing could hand the service a task with no placement history), and freezing a satisfied requirement to stop it regressing is precisely what OPEN-22 asked for (*"nothing would fail if the default came back"*). Moving it to packet 06 would re-freeze `c30e784` for a case that suite could not reach; dropping it reopens the regression window. The green-in-RED-freeze convention is recorded here because it is a genuine method decision that outlives this packet. The self-gate is honest only because it is disclosed with its authorization; the freeze runs no commit because §8.5 forbids an agent asserting a human read the diff. | **Patrick Rucker** *(self-gated; teammates green-lit)* |
| **25 Jul** | **`RecommendationReason` is single-metric by design; multi-metric reason semantics are deferred to FR-REC-12 (Conditional).** The contract's `RecommendationReason` carries one `metricName`/`metricValue`, while `RecommendationRule.requiredMetrics()` returns an array — so for a rule consuming several metrics, WHICH one drives the reason is unspecified by FR-REC-13. Both Essential rules (`SleepToIntensityRule`, `CaloriesToTargetRule`) declare exactly one metric, so the question does not arise in this release, and packet 09's RED suite exercises only single-metric rules (it invents no multi-metric reason rule). A combined metric+rule (e.g. stress + HRV) is **FR-REC-12, Conditional** (§6, deferred until all Essential work is verified); if realized it must decide the driving metric then. **Raised by the packet 09 RED session (fresh window) as judgment call #1; recorded, not resolved by widening the contract now.** *(Also from the same run, recorded not as decisions but as confirmed-correct calls: fixtures inlined under the frozen path rather than shared `test/support/*` — the packet-08 E3 freeze-isolation lesson; `metricName` populated even on the unavailable path; out-of-range sleep/negative-calorie inputs left untested per the escalation clause.)* **Output-array ordering left unpinned** — the suite asserts one recommendation per rule but not their order; a conscious choice, revisited at packets 14/15 if deterministic rendering needs it (registration order the natural default). | Widening `RecommendationReason` to carry multiple metrics, or picking a driving-metric rule, before any multi-metric rule exists would freeze a guess into the contract for a Conditional feature — the §0.1/§4.11 trap. The single-metric reason is correct and complete for every Essential rule; the gap is real but belongs to FR-REC-12's own design, not this packet. Leaving ordering unpinned keeps the frozen suite from asserting a behaviour no requirement states, and it can be pinned later where a consumer actually needs it. | **Ryan Woosley** |
| **25 Jul** | **Contract amended — `Recommendation`, `Decision`, `CalorieTarget`, `RecommendationReason` added to `shared/src/contract.ts` (SRS v2.26), realizing the recommendation-output types the §3.6 class diagram has named since the recommendation architecture was drawn.** Filled ahead of packet 09 (rules RED), not during it — a frozen test run must not be where the output shape is first decided (§0.1, §4.11), the same discipline as `Task.recurrence` before packet 12. **Decisions: (a) the rules and `RecommendationEngine` stay server-local (`server/src/recommendation/`); only the DATA enters the contract** — because the wellness views (FR-WEL-02, packets 14/15) render it and a server-local type would be re-declared web-side, the exact duplication §4.7 exists to prevent. *Rejected: a server-local output type (duplicate later); the `RecommendationRule` interface in the contract (a service abstraction does not belong in a data contract — none of the other §3.6 services, `RescheduleService`/`WearableAdapter`, are there either).* **(b) `Decision` is a discriminated union on `kind`** matching the diagram's two rules (`SleepToIntensityRule` → `IntensityTier`; `CaloriesToTargetRule` → `CalorieTarget`); `Recommendation` bundles a `Decision` with a `RecommendationReason` carrying FR-REC-13's metric/value plus a `usedFallback` flag (the UML omits the reason). `metricValue: null` ⇔ metric unavailable, kept distinct from a measured zero (DR-02, NFR-ROB-01). ⚠️ **Caught by chasing the cross-reference (§0 rule 3): the first-pass draft invented `Rule`/`ruleRegistry` and a `Recommendation` union, diverging from §3.6's already-drawn `RecommendationRule`/`RecommendationEngine`/`Decision` — reading the class diagram before freezing anything realigned both the contract AND packet 09 to the names the spec already set.** The §3.6 diagram needed no change; no existing requirement changed. `npm run typecheck` green after the amendment. | The types living in the diagram but not the code is exactly the gap packet 09 would otherwise fill silently — whoever ran it first would mint the shape, and one frozen run's arbitrary choice becomes the spec (§0.1). The name divergence the first pass introduced is the concrete argument for §10: the SRS, not a fresh design pass, is the authority. Server-local rules / contract-data is the seam every other module already follows (engine, reschedule, adapter all sit this way). | **Ryan Woosley** |
| **25 Jul** | **Packet 08's freeze-manifest provenance `sha` corrected from `40a3447` to `1199d80` — a provenance correction, NOT a re-freeze.** `npm run verify` was failing on `guard:tests-frozen` with a P08 *provenance mismatch*: the manifest's recorded `sha` (`40a3447`) is the **parent** of the RED freeze commit, a commit where `server/test/wearable/` does not yet exist, so the working tree "differs" from it and the guard (working exactly as designed) failed the build. **Root cause: `npm run freeze` records `HEAD`-at-freeze-time, and it was run before the RED commit existed**, capturing the parent. **The suite itself is untouched** — all four file hashes still verify against the manifest (the guard's *primary* check passed), and `git diff --stat 1199d80 -- server/test/wearable` is empty (the 08b GREEN commit never touched a test file). Fixed by hand-editing `scripts/frozen-tests.json`: `sha` → `1199d80` plus a `shaCorrectedFrom` note; **`npm run freeze` was deliberately NOT re-run** — that would record the GREEN commit and re-hash, i.e. the go-green re-freeze §4.12 forbids. `npm run guard:tests-frozen` green after the edit. Packet 09's DoD updated to run `freeze` *after* its RED commit so this does not recur. | The hash-based primary check is what makes this correction unambiguously benign rather than a disguised weakening — exactly the enforcement-vs-provenance split §4.12 draws: hashes enforce, the `sha` only attests *when*. Changing a manifest entry is a team decision (§4.12), logged here as such. The trap is structural, not careless: §8.3's correct RED workflow commits the tests and the manifest *together*, so freezing before that commit always records the parent — which is why the manifest comment already blesses a no-`sha` entry for the "frozen before commit" case. Patrick owns the freeze convention and should confirm this (or prefer dropping the `sha` entirely, which is equally valid here). | **Ryan Woosley** *(Patrick to confirm — convention owner)* |
| **25 Jul** | **E2 settled by reading the real export, and OPEN-02 corrected in the process. Sleep scores are KEPT — abandoning them was considered and rejected.** **(1) OPEN-26 CLOSED — the real export contains no intra-file duplicate `calendarDate`s.** Every date is unique in both the UDSFile (active calories, `2026-02-05→05-16`) and the sleep file (`2023-12-16→2024-10-03`); the RED agent's "2026-04-01 twice" was its synthetic fixture, and that date isn't even in the real UDSFile (it falls in a gap). So *last-write-wins per `(date, metric)`* stays only as a defensive default (consistent with FR-WER-09's upsert) and **08b GREEN needs no duplicate-resolution logic** beyond the upsert. **(2) OPEN-02 was slightly wrong and is corrected: an unavailable sleep score is the `overallScore` key being ABSENT from an otherwise-present `sleepScores` object — NOT `overallScore: null`.** Real cases: 2024-01-11 and 2024-09-19, both `sleepScores: {feedback, insight}` with no `overallScore`. The RED fixture and the adapter's guard must key on **presence** (`'overallScore' in sleepScores`), since `sleepScores.overallScore ?? 0` on a missing key still yields 0 — the exact FR-WER-06 bug. **(3) Abandoning sleep score was raised and rejected**: it is FR-REC-01/02 (Essential), it is FR-WER-10's chosen demonstration, and it is "poor sleep lowers workout intensity" — the sentence the project is about (per the 22 Jul mixed-vintage decision, which already rejected swapping the driver metric for the same reason). The null-key wrinkle that prompted the question is a one-line guard, not a reason to cut a Core-adjacent metric. | Reading the file answered E2 in a way the spec could not: the winner-rule is real Garmin behaviour in general but does not occur in *this* export, so it drops from a required behaviour to defensive coverage. The OPEN-02 correction matters because a fixture built around `null` would test a shape the adapter never sees, and the real 20 usable scores (14→83, all three tiers, an exact 75) are a clean basis regardless. Keeping sleep score is scope discipline in the other direction — §6 says cut from the deferred list (live API, displacement), never from the Core demonstration. | **Ryan Woosley** |
| **25 Jul** | **Packet 08 RED ran and raised three escalations; all adjudicated.** **E1 — the sleep score's unit token is `score`** (SRS v2.25, FR-WER-03). FR-WER-02 makes `Metric.unit` mandatory but the table specified no token for a 0–100 index; ratified rather than left to the frozen test, so it is a team decision, not one run's default (§0.1). **E2 — which record wins when one export holds two records for the same `calendarDate` is DEFERRED to OPEN-26**, owned by Ryan, due before 08b GREEN. The RED test correctly asserts only *collapse-to-one*; 08b must not invent a winner. The likely rule is *last-write-wins per `(date, metric)`* (consistent with FR-WER-09's cross-ingest upsert), but it must be checked against the real export first — the failure mode is a later `overallScore: null` record discarding an earlier real measurement, which is only correct if Garmin's later record is genuinely authoritative. **E3 — a packet's frozen suite must be ONE dedicated directory, fixtures and harnesses included** (`hashSuite` fingerprints the whole directory, per NFR-MNT-08). The packet had placed `MetricStore.test.ts` in `server/test/db/` and the loader in `server/test/support/`, both shared with packet 12 — freezing there would sweep packet-12 tests into packet-08's manifest and trip packet-08's guard on any future packet-12 edit. Fixed by consolidating all packet-08 test artifacts under `server/test/wearable/` (mirroring how 04 froze `engine/test` and 06 froze `server/test/reschedule`); the packet's file list was corrected to match. **RED re-verified after the move: 20 new tests fail at a thrown stub, 217 pre-existing pass, `tsc`/`eslint` clean. Not yet frozen — awaits the second-human gate (§4.6).** | E1: recording the token in the SRS converts an arbitrary run choice into a ratified fact, which is the whole of §0.1's argument — a frozen test's incidental decisions must not silently become the spec. E2: deferring is correct because the rule depends on the real data's shape (is there a per-record recency signal?), which is a Data-Lead question against the export, not something derivable from the spec or inventable by an agent. E3: the one-directory-per-frozen-suite invariant was followed by 04/06 but never written down; it surfaces only when you trace the RED suite all the way to its freeze step, and co-locating fixtures under the frozen dir means a fixture edit is caught by the same guard as a test edit — which is correct, since a fixture change silently alters what the suite asserts. | **Ryan Woosley** |
| **25 Jul** | **OPEN-20 CLOSED — the absent-vs-measured-zero discriminator for `activeKilocalories` is the presence of the `totalSteps` key.** `totalSteps` present → the day was measured, so a `0.0` is a real rest-day zero (FR-WER-06 *available*, value 0); `totalSteps` absent → the watch supplied nothing, so the metric is recorded *unavailable*. This is now written into **packet 08 as a stated constraint** the adapter is *given*, never something it infers. | The rule was inferred from the real export, not documented by Garmin, so it is a team decision, not an agent's derivation — the same logic as §4.9/§0.1. Getting it wrong is **invisible**: `activeKilocalories` is a valid float either way and no test fails, so the adapter must be *told* the rule rather than left to guess it. **`totalSteps`-presence separated the two correctly on all 88 days checked** (2026-03-17 carries `totalSteps` with 26 steps — a real measured near-zero; a not-worn day carries no `totalSteps` at all). **`includesActivityData` was rejected as the discriminator** — 2026-02-13 shows 721 active kcal with `includesActivityData: false`, so it tracks something else and would misclassify a real day as absent. This is FR-WER-06 / DR-02's *"an absent metric and a measured zero are different facts"* made operational for the Garmin adapter. | **Ryan Woosley** |
| **24 Jul** | **FR-CAL-07 (the `.ics` export) built on `wp-cal-export`, off `dev` — deliberately started ahead of the stated sequencing** (this file's "🔁 THE SCOPE CUTS" section lists FR-CAL-07 among items "NOT started until all Essential work is verified," which is not yet true — Ryan's wearable/recommendation chain and the FR-REC-04 wiring are still pending). Owner's call: it's Conditional, isolated (touches no engine/reschedule code), and doesn't compete with anyone else's queue, so the risk of starting it early is low even though the general rule against Conditional-before-Essential still holds for everything bigger. **New backend**: `server/src/calendar/ics.ts` (pure — no clock read, no DB, no network; `now` is passed in, same discipline as `Task.createdAt`) builds an RFC 5545 document with line folding (§3.1) and text escaping (§3.3.11); DTSTART/DTEND are emitted as **floating local time** (no `Z`, no `TZID`) because no type in this System (`Task`, `Placement`, `User`) carries a timezone — floating time is the only form that doesn't fabricate one. New `TaskRepository.placementsInRange`/`getTasksByIds` and `GET /schedule/export?start=&end=` (bounded to 366 days), excluding only `CANCELLED` placements (a withdrawn automatic reschedule never actually occupied that slot — FR-RSC-09). New frontend: an "Export .ics" button in `ScheduleView`'s nav bar, downloading the current day via `exportScheduleIcs` (`web/src/api/client.ts`'s second direct `fetch`, alongside `request()`, since the response is `text/calendar` not JSON) and a client-side blob-URL download. 20 new tests (pure `.ics` formatting, repository range queries, and full HTTP round trips including cross-user isolation and range-bound rejection). **Verified two ways**: `npm run verify`: **263/263 green**, engine still 100% coverage, all four guards clean; and a live `curl` round trip against the running dev server (register → create a FIXED task → `GET /schedule/export` → correct `Content-Type`/`Content-Disposition` headers and a well-formed `VCALENDAR`/`VEVENT` body → account deleted after). **Not done: an actual import into a real calendar application** — FR-CAL-07's own verification line asks for that explicitly, and it wasn't performed this session to avoid writing an event to anyone's real calendar without asking first. Flagged, not silently claimed. | The team's own sequencing note exists to stop Conditional work crowding out Essential work under time pressure, not to forbid a scoped, non-blocking side task — FR-CAL-07 touches none of Patrick's or Ryan's files and cannot regress FR-REC-04. Recording the deviation here (rather than just doing it quietly) is what keeps this section's guidance meaningful for the next, bigger Conditional item someone might be tempted to start early. | Miguel Alvarez |
| **24 Jul** | **On `wp-13b-frontend-followups` (off `dev`): a visual redesign of the dashboard, a new collapsible month-grid navigator (FR-DSH-08, SRS v2.23), and a light/dark toggle — plus a `/simplify` cleanup pass afterward.** New backend surface: `TaskRepository.taskTypesInRange` + `GET /schedule/overview`, reading task **definitions** (not `Placement` rows, per FR-RSC-10's lazy evaluation — see FR-DSH-08's SRS note), bounded to 62 days, 4 new server tests. New frontend: `MonthCalendar.tsx`, `theme.ts` (a `data-theme` override layered under the existing `prefers-color-scheme` media query, so an explicit pick wins over the OS setting either way), and a `styles.css` rewrite using the same validated categorical palette method the `dataviz` skill uses for chart series, mapping each `TaskType` to a fixed hue — decorative only, never the sole carrier of a meaning the legend already requires in text/symbols (═/─/✦/↻, the FIXED badge). **Cleanup pass found and fixed real duplication**: five near-identical circular-button CSS blocks consolidated into one `.icon-button` base + size/style modifiers; two `[aria-label]`/`:last-child` structural selectors replaced with explicit classes; the `.occurrence__dot--*`/`.month-calendar__dot--*` TaskType-color mapping (12 rules) merged into one shared `.type-dot--*` block (6 rules); `MonthCalendar` memoized (`React.memo` + a stable `useCallback`'d `onClose`) since it was rebuilding its 42-cell grid on every unrelated `ScheduleView` re-render. `npm run verify`: 247/247 green throughout. | The month view is genuinely new UI scope with no prior requirement, so it got one (FR-DSH-08, Conditional — convenience on top of the Essential day view, not part of §2.7.1's Core Demonstrable Capability) rather than shipping unrecorded. Sourcing the overview from task definitions rather than placements was deliberate: a placements-only version would under-report every day nobody had opened yet, which is most of a calendar month by construction. | Miguel Alvarez |
| **24 Jul** | **A real browser finally exercised `ScheduleView` (rendering it had never been checked before — only `curl` — per `docs/P13-REPORT.md`'s own admitted gap), and it crashed on load: `ScheduleView.tsx:110` read `data?.awaitingChoice.length`, and `?.` only guards `data`, not `.awaitingChoice`.** With no error boundary, the whole tree unmounted, leaving the page's `canvas`/`canvastext` background (black under a dark-mode browser) with nothing rendered — reported by the user as "a black screen." **Fixed**: `awaitingChoiceCount` is now computed once, defensively, in the existing derived-state `useMemo` (`(data?.awaitingChoice ?? []).length`), same pattern as `taskById`/`placements`/`unplacedTasks` there. The live server was independently confirmed (via a fresh `curl` register + `GET /schedule`) to already return `awaitingChoice: []` correctly, so this was a frontend defect, not a stale backend. Both dev processes (`tsx watch`, Vite) had also been running 1–2 days across today's merges, so they were restarted too, to remove staleness as a variable. `npm run verify`: 243/243 green after the fix. | The chaining bug was real and independent of process staleness — `data?.awaitingChoice.length` throws whenever `awaitingChoice` is undefined for *any* reason, not just this one; hardening it removes a whole class of future crash, not just today's. This is the exact case `CLAUDE.md`'s §9 already flagged as unverified risk: packet 13 was confirmed only against the API surface, never against its own rendered output. | Miguel Alvarez |
| **24 Jul** | **`wp-13-frontend-dashboard` and `wp-replay-guards` both merged to `dev` today** — Patrick's guard rework first (clean), then this branch (three conflicts, all confined to `CLAUDE.md`/`docs/TEAM-MEETING.md`/`docs/SRS-v2.md`'s narrative text, zero code conflicts). `npm run verify` on `dev` post-merge: 243/243 green, both freeze entries verified by the new digest-based guard. | Both branches had been sitting unmerged past §7.1's "integrate daily" for a few days; merging kept branch lifetime from growing further per §8.5's own reasoning ("a branch that cannot be merged today has grown too big"). | Miguel Alvarez |
| **24 Jul** | **Packet 16a's Check C (the FR-RSC-03 re-implementation vocabulary tripwire) is narrowed to the SRS-derived terms only — `placeTask` and `scheduleTask` are removed from the list.** Guards `scripts/guard-single-placement.mjs` and `scripts/guard-engine-purity.mjs` run on `wp-16a-guards`, re-validated clean against `dev` with 12/13 present, and merged; full write-up in `docs/P16A-REPORT.md`. | Validating the guard against `wp-13-frontend-dashboard` (per the packet — run it against the code most likely to violate FR-RSC-03 *before* that code merges) **fired Check C on `web/src/api/client.ts:113`'s `placeTask`** — which is a frontend HTTP transport wrapper that `POST`s a slot the user *already chose*, computing nothing. **A false positive, and in vocabulary I had added beyond the packet's SRS-derived example list.** The packet says the narrowing is the owner's call, not the agent's, so it was escalated rather than silently patched. **`placeTask`/`scheduleTask` name orchestration/transport, not the SRS's act of *computing* a slot** (find a free gap, merge intervals, choose a candidate) — they legitimately appear in UI clients and services. **No enforcement lost: a genuine second engine named `placeTask` is still caught by Checks A/B**, which key on the type signature (`findCandidateSlots` definition; a function returning `PlacementResult` or bodied `FindCandidateSlots`), not the name — Check C is explicitly only a tripwire for the documented *"tidy local `findNextFreeSlot()` helper"*. *Rejected: keeping the terms and asking Miguel to rename `placeTask`* — renaming correct transport naming to satisfy a tripwire, and it re-fires on any future legitimate `placeTask`/`scheduleTask`; "a broad guard that gets disabled in three days" is exactly what the packet warns against. Both guards now pass on `wp-13`; `ci.yml` also gained the missing `guard:tests-frozen` step and `fetch-depth: 0`. | **Patrick Rucker** |
| **24 Jul** | **Reproducibility test run (engine + reschedule, packets 04–07 on a throwaway branch) — the mechanism PASSED, and it surfaced three things now fixed/recorded.** The mainline is untouched; nothing regenerated was merged. | **The loop works, proven twice**: delete the code → RED (fresh session) → gate → hash-freeze (no commit) → GREEN (separate session) → green `verify`, for both the engine (27 regenerated tests vs the mainline's 30) and the reschedule service (66 vs 154). FR-RSC-03 held — one injected engine, re-invoked, no second placement function — and `taskOrder`'s non-transitive-comparator subtlety (v2.14 E5) came back correct, so the adjudicated escalations are genuinely *in* the spec. **The count differences are the point** (independent enumeration, not restoration), which is exactly why a replay's suite must never flow back to the mainline. **Finding 1 (the important one): the regenerated reschedule suite was WEAKER** — it omitted FR-RSC-05's displaced-or-edited-still-`PLANNED`-cannot-re-place deletion (`deletePlacement`, v2.16), and the GREEN impl **passed while being wrong on it**, since nothing tested it. The SRS was complete (§3.6 names the method); the case is simply subtle enough to miss in a from-scratch derivation. **Fix: promoted to a named required-test-case in packet 06's RED prompt**, the way FR-SCH-09's boundary matrix already is; SRS NFR-MNT-09 gained the honest limit (v2.22). **Finding 2: a real bug in `freeze-tests.mjs`** — it recorded `rev-parse HEAD` as provenance unconditionally, but the tree never matches HEAD at freeze time (replay: different suite; normal flow: pre-commit parent), so `guard-tests-frozen` reported a **false provenance mismatch** and the replay couldn't go green until it was fixed to record HEAD only when the tree matches it. Adopted after review. *(The replay agent edited the guard file in place to get past it — outside its packet scope; it should have escalated. The fix is correct and is now landed deliberately.)* **Net: the mainline's curated suites stay authoritative; a replay demonstrates the method, it does not replace them.** | **Patrick Rucker** |
| **24 Jul** | **A frozen test suite is pinned by a sha256 per file, not by a commit.** `npm run freeze -- --packet NN --path <dir>` records the digests; `npm run guard:tests-frozen` re-hashes and compares. The git sha is still recorded **as provenance, not enforcement**. SRS **NFR-MNT-08** (v2.21); `CLAUDE.md` §4.12. | A commit-based freeze needs a repository **and** that commit reachable — neither holds in a shallow CI checkout, and **neither holds during a rebuild from the specification**, which freezes its suites before it commits anything. **A freeze that only works in the repository that created it cannot protect a rebuild, which is the case it most needs to protect.** **Strictly stronger than what it replaced**, not a relaxation: it also catches a file **quietly added** to a frozen suite, which the old `git diff --stat` form reported but nothing named. **The sha is kept and is not decoration** — it is what catches a suite edited and then re-frozen to cover the edit, which the hashes alone cannot see; the guard reports that as a *provenance mismatch*. **Verified by watching it fail in all four modes** (modified / deleted / added / edit-then-re-freeze) before it was trusted. *Rejected: keeping the commit-based check and requiring `fetch-depth: 0` in CI — it fixes the shallow-clone half and does nothing for the rebuild half.* | **Patrick Rucker** |
| **24 Jul** | **`scripts/` and the `engine/**` lint overrides are HUMAN-AUTHORED and transcribed verbatim, like `shared/src/contract.ts`** — delivered by the new `prompts/foundation/02b-executable-guards.md`, which runs **after 02 and before 04**. SRS **NFR-MNT-07** and **NFR-MNT-09**'s named exemption; `CLAUDE.md` §4.12. | **No packet created any guard.** `guard-engine-deps.mjs`, `guard-tests-frozen.mjs` and `frozen-tests.json` were hand-added on 22 Jul outside the pack, and the `engine/**` `no-restricted-imports` overrides live in a file packet 02 creates but does not specify — **so a rebuild from the packets produced an application with no freeze guard and no purity guard, and a `verify` chain calling scripts that did not exist.** Made human-authored rather than generated because **you cannot verify a verifier with the thing it verifies**: a regenerated guard that came back weaker is indistinguishable from a correct one — **both exit 0**, nothing fails, nobody is told. **This is not a precedent for §0.1 generally**: no coverage figure or property test measures `scripts/`, so transcribing it costs nothing, which is exactly why the same move on `engine/src/` would be fatal. *Rejected: specifying the guards in prose and letting each run derive them — a tidier claim, bought by trusting the thing that catches untrustworthy output to itself be untrustworthy output.* | **Patrick Rucker** |
| **24 Jul** | **Reproducibility from the specification is written down as a requirement — SRS NFR-MNT-09, Conditional (D) — and the run sheet is `docs/REPLAY.md`.** Packets 04/06/07 had their **hardcoded test counts and freeze shas removed**; pass conditions are now relational (*"every test in the suite"*, *"the sha recorded in the manifest"*). | The goal is to delete every generated file, run the packets in order, and get the application back **with each suite written and frozen by that run**. Three things blocked it and all three were mechanical: **no packet created the guards** (above); **packets 05/06/07 cited this run's shas as instructions**, one of which — `8b24320` — **was already wrong**, since packet 06 was re-frozen at `c30e784` and the prompt was never updated; and **packet 07 made "all 154 tests pass" its definition of done**, which a rebuild falsifies on arrival. **Conditional, not Essential, deliberately**: it is a property of how the System is built, not of what it does, and it cannot be demonstrated in full until packets 08–11 and 14–17 exist — **promoting it sooner would make an incomplete prompt sequence an acceptance failure**, which §2.7.1 exists to prevent. **The claim is deliberately narrow: not the same code, but the same requirements met by an independently derived implementation** — because carrying one run's implementation into the specification is exactly what would make NFR-MNT-01's coverage and NFR-COR-01's property test measure the code against itself and stay green while measuring nothing. | **Patrick Rucker** |
| **23 Jul** | **OPEN-25 CLOSED — server tests get a bounded automatic retry (`jest.retryTimes(2)`) instead of capping Jest's worker parallelism.** New `server/test/support/jest.setup.ts`, wired via `setupFilesAfterEnv` in `server/jest.config.cjs`. | Chased the flaky FR-TSK-07/DR-01 test by force-repeating `npx jest --coverage` rather than leaving the earlier hypothesis unconfirmed — reproduced it with a sharper error on a *different* test: `Port "52703" already in use`, thrown by `mongodb-memory-server-core` itself. Root cause: every server test builds its own Mongo instance per `beforeEach`, and the library's ephemeral-port selection has a known race under Jest's parallel workers (no `maxWorkers` cap, 5 server test files can run concurrently). **Rejected: `maxWorkers: 1` / `--runInBand`** — reliable, but serializes all 5 server test files on *every* future `npm run verify` to fix a race that fails roughly 1 run in several dozen; a capped retry costs nothing when nothing fails and absorbs the rare case. Verified clean across 7 full coverage runs after the fix. | Miguel Alvarez |
| **23 Jul** | **OPEN-09 CLOSED — Ryan takes packets 14 and 15 (the wellness and analytics views); Miguel keeps 12 (backend) and 13 (the Core schedule dashboard).** Miguel has now agreed to Patrick's 22 Jul direction. | Balances the load once Ryan's wearable chain empties after packet 11, and keeps the Core dashboard (FR-DSH-01/05, the view the demo actually runs through) with the person who built its backend and already knows `server/src/api/app.ts`. Confirmed rather than re-argued: the alternatives were already weighed and rejected on 22 Jul (Ryan takes only view 15 — leaves Miguel with three; all three views stay with Miguel — four packets on one person, against §7.3's "reviewing is the bottleneck"). Ryan's queue is otherwise idle after 11, which is exactly when the hand-off is cheap. | Ryan Woosley · Miguel Alvarez (agreed) · Patrick Rucker (directed) |
| **23 Jul** | **Local manual dev/testing against Mongo uses MongoDB Atlas (cloud), not a locally-run `mongod` process.** `server/.env` (gitignored, machine-local, never committed) now holds an Atlas `mongodb+srv://` connection string instead of pointing at a standalone `mongod`. | Miguel's preference: no local database process to start/manage/remember to kill. **`MONGODB_URI` is the only key `server/src/index.ts` reads** — an initial attempt at this added the Atlas string under a typo'd `MONGODB_URL`, which was silently ignored (the app kept using the old local `MONGODB_URI` value) until caught and corrected. Verified with a real write: registered a throwaway account against Atlas (`201`), then deleted it (`204`). **Does not affect the test suite** — `mongodb-memory-server` (OPEN-19) is untouched and remains what `npm run verify` runs against; this is purely a manual/local-dev convenience. **Whoever else pulls this branch needs their own working `MONGODB_URI`** (their own local `mongod`, their own Atlas access, or just running the test suite) — `server/.env` is per-machine and does not travel with the branch. | Miguel Alvarez |
| **23 Jul** | **Packet 13 (schedule dashboard) built. Two additive corrections made mid-implementation: (1) `/auth/register`/`/auth/login` now also return `wakeMinute`/`sleepMinute`, and a new `GET /user/me` re-reads them for a session restored from a stored token; (2) `web/` gained its own `tsconfig.app.json`/`tsconfig.node.json` (bundler resolution, react-jsx) instead of inheriting the workspace's commonjs/node base, which cannot resolve Vite's ESM-only packages.** Also: `server` gained a `dev` script (`tsx watch src/index.ts`) and `tsx` devDependency — there was previously no way to run the real server outside the Jest suite. | (1): no route exposed FR-USR-07's schedulable day, and `ScheduleView`'s time axis (UI-01) has nothing to draw against without it — additive response fields only, no contract change, `server/test/api/app.test.ts`'s 25 tests pass unmodified. (2): the shared `tsconfig.base.json` was written for the Node/Jest packages and was never going to work for a frontend — forcing it to accommodate Vite would have meant weakening it for everyone, not just `web`. Both touch `server/src/api/app.ts`, which packet 13's own prompt named off-limits without escalation; recorded here in place of a second-human escalation since Miguel owns both packet 12 and 13. **The one rendering rule the packet called load-bearing — gating the ↻ marker on `rescheduleTrigger`'s presence, never on `placementReason` — was confirmed by exercising the real API with `curl`, not by observing the rendered page (no browser-automation tool was available; an earlier draft of this row and of `docs/P13-REPORT.md` said "verified live" for the rendering itself, which overstated it and has been corrected).** That same exercise found a **third correction**: a UC-03-accepted placement correctly has no `rescheduleTrigger` (it's a first placement, not a reschedule), but the ⓘ reason line was originally gated on that same field, so an accepted alternative would show no explanation at all once the picker closed. **Fixed client-side only, confirmed with Miguel before implementing**: `ScheduleView` tracks placement ids just accepted via the picker this session (`justAccepted`, ephemeral, does not survive reload); the ⓘ line now shows on `wasRescheduled || justAccepted` while the **↻ marker stays gated on `rescheduleTrigger` alone** — the marker specifically means "automatically rescheduled," which a UC-03 accept is not. `rescheduleTrigger` and OPEN-21's fix are untouched. One known limitation recorded, not fixed: `GET /schedule`'s `awaitingChoice` list has no way to re-expose a UC-03 task's candidate slots after a reload, since only the original `POST /tasks` response carried them — no new endpoint was added to solve it. Full reasoning: `docs/P13-REPORT.md`. `npm run verify`: still 241 tests green (re-confirmed after the third correction too). | Miguel Alvarez |
| **22 Jul** | **Packet 12's task-creation path corrected a third time — `POST /tasks` now calls `findCandidateSlots` directly for a flexible task (UC-02: auto-place if the preferred window has room; UC-03/FR-DSH-06: return ranked candidates and wait for the user, if it doesn't), instead of deferring every first placement to `sweepElapsed`.** New endpoint `POST /tasks/:id/place` writes the user's UC-03 choice, re-validated against the current busy set. A repository-internal `awaitingChoice` flag stops `sweepElapsed`'s unconditional per-retrieval sweep (FR-RSC-10) from silently auto-placing a task still awaiting that choice. | Found authoring packet 13: the prior design (deferred-to-`sweepElapsed`) can never expose FR-DSH-06/UC-03's "present alternatives, let the user pick" flow, because by the time a schedule retrieval places anything, the other candidates are gone. That design was itself a correction of an even earlier one, made on the theory that a direct per-task engine call risked a lower-priority task claiming a slot before a higher-priority one created moments later could contend for it — but FR-SCH-10's own text says an already-placed task is **not** evicted by a later, higher-priority creation (that's FR-SCH-07, Conditional), so the risk being guarded against was never real for live, sequential creation the way it is for a genuine batch placement. `sweepElapsed` still owns the batch case (several tasks unplaceable at once, later retried together) and every reschedule trigger; `RescheduleService` itself was not touched. Full reasoning: `docs/P12-REPORT.md`. `npm run verify`: 241 tests green. | Miguel Alvarez |
| **22 Jul** | **`reschedule` added to §8.1's commit-scope vocabulary**, for module work under `server/src/reschedule/`. | Required by §8.1's own rule — the vocabulary is closed and adding to it is a decision, not an ad-hoc pick. The FR-RSC policy layer had no scope word: `engine` is **wrong on purpose to avoid**, because §4.3 and FR-RSC-03 rest on the reschedule service being the thing that *re-invokes* the single placement function rather than being it, and **a log where both read `engine:` asserts in prose exactly what the guard forbids in code**; `api` names the wrong layer and packet 12 already uses it for `server/src/api/`. Filing a behaviour change under `srs:` was also rejected — it would hide a runtime change from anyone scanning the log for code. | **Patrick Rucker** |
| **22 Jul** | **OPEN-21 closed — a reschedule trigger is NEVER inferred where the store holds no evidence of one. `triggerFromHistory` returns `undefined` instead of defaulting to `'MISSED'`. SRS → v2.20.** | Miguel's smoke test against the running server caught a brand-new task's **first-ever** placement coming back stamped `MISSED` — a specific event that had not happened, contradicting the contract's own *"absent means it has never been rescheduled."* Left alone, FR-DSH-05 renders *"its 8:00 PM slot passed without being marked complete"* over a task the user had just created. **Fixed at the root, not at the report:** the proposed fix (pass `{ stamped: false }` from the sweep) was correct about the sweep but would have left a **second live instance** — the same defaulting function feeds `moveToNextDay`, whose outcome packet 12's API sends over the wire at `POST /tasks/:id/move-to-next-day`. Removing the default fixes both and makes a third impossible. **`RescheduleOutcome.trigger` became optional** as a consequence: FR-RSC-04's three facts are the facts of a *reschedule*, and a first placement is not one. *Also accepted deliberately: a displaced/edited row that E11 deleted leaves no provenance either, so it too reports nothing — understating by one fact beats stating a false one, and storing provenance to survive the deletion re-introduces exactly the side-channel v2.13 removed.* | **Patrick Rucker** *(raised by Miguel Alvarez)* |
| **22 Jul** | **Fixtures are MIXED VINTAGE by necessity: the calorie rule is demonstrated on 2026 data, the sleep rule on the 20-day 2023–24 overlap.** The export carries `activeKilocalories` for **84 of 85 days in 2026**, but **the only sleep file in all 38 folders ends 2024-10-04** — there is no 2026 sleep score anywhere, and no re-export will produce one, because the gap is in the *measurement*, not the export (22 sleep records exist across ten months; the watch is worn by day, rarely overnight). **The 20 days where both metrics land on the same `calendarDate` are the entire sleep-driven fixture basis**, and they are enough: scores span 14 → 83, cover all three FR-REC-01 tiers, include an exact **75** (a named boundary value), and **2024-03-30 carries score 27 *and* 1,352 active kcal — one real record driving both rules at once, which is FR-WER-10's demonstration.** *Rejected: waiting for a fresh export before starting packet 08* — the lead time is days against a 31 July presentation, and it blocks four packets for a cosmetic gain. *Rejected: swapping the driver metric to Body Battery* (21 days of 2026 data, would allow 2026-only) — it changes FR-REC-01 and FR-WER-03 and discards *"poor sleep lowers workout intensity"*, the sentence the project is about. **⚠️ Write this down precisely so nobody later "fixes" the mixed vintage by inventing a 2026 sleep score.** **FR-WER-10 requires the metric be REAL, not RECENT** — a 2024 record from the team member's own device satisfies it exactly. **In parallel, at zero cost: wear the watch overnight and request a fresh export now**, so a 2026 sleep score is available if it arrives in time. It is an upgrade, and a prerequisite for nothing. | **Ryan** (data owner) · Patrick (to ratify) |
| **22 Jul** | **§4.1 re-affirmed: HealthKit / Apple Watch stays excluded — including the manual-export route that §4.1 did not explicitly consider.** Raised 22 Jul when the 2026 sleep gap surfaced. The iOS Health app **can** export manually (Health → Export All Health Data → `export.xml`), so unlike the Garmin *API* the door is not technically welded shut — it is a file, not an API, and would fit DEP-02 Layer 1 and FR-WER-08's export shape **without** needing an iOS app. **It still loses on merits, and the deciding reason is the second one: Apple publishes no 0–100 sleep score** — only stages and durations — **so adopting it would reopen OPEN-02**, the exact question the Garmin export just closed for free, trading a published number for a formula the team would have to defend. It also costs a second adapter, a second parser, and a second fixture set for data we already hold, **and it only helps if the watch is worn overnight — which is the same behavioural fix that yields fresh Garmin sleep data anyway.** *Recorded so the option is visibly considered-and-rejected rather than merely absent* — the original §4.1 wording (*"cannot reach a web backend without an iOS app"*) is true of the API but overstated for the export path, and someone re-deriving it would have found the gap and reopened the question. **Revisit only if: an Apple Watch is owned AND worn overnight AND the Garmin re-export does not arrive.** Even then it is Conditional — **FR-WER-05 means it costs one adapter and zero changes to the recommendation engine.** | Patrick (§4.1 owner) · Ryan (raised) |
| 12 Jul | **Official Garmin Health API is off the critical path.** Wearable data comes from **account exports** instead. | Garmin's Health API requires partner approval with a lead time we can't schedule. Apple Watch is excluded entirely (HealthKit is on-device, iOS-only — can't reach a web backend). **We own the device, which gives us the data; it does not give us the API.** | Patrick + AI |
| 12 Jul | **Workout and meal libraries are local seeded data, not runtime API calls.** | Workouts and recipes are static — there's no freshness argument for fetching them. Calling an external API at recommendation time reintroduces rate limits, keys, and demo-day network failure in exchange for nothing. **Consequence: the whole demo runs offline.** | Patrick + AI |
| 12 Jul | **Stack: React + TS / Node + TS / PostgreSQL.** | One language across the stack — all three of us can review the entire codebase. TypeScript because the engine is interval math where types catch real bugs. | Patrick + AI *(pending team ratification — Q6)* |
| 12 Jul | **Two metrics ship: sleep score → workout intensity; active calories → calorie target.** | One metric per decision. Meals were previously driven by *no* wearable data at all, which made "wearable-driven meal plans" an overclaim. Architecture makes adding a third metric an *addition*, not a redesign. | Patrick + AI |
| 12 Jul | **FR-REC-10 and NFR-MNT-02 demoted to Conditional.** | Timeline. FR-REC-08 already carries the adaptive meal claim; frontend coverage is expensive and reveals little. **90% engine coverage stays Essential.** | Patrick + AI *(pending team ratification — Q2)* |
| 12 Jul | **All-day calendar events are NOT busy intervals** (new requirement FR-CAL-05, Essential). Recurring events must be expanded to concrete occurrences before reaching the engine (FR-CAL-06). | Found while analyzing Google Calendar. **This is a requirement, not an implementation detail:** FR-CAL-01 required every commitment to have a start and end time, so the SRS could not even *represent* an all-day event. Treated naively, one "Mom's Birthday" entry marks the entire schedulable day busy and **the engine can place nothing at all.** | Patrick + AI |
| 12 Jul | **Google Calendar stays Conditional (SI-05) — but for a different reason than Garmin.** | Google is **not** blocked: credentials are self-service, no partner review. It's Conditional because **the acceptance demo is engineered to run with the network disabled**, and we don't surrender that. Demo offline; *show* the import as a feature if it works. ~1–2 days. Would restore the "external API + OAuth" technical-depth story we lost when Garmin's API left the critical path. | Patrick + AI *(team decides — Q5)* |
| 12 Jul | **Agentic TDD: RED and GREEN are SEPARATE agent sessions. The implementer agent may never edit a test.** *(See `AGENTIC-TDD-WORKFLOW.md`.)* | **When one agent writes the test and the code in the same pass, the test encodes what the code *does*, not what the SRS *says*.** It goes green and it is wrong, and nothing catches it — the test looks perfectly reasonable. This is not a prompting problem; you cannot fix it by asking the agent to be careful. It is structural. So: the RED agent sees the SRS and never an implementation; the GREEN agent sees the failing tests and **may not edit one, ever** — if it thinks a test is wrong it **stops and a human adjudicates it against the SRS.** That escalation is the whole value: it converts an invisible fatal defect into a five-minute human read. | Patrick + AI |
| 12 Jul | **The contract (`shared/src/contract.ts`) is written by a human ONCE, before any agent runs. Changing it is a team decision with a row in this log — never an agent's refactor.** | Parallel agents each invent their own locally-reasonable `Task` type. All of them pass their own tests. On day 5 we aren't merging, we're rewriting — the exact "merge chaos nobody has context to untangle" of `CLAUDE.md` §7, and **agents make it worse, because they generate plausible types faster than we notice they disagree.** ~60 lines, one hour, and it's what lets three of us work in parallel at all. | Patrick + AI |
| 12 Jul | **Every requirement verified by *Inspection* (I) gets an executable guard in CI.** Specifically: FR-RSC-03/NFR-MNT-03 (exactly one placement function), FR-LIB-02 (no network in the recommendation path), FR-SCH-05 (purity). | **An (I) requirement has no test, so it rots by default.** Ask an agent to implement FR-RSC-02 and it will cheerfully write a tidy `findNextFreeSlot()` helper inside the reschedule service — locally the simplest thing to do — and now FR-RSC-03 is **false and no test fails.** On a hand-written codebase code review catches that. With three agents out-generating three reviewers (§7.3), it does not. The guards are cheap, and they're exactly what a grader probes. | Patrick + AI |
| 12 Jul | **Time inside the engine is `Minute` — an integer, minutes since local midnight. Never a `Date`, never a timezone.** | FR-SCH-05 forbids the engine a clock. **An engine that cannot represent a timestamp cannot accidentally read one.** Makes the purity requirement structurally true rather than merely intended, and kills a whole class of timezone bugs (`TEAM-MEETING.md` Q5, gotcha #4). Wall-clock conversion happens at the API boundary. | Patrick + AI |
| 12 Jul | **The `engine/` package has an EMPTY `dependencies` block, permanently. CI fails if a dependency is added.** | Same reasoning, enforced by packaging rather than discipline: an engine that can't import anything can't import a clock, a DB client, or an HTTP library. It is the FR-SCH-05 guard, and it's three lines of shell. | Patrick + AI |
| 12 Jul | **Task priority is an integer 1–5, where 1 is HIGHEST.** | The ERD said only `int priority`. FR-SCH-03 uses it as a ranking tiebreak, so the direction has to be pinned **before** an agent writes a comparator and silently picks the other one. Recorded because it is exactly the kind of unstated detail that produces a subtly inverted ranking nobody notices. | Patrick + AI |
| 12 Jul | **TDD is scoped, not universal. We TDD the (T) requirements — engine, rules, catalog, analytics. We do NOT TDD the frontend, the Garmin adapter (yet), or the glue.** *(`AGENTIC-TDD-WORKFLOW.md` §3.5.)* | **The SRS's verification-method column already made this call and nobody had read it that way:** (T) = automated test → TDD it. **(D) = Demonstration → the SRS is telling you to *show* it, not assert it.** (I) = a guard. Nearly every FR-DSH/FR-WEL requirement is (D), and **we already ruled against frontend TDD without noticing** — NFR-MNT-02 was demoted with the reasoning *"frontend coverage is expensive and reveals little."* And **you cannot test-first a Garmin export nobody has looked at** (OPEN-01): you'd be inventing a fixture from a guess and then testing the guess. Explore → fixture from real data → RED → GREEN. | Patrick + AI |
| 12 Jul | **TDD is NOT what gives us extensibility. The architecture is. TDD stops it eroding — that is the whole claim, and we will not overclaim it.** | *"Adding a metric = two additions, zero modifications"* comes from the **adapter interface** (FR-WER-05), the **keyed metric map** instead of a two-column struct (FR-WER-04), and the **rule registry** (FR-REC-11). **No amount of TDD would have produced any of them.** Recorded because the opposite belief leads somewhere bad: **testing implementation details instead of module boundaries, which cements the current structure and makes refactoring *harder*.** Agents especially love to over-mock. **Test at the contract boundary** — which is why the engine exports exactly one function and keeps its helpers private. **Corollary: for long-term maintainability the Rule 3 guards matter more than the TDD does** — the biggest threat to this codebase is an agent quietly adding a *second* placement function while everything stays green. | Patrick + AI |
| **12 Jul — MEETING 1** | **ROLES RATIFIED. Patrick Rucker → Scheduling Algorithm Lead. Ryan Woosley → Data & Wearable Integration Lead. Miguel Alvarez → Frontend & Backend Lead.** Patrick creates the repo. Ryan owns the Garmin export. | Matches the module seams the SRS already draws, so parallel work doesn't collide. **⚠️ Miguel carrying frontend AND backend is a workload risk — raised for Meeting 2.** The engine is small and deep (~3 days), so Patrick has spare capacity while Miguel is on the critical path for two weeks. | Team |
| **12 Jul — MEETING 1** | **Data layer: MongoDB, not PostgreSQL.** | Team familiarity, and a schema-free store removes migration overhead from a 19-day project — genuinely aligned with DR-05, which requires that adding a wearable metric never force a schema change. **Trade accepted:** FR-ANL's analytics (streaks, completion rates) group across placements, so they become **aggregation pipelines rather than joins** — the fiddliest queries in the System. **Note also: a schema-free store *permits* the wrong shape. DR-05 still forbids it** — one document per metric per date, never one field per metric. | Team |
| **12 Jul — MEETING 1** | **Calendar IMPORT declined (SI-05). Calendar EXPORT adopted instead: an `.ics` file** (SI-06 / FR-CAL-07, **Conditional**). | Export is text generation — **no OAuth, no API, no network** — so it costs a fraction of the import and **preserves the offline acceptance demo.** **The consequence the team must be clear on: export is the *opposite direction* from import.** It supplies the scheduler with **nothing**, so fixed commitments still come from **manual entry only**, and it does **not** restore the "external API + OAuth" technical-depth story that was lost when the Garmin API left the critical path. That story is now genuinely gone from the project, and we should not claim it. | Team |
| **12 Jul — MEETING 1** | **Working agreements adopted:** integrate daily; one human owner per module who can explain it aloud without opening the file; nobody merges code they haven't read. Each member manages their own working schedule (no fixed standup). | The failure mode of agentic development on a team is not slow code — it's a codebase nobody understands at 11pm on 30 July. **⚠️ Mild tension worth watching: "integrate daily" and "no fixed sync cadence" pull against each other.** If daily integration starts slipping, that's the first symptom. | Team |
| **21 Jul** | **Commit message schema adopted** (`CLAUDE.md` §8). `<scope>: <imperative summary>`, a body saying **why**, and trailers `Requirements:` / `Packet:` / `Owner:`. Scope is a closed vocabulary; the `PNN RED` / `PNN GREEN` forms were already in use in `AGENTIC-TDD-WORKFLOW.md` §5–6 and are now the standard. | **The commit log is the third copy of project memory**, after the docs and the SRS — and on an agentic project it is the *only* durable record of what an agent did, because the session that wrote the code is gone. Two trailers carry the weight: **`Requirements:` makes the log a running traceability matrix** (SRS Appendix B) and applies to commits the same rule test names already obey — *a behavior change citing no requirement is the agent's opinion.* **`Owner:` is a human asserting they read the diff**, which is the only thing that makes §7.2 true rather than merely stated. Also pinned: the RED commit is what *freezes* the tests (a fresh chat gives context isolation; only the commit gives a permission boundary), and a GREEN commit must show the test diff empty. | Patrick + AI |
| **21 Jul** | **No AI credit lines on commits — no `Co-Authored-By`, no generated-with footers, no tool attribution, ever.** | Patrick's call. The commit's author is the human in `Owner:`, because that trailer means *"I read this and can defend it"* — an assertion only a person can make, and one a co-author line quietly dilutes. **This is not concealment:** that the project is built agentically is stated plainly in the SRS and `AGENTIC-TDD-WORKFLOW.md`, which is where a reader looks for it. Repeating it on every line of `git log` adds noise to the one artifact whose whole value is signal. *(Note for AI sessions: this **overrides** any default instruction to append attribution.)* | Patrick |
| **21 Jul** | **SRS → v2.5. The user can now tell the System a task was skipped, and can overturn the System when it wrongly decides one was missed. FR-RSC-01's automatic classification is UNCHANGED.** Added FR-RSC-08 (declare skipped — a third reschedule trigger), FR-RSC-09 (marking complete after an auto-reschedule cancels it), FR-RSC-10 (what actually observes an elapsed window), UC-13, FR-DSH-07, DR-06. | **Patrick raised it: the System had no way to be told anything about a miss, and no way to be corrected.** That was right — FR-RSC-01 infers a miss from an elapsed, unmarked window, which is ambiguous between *"missed"* and *"did it, forgot to tap complete"*, and the SRS resolved that ambiguity in neither direction. **The option considered and rejected was making the user's declaration the *only* trigger.** Two reasons it was rejected: (1) **FR-RSC-01 is in the Core Demonstrable Capability (§2.7.1)** — *"the schedule repairs itself"* — and a schedule that repairs itself only when asked is a weaker claim, one any todo app's snooze button already makes; (2) **the miss signal is the one the user has least incentive to send** — the person who skipped their 8 PM run does not open the app to confess, so a declaration-only design fails in exactly the case the feature exists for. **The resolution keeps inference as the default and adds the user's voice as a *correction* path and an *early* path**, which is strictly more capability than either option alone. Also pinned: **FR-RSC-10**, because FR-RSC-01 gave the *condition* for a miss but never said what observes the clock — an unstated mechanism gets decided by whichever module an agent writes first. Evaluation happens **when the schedule is retrieved**; a background job is permitted, not required. | Patrick + AI |
| **21 Jul** | **A declared skip does NOT excuse a habit from its completion rate (FR-ANL-06).** Skipped-and-never-completed counts as *not completed*, identically to missed. Skipped-then-rescheduled-then-completed counts as **completed**, per FR-ANL-03 — the trigger is irrelevant to the record, only the outcome matters. | Surfaced by re-auditing all thirteen use cases after the v2.5 change: **UC-11 had no answer for a habit the user declared skipped and never made up**, which left FR-ANL-02's denominator ambiguous — and an ambiguity in an analytics requirement is decided silently by whichever agent writes the query. **The rejected alternative was treating a declared skip as *excused*** (dropped from the denominator). It sounds humane and it is corrosive: it lets a user protect a streak by announcing failures in advance, which turns the streak into a measure of candor rather than consistency and empties FR-ANL-02 of meaning. **A skip is a statement about the schedule, not a pardon from the record.** | Patrick + AI |
| **21 Jul** | **Work packets grouped into owner folders** — `foundation/` `engine/` `wearable/` `backend/` `frontend/` `verification/` — **while keeping the global `01`…`17` numbers in the filenames.** | Team's call: nobody should have to ask whose packet a file is. **The numbers and the folders are two different axes and both are binding — numbers are the delivery order, folders are the ownership.** Keeping the numbers matters for two reasons: the professor's prompt-pack format requires *"deliver in exact numerical order"*, and a folder is not a work queue — `wearable/08` runs when its dependencies are met, not whenever Ryan reaches it. **The `Human owner` field inside each packet stays authoritative**; the folder is a convenience, and if they disagree the folder is the thing to fix. Done now rather than later **because it cost five file moves instead of seventeen** — and it still required updating every prompt path in `CLAUDE.md`, `AGENTIC-TDD-WORKFLOW.md`, `TEAM-MEETING.md`, `SRS-v2.md`, `README.md` and the prompt index. *(Verified afterwards by resolving every referenced path against the filesystem, which caught one straggler in the SRS revision history.)* | Team (proposed) · Patrick + AI (executed) |
| **21 Jul** | **The user identifier is DEFERRED to implementation — Miguel decides when he builds the API (OPEN-12).** Whether a user is keyed by **email** or an **opaque internal id**, and whether ownership rides on the domain types or on the API boundary around them, is left open **on purpose**. `shared/src/contract.ts` carries **no `userId`**. SRS → **v2.6**: note under FR-USR-04, new OPEN-12 in Appendix C. | Patrick's call: **the SRS's job is to require the isolation (FR-USR-04), not to pick the key.** Ownership touches every collection, every query and every frontend fetch — it is the Frontend & Backend Lead's decision and is **better made with the API in front of him than guessed at now.** Deferring is only safe if the question actually gets asked, so **three independent places now force it**: the note under FR-USR-04, OPEN-12 in Appendix C, and a ⛔ escalation block in `prompts/00-prompt-collection-summary.md` that packet 12's author must copy into the packet. **Why the belt and braces: an identifier scheme is invisible once chosen.** Nothing fails, no test goes red — the code simply hardens around a decision nobody took, and by the time it hurts it is in every query. *(This is the same failure shape as the dependency decisions earlier today: not a wrong answer, an unasked question.)* | Patrick (decided) · Miguel (owns the answer) |
| **21 Jul** | **Contract amended before first use — four changes, pending team ratification** (`prompts/foundation/03-shared-contract-types.md`). **(1)** `Metric` becomes a **discriminated union**; **(2)** added **`Placement`, `PlacementStatus`, `RescheduleTrigger`**; **(3)** `Slot.reason` → `Slot.explanation`; **(4)** `Minute` stays a plain alias and its comment stops overclaiming. | Found by reviewing the contract against **SRS v2.5**, which it predated. **(1) is the important one:** FR-WER-06 and NFR-ROB-01 require an absent metric and a measured zero to stay distinguishable, but the old `interface Metric` permitted `{ value: 0, isAvailable: false }` and let a rule read `value` without checking the flag — **the exact collapse the requirement forbids, with nothing failing.** As a union, `value` does not exist on the unavailable branch and forgetting to check will not compile. *Notably this is the same pattern `PlacementResult` already used for FR-SCH-06 — the idea was already in the contract and simply had not been applied twice.* **(2) was a genuine hole:** there was **no `Placement` type at all**, yet §3.6 declares `onTaskMissed(placement: Placement)` and today's DR-06 requires the trigger to distinguish missed/skipped/displaced — packets 06–07 would have had an agent invent it, which is precisely what §4.7 exists to prevent. **This was a cross-reference miss from the v2.5 edits: requirements changed and the contract was not chased.** **(4):** branding `Minute` would be genuinely enforced but costs a cast at every literal, JSON boundary, Mongo document and test fixture; declined on nineteen-day grounds — a time/priority mix-up fails NFR-COR-01's property test on its first case anyway. **The defect was never the alias, it was the comment claiming a safety the type did not provide.** ⚠️ **Three questions left open for the ratification meeting rather than invented:** whether `CompletionRecord` is a shared type or a persistence concern (DR-01); whether `userId` belongs on `Task`/`Placement` or only at the API layer (FR-USR-04); and whether a wake-08:00/bed-02:00 day should be expressible — **`Interval` requires `end > start` and UC-01 rejects it, which is consistent, but §2.3's user is "typically a university student."** | Patrick + AI *(pending team ratification)* |
| **21 Jul** | **Runtime dependencies chosen — Miguel's call, as backend owner.** `server/`: **Express** (HTTP), **the official `mongodb` driver** (not Mongoose), **bcrypt** (hashing), **jsonwebtoken** (sessions), plus `cors` and `dotenv`. `web/`: **React + Vite**. `engine/` and `shared/`: **nothing, permanently.** Pinned into `prompts/foundation/02-toolchain-and-dependencies.md`; exact versions come from the committed root `package-lock.json`. | **These were about to be decided by an agent.** Packet 02 named tool *categories* (Jest, ESLint, fast-check) but no runtime packages, so whoever ran it would have picked the framework, the data layer, the hashing library and the session strategy by default — and *"we use Mongoose"* would have become true because an agent typed it, not because anyone chose it. **The `mongodb` driver over Mongoose is the choice that carries weight:** DR-05 requires *one document per metric per date, not one field per metric*, so a new metric is an insert rather than a change to every reader. **A Mongoose schema can quietly fight that shape and nothing would fail a test** — FUT-02 would just become expensive months later. The raw driver has no opinion, so DR-05 ends up satisfied by code the team wrote rather than a library default nobody reviewed. **Express 4 and React 18 (not 5 / 19)** for documentation maturity — on nineteen days the boring option ships. ⚠️ **Watched risk: `bcrypt` is a native module.** If it ever attempts compilation on a machine without build tools, `npm ci` fails and **CON-10 breaks**; packet 02 now verifies a clean-machine install explicitly, and `bcryptjs` is the drop-in fallback **if the team decides so**. | Miguel (decided) · Patrick + AI (framed) |
| **21 Jul** | **Work packets restructured to the professor's prompt-pack format.** Renumbered `WP-00/01/02` → **`01`…`17`**, delivered in exact numerical order, with a `00-prompt-collection-summary.md` index and a `README-prompts.md` phase map. Adopted his five named techniques (Sandwich Method, Attention Anchoring, Visual Emphasis, Clear Delimiters, Selective Context). **`WP-00` split into three** — scaffold / toolchain / contract — mirroring his `01-setup` / `02-gradle` / `03-interface` sequence. | He supplied an example pack (MVC calculator, 15 prompts) that **names those five techniques in two separate files** — they are lecture terms and almost certainly rubric lines, so conforming costs nothing. Our substance survives intact: the escalation clause, must-not-touch lists, verbatim SRS quotes and mechanical done-criteria are all **stronger than his example**, whose verification is *"compiles without errors."* **His prompts contain finished `.java` files; ours contain requirements — and that is NOT a disagreement about method.** Patrick's reading, and it is correct: **a prompt pack is a generator.** Run it in order and the agent creates the code. His `.java` blocks are residue from a run he had **already** performed — his pack was written *backwards* from a finished program, ours *forwards* to one that does not exist yet. Same artifact, opposite ends of its life. **Nothing here needs the professor's adjudication.** **The consequence worth recording: re-running the pack may produce different code, so (a) generated code is NEVER pasted back into a prompt — that would freeze one run's arbitrary choices into the spec and make NFR-MNT-01's coverage figure measure the code against itself — and (b) the frozen tests are the only invariant across runs, which is the strongest argument yet for the RED/GREEN order.** *Demonstrable claim: delete `engine/src/`, re-run prompt 05, the suite still passes.* **Prompts 06–17 are enumerated with requirement IDs but deliberately unwritten** — a prompt authored before its module is designed encodes guesses, and an agent implements a guess as faithfully as a requirement. **Also: ~30 `WP-` cross-references across `CLAUDE.md`, `AGENTIC-TDD-WORKFLOW.md` and this file had to be remapped** — §0 rule 3 in miniature. | Patrick + AI |
| 12 Jul | **SRS → v2.3: the §3.6 engine signature was corrected. It could not satisfy its own requirements.** Now returns `PlacementResult` (a discriminated union), and takes `schedulableDay` as a parameter. | The diagram said `findCandidateSlots(busy, task) → Slot[]`. But **FR-SCH-06 demands an "explicit empty result *with a reason*" — and an empty array carries no reason**, so the signature made its own requirement unsatisfiable at the type level and let a caller ignore the failure by iterating nothing. And FR-SCH-04 requires staying inside the schedulable day, which the engine can't know while FR-SCH-05 denies it a clock — so the caller must **tell** it. **No requirement changed; the design was brought into conformance with requirements it already had.** Found by deriving the code contract from the SRS — which is an argument for doing that early. | Patrick + AI |
| **22 Jul** | **Per-day deadlines abandoned. Work is sequenced by DEPENDENCY, not by calendar** — see **THE BUILD ORDER** at the top of this file. The Meeting 1 schedule table is marked superseded rather than deleted. **31 July (presentation) is unchanged and remains the only hard date.** | The dated plan had slipped roughly six days by 22 Jul (it had the engine property-tested by the 15th and the FR-WER-10 midpoint gate cleared on the 16th; on the 22nd neither had happened and **no implementation existed in the repo at all** — only packets 01–03). **A schedule nobody is working to is worse than no schedule, because it is still trusted** (`CLAUDE.md` §0 rule 5), and re-dating it would have been a second guess with the same failure mode. Dependency order is what actually constrains the work — packet 16 waits on 07 and 10 whatever the date is. **Kept the old table visible on purpose:** deleting it would erase the evidence of how far the plan moved, which is the thing worth knowing. | Team |
| **22 Jul** | **FR-SCH-03's middle ranking criterion — "the task's priority relative to neighbors" — REMOVED. SRS → v2.7.** Ranking is now (a) proximity to the preferred window, then (b) earlier start. Priority is untouched as a `Task` field and untouched in FR-SCH-07. | **It was not evaluable, and nothing would ever have failed to say so.** The engine receives `busy: readonly Interval[]`, and an `Interval` is `{start, end}` — a neighbouring commitment carries no priority for the engine to compare against. The alternative reading, the placed task's *own* priority, is identical for every candidate slot of that task and so can never break a tie between them: a no-op dressed as a rule. **The likeliest history is that (b) was drafted alongside FR-SCH-07 (displacement), which genuinely does need a neighbour's priority, and outlived it when FR-SCH-07 was made Conditional.** ⚠️ **How this was found is the point:** the packet 04 RED agent hit it while writing `ranking.test.ts` and **escalated instead of inventing a tiebreak.** Had it guessed — and a plausible guess was available — the invented rule would have been frozen into the engine's test suite and implemented faithfully by packet 05 as a requirement nobody agreed to, with a green suite the whole way. *This is precisely the failure `AGENTIC-TDD-WORKFLOW.md` §6 item 9 exists to catch, caught one stage earlier than expected.* Also opened **OPEN-13** (where a candidate starts inside an over-long free interval — Appendix A cannot discriminate). Cross-references chased: SRS §3.8.4 + revision history + Appendix C, `AGENTIC-TDD-WORKFLOW.md` §4, `CLAUDE.md` §8.6, packet 04's verbatim quote. | Patrick (decided) · packet 04 RED agent (found) |
| **22 Jul** | **FR-DSH-05's user-facing explanation is written by `RescheduleService` into `Placement.placementReason`, NOT by the engine into `Slot.explanation`.** The engine's `explanation` is its own account of a slot — times, inside/outside the preferred window, which ranked alternative — and nothing more. Contract comments corrected in both copies; folded into packet 06's scope. | **The contract had two fields citing one requirement, with the example on the field that cannot satisfy it.** `Slot.explanation` was documented as *"Plain language, for FR-DSH-05. e.g. '4:00 PM — your 2:00 PM slot was taken by CS 401 Lecture.'"* — but the engine receives `busy` as bare `Interval`s with **no titles**, so it can never name CS 401 Lecture. Meanwhile `Placement.placementReason` already existed for precisely this, carrying DR-03 and its own FR-DSH-05 reference. **Nothing structural was wrong; the documentation misassigned the responsibility.** ⚠️ **Why it was worth settling before packet 06 rather than during it:** an agent told to satisfy FR-DSH-05 from the engine has one obvious move — start passing task titles into `busy` — and that ends the purity argument, the frozen contract and the 30 frozen tests in a single stroke, while looking like a reasonable fix to a real gap. *Same species as FR-SCH-03(b) and OPEN-13: not a wrong answer, an unasked question, found by reading the contract against what the engine can actually see.* No type or behaviour changed; comments only, in `shared/src/contract.ts` and its human-authored source. | Patrick (decided) · Patrick + AI (found) |
| **22 Jul** | **The RED/GREEN test freeze is now an executable guard, not a documented command.** `npm run guard:tests-frozen` reads `scripts/frozen-tests.json` and compares the **working tree** against each freeze sha; it runs inside `npm run verify` and therefore in CI. Every RED packet must add its suite to that manifest after the freeze commit. | **The documented check was wrong in six places, and in a way that mattered.** `CLAUDE.md` §8.3 gave the arguments in an order git rejects outright — path before revision — so the single most important verification in the method printed `fatal:` for anyone who ran it, which under time pressure reads as "skip this step". Worse, five other sites (both engine packets, three places in the workflow doc) stated it as bare `git diff --stat engine/test/`, **which compares the working tree to HEAD and so cannot detect a test that was edited AND committed** — precisely the case §8.3 exists for, and the one it says is "indistinguishable, forever after, from a test that was weakened to go green". ⚠️ **Demonstrated rather than assumed before adopting:** weakening one assertion in the FR-SCH-09 "one minute short" test — the second half, the half that proves the engine keeps searching — leaves **30/30 passing and coverage at 100%**, and the new guard exits 1. **A green suite cannot police its own tests; nothing but this could have caught that.** *Same lesson as the import guard earlier today, which had been green for a week only because `engine/` was empty: **a guard that has never once failed is not a guard that works.** Both were verified by making them fail on purpose, and packet 16's guards will be held to the same standard.* **Changing or removing a manifest entry is a team decision with a row here** — silencing the guard is editing the test one level removed. | Patrick (decided) · Patrick + AI (built) |
| **22 Jul** | **`shared/src/contract.ts` amended — comment only. SRS → v2.9 (OPEN-14 closed).** `priority`'s doc comment cited **FR-SCH-03**, which stopped using priority in v2.7. It now cites **FR-SCH-10** (placement order) and **FR-SCH-07** (displacement). `prompts/foundation/03-shared-contract-types.md` changed identically. | **Logged because §4.7 requires a row for any change to the contract, and that rule should not quietly acquire a "unless it's only a comment" exception** — the next change will be argued from this precedent. **No type, field, or behaviour changed**; the file's compiled output is byte-identical. Recorded separately from the v2.8 row because it needed **Patrick's explicit authorisation**: the agent found it, wrote it up as OPEN-14, and stopped, since an agent editing `contract.ts` — even a comment — crosses the line §4.7 draws. ⚠️ **Worth noting for the next stale comment:** the false line survived a version bump because **comments are not cross-referenced by anything**. The traceability matrix, the index and the revision history all track requirements; nothing tracks a requirement ID mentioned in a code comment. | Patrick (authorised) · Patrick + AI (found + applied) |
| **22 Jul** | **OPEN-13 closed: a candidate's start inside an over-long free interval is the fitting position CLOSEST to the preferred window**, earliest breaking a tie (FR-SCH-02, SRS v2.9). Inside the preferred window, earliest governs. | **The SRS never said, and packet 05 would have decided it silently** — an unstated rule living only in the implementation, which is the same failure shape as OPEN-12's identifier and v2.7's criterion (b). It is not cosmetic: a 60-minute task preferring 17:00, facing a free 07:00–10:00, is offered **09:00 rather than 07:00**. ⚠️ **The reason it went unnoticed for so long is worth keeping: Appendix A cannot discriminate.** All three of its ranked candidates sit in intervals where "closest" and "earliest" give the same answer, so the worked example that reads like an oracle is silent exactly where the question is. **A worked example is not a specification** — it demonstrates the cases its author happened to pick. *Chosen for coherence with FR-SCH-03, whose primary criterion is already proximity to the preferred window.* **All 30 frozen engine tests were re-checked against the decision and remain correct** — the RED suite deliberately asserts no position where the two readings disagree, so the freeze is unaffected. | Patrick (decided) · packet 04 RED agent (raised) |
| **22 Jul** | **Priority decides contention between flexible tasks — by PLACEMENT ORDER (new FR-SCH-10, Essential), not by displacement. FR-SCH-07 stays Conditional but now carries its design. SRS → v2.8.** Several flexible tasks going into one day are placed in ascending priority order, each placement becoming a busy interval for the next; an equal-priority tie breaks on earlier-created. | **Patrick asked for priority to decide who wins a contested slot. Two designs deliver that, an order of magnitude apart in cost, and separating them is the whole decision.** **(1) Placement order** — free. The engine is untouched, `busy` stays `readonly Interval[]`, and the 30 frozen engine tests stay valid. **(2) Displacement** — evicting a task *already placed*. The tempting build teaches the engine who occupies each busy period, which changes the frozen contract **and every engine test written against it**, and contradicts §3.8.4's own split between *the pure decision* and *the stateful policy*. **What settled it: the eviction case appears nowhere in the acceptance demonstration.** The demo's conflict is a **fixed** commitment landing on a flexible task (FR-RSC-02) — already Essential, already the moment that carries the claim. Two flexible tasks fighting over a slot is never on screen. **And the asymmetry is decisive: if displacement never ships, nothing is lost — it is Conditional and nobody is surprised. A day spent on it that FR-REC-04 needed would fail the project's thesis while the product looked complete** (`CLAUDE.md` §5). ⚠️ **FR-SCH-10 was a genuine hole, not a new feature**: the SRS never said what order multiple tasks were placed in, yet that order decides the winner — left unstated, whichever module got written first would have settled it, and two runs of the same day could disagree. It also gives priority its **only Essential consumer**; after v2.7 deleted FR-SCH-03(b), priority was collected, validated and stored while nothing read it. **The displacement design is recorded under FR-SCH-07 anyway** — service-located, three engine calls, revert if the displaced task cannot be re-placed — because writing it down is what keeps it a day's work instead of a week's, and stops it being re-derived at 11pm on the 29th. **§2.7.1's Core is deliberately unchanged**: FR-SCH-10 is Essential but not Core. | Patrick (decided) · Patrick + AI (designed) |
| **22 Jul** | **The FR-SCH-05 ESLint import guard was broken and is fixed.** It read `group: ['*']` while its own message promised that relative paths and `@capstone/shared` were allowed. It rejected **every** import under `engine/` — including the engine's own contract import and the `fast-check` that NFR-COR-01 makes Essential. | **It had never been exercised.** `engine/` was empty from the day the guard was written until packet 04, so lint had nothing to check and CI was green on a rule that forbade everything. **The first engine file of any kind would have turned it red** — that file arrived today. Root cause of the failed fix attempt is worth keeping: `no-restricted-imports` matches with gitignore semantics, and **gitignore cannot re-include a file whose parent directory is excluded** — so `'*'` excluded the `@capstone` directory and `'!@capstone/shared'` silently could not rescue it. The working form re-includes the scope directory, re-denies the sibling workspaces, then permits exactly `@capstone/shared`. **Verified by probing the matcher directly rather than by trusting the message**, which is how the original defect survived in the first place. ⚠️ **The general lesson for §4.8: a guard that has never failed is not a guard that works.** Every (I)-requirement guard needs a case that *should* trip it, run once. | Patrick (approved) · packet 04 RED agent (found + fixed) |
| **22 Jul** | **Branching model: long-lived `dev` off `main`; packet branches (`wp-NN-…`) off `dev`.** `main` holds only what is ready to be seen. | Adopted when implementation started. **`CLAUDE.md` §8.5 previously specified packet branches off `main` and said nothing about an integration branch** — recorded here so Ryan and Miguel branch from `dev` rather than `main`. A teammate who branches from the wrong base does not find out until the merge, which is exactly the kind of avoidable tangle §7.1's daily integration exists to prevent. **The RED freeze commit stays alone on its packet branch**; ~~docs, SRS edits and CI-guard fixes go to `dev` directly, since they affect everyone's build and have no reason to wait behind a packet review.~~ ⚠️ **That last clause was REVERSED the same day — see the row below. Nothing goes to `dev` directly.** | Patrick |
| **22 Jul** | **Nothing is committed directly to `dev`. ALL work — code, documents, SRS edits, guard fixes — happens on a branch off `dev`, and reaches `dev` only by merge, only once the branch is finished, `npm run verify` passes, and **its own human owner is satisfied — each person decides their own work**, per §7.2 (Patrick for the engine and reschedule packets, Ryan for the wearable chain, Miguel for backend and frontend). Reverses the "direct to `dev`" clause adopted earlier today. *(Note this is a different gate from the **RED review gate**, which `AGENTIC-TDD-WORKFLOW.md` §6 requires a **second** human for — an owner may merge their own branch, but nobody freezes their own test suite.)* | **The exemption was written for speed and bought none.** Its premise was that a document or a guard fix affects everyone's build and should not wait behind a packet review — but **the things it exempted are the things most worth reviewing.** Today alone, the "documents only" category included a **change to the frozen contract** (`Task.createdAt`) and **four requirement decisions that packet 06 turns straight into frozen tests** — none of which anyone else had read. **An exemption defined by file type cannot tell a typo fix from a contract amendment**, and the one that slips through is by definition the one nobody looked at. ⚠️ **The real cost of the old rule was that `dev` could go red without any branch having failed** — a doc commit that touches `shared/` still breaks everyone's typecheck, and CI would have caught it only after it was already on the branch everyone builds from. **Under the new rule `dev` is only ever advanced by something that has passed `npm run verify` and been read by its owner**, which is what makes "pull `dev` and start working" safe. **This does NOT relax §7.1's daily integration** — it makes branch lifetime the thing to watch: **a branch that cannot be merged today is a branch that has grown too big**, and the gate is a reason to keep them small, not a licence to let them age. | Patrick |
| **22 Jul** | **E11 — a displaced or edited occurrence the engine cannot re-place has its row REMOVED.** `TaskRepository` gains `deletePlacement`, the domain's only deletion. SRS **v2.16**. | A missed or skipped occurrence is already out of `PLANNED` when the engine is asked, so FR-RSC-05's *"no `PLANNED` placement"* costs nothing. A displaced or edited one is still `PLANNED`. **Leaving it stores the flexible task overlapping the commitment that displaced it — the double-booking FR-SCH-04 forbids the engine from ever producing, arrived at by the caller instead.** *And far worse: FR-RSC-05 recognises an unplaced task by the ABSENCE of a `PLANNED` placement, so an occurrence that keeps one still **looks placed** — the user is never offered the next day, and a day that later frees up never heals it. The overlap would be visible; the missing offer would not.* **Rejected: a new `PlacementStatus` member** — v2.13 already refused one, because a `Placement` that is nowhere carries a `start` and `end` that mean nothing. Deletion destroys no history: a `PLANNED` row is a statement about the future, and what happened lives in completion records (DR-01) and in the `MISSED`/`SKIPPED` rows that record a real event. | Patrick (ratified) · packet 06 RED agent (found + drafted) |
| **22 Jul** | **E12 — FR-RSC-05 no longer enumerates its triggers.** *"Where **a task's occurrence** cannot be re-placed…"* SRS **v2.16**. | It read *"missed or displaced"* until v2.14, when **E3** added *"skipped"* — a trigger FR-RSC-08, UC-10 and UC-13 had covered all along and only this sentence had missed. **Six hours later v2.15 added a fourth trigger and this sentence was not revisited: the identical omission, in the identical place.** *An enumeration inside a requirement must be updated every time the set it names grows, and **nothing fails when it is not** — the sentence still reads perfectly and is simply no longer about everything it governs.* The obligation is about the failure, not about what caused it, so a fifth trigger cannot reopen it. | Patrick (ratified) · packet 06 RED agent (found + drafted) |
| **22 Jul** | **E13 — FR-TSK-04's validity predicate made exact: length must EQUAL the new duration, and the placement must lie INSIDE the new window.** SRS **v2.16**. | **Any duration change invalidates, including a shorter one.** Every placement leaves the engine with `end = start + duration`, so a 60-minute block held for a 30-minute task is not a roomy booking — **it is the wrong occurrence**, it blocks half an hour of the day from everything else, it skews FR-SCH-10's contention, and it shows the user a block of the wrong length. *Re-placing costs nothing and is correct by construction; **trimming `end` in place was rejected** because it computes a time outside the engine, which is the one thing FR-RSC-03 forbids.* **And "inside the window" is genuinely part of validity** even though FR-SCH-02 legitimately places tasks outside their window: without it, **editing the preferred window could never invalidate anything** — the old slot still fits, still free — and half of FR-TSK-04's own sentence would be dead text. | Patrick (ratified) · packet 06 RED agent (found + drafted) |
| **22 Jul** | **E14 — an edited occurrence is MOVED IN PLACE: same row, still `PLANNED`, trigger `EDITED`, no successor.** SRS **v2.16**. | FR-RSC-02's reasoning, unchanged: a `MISSED` or `SKIPPED` original is kept because **something happened** at the old time and FR-ANL must read it. **Nothing happened here** — the user changed the task before its occurrence came round. A successor would also need a status for the row left behind, and `PlacementStatus` has none, which is exactly why v2.14 declined to invent one for displacement. ⚠️ *Found only because **nothing in the RED suite pinned it**: the shape was invisible, packet 07 would have chosen one by instinct, and it is the shape FR-ANL and the dashboard read. An escalation about an **absence** is the hardest kind to notice and the most valuable to record.* | Patrick (ratified) · packet 06 RED agent (found + drafted) |
| **22 Jul** | **A RED branch reaches `dev` only after its GREEN packet passes — the pair merges together.** Amends §8.5's merge gate. | §8.5 admits a branch to `dev` only once `npm run verify` passes, and **a RED branch can never satisfy that**: its tests fail by design. **Merging it red breaks the one thing the rule exists for** — Ryan and Miguel pull `dev`, run `verify`, and it fails for reasons that have nothing to do with their work. *The freeze does not need `dev`: `guard:tests-frozen` compares the working tree against a **sha**, which exists the moment the commit does, on any branch — and §8.3 is explicit that the **commit** is what buys the RED/GREEN permission boundary. The split needs a session boundary, not a branch boundary.* So the RED freeze commit and its GREEN implementation land on the same branch, and it merges green. **No exception to §8.5 is needed, and `dev` stays green.** | Patrick (decided) · packet 06 RED agent (drafted the row) |
| **22 Jul** | **Packet 16 is split into 16a (FR-RSC-03 + FR-SCH-05, runnable now) and 16b (FR-LIB-02 + FR-REC-11, after packets 10/11).** | **An (I) requirement with no executable guard silently becomes false, and FR-RSC-03 — "exactly one placement function" — is the most exposed of the four.** It held through packets 06 and 07 only because agents behaved and a human grepped, which is precisely the state §4.8 says rots. Two of the four subjects exist now (the engine and the reschedule service); two do not until Ryan's recommendation path and libraries land. **Waiting for all four would leave FR-RSC-03 unguarded across exactly the window packet 12 adds `server/src/api/` and `server/src/db/`** — a stray `findNextFreeSlot()` there would pass every test and every review. It is also cheaper to write now, against a small and known-correct `server/src/reschedule/`, than later against a codebase nobody knows cold. *Cost is one extra packet number and this row; the pack phase stays GUARD for both.* ⚠️ **Both halves carry the same discipline: name the legitimate arithmetic exceptions in the packet** (`Math.max(day.start, now)`, the exact-length validity check) **so the guard is not written to fit the code, and make every guard fail on purpose before trusting it** — this project has shipped two guards that were green while enforcing nothing. | Patrick |
| **22 Jul** | **Packet 17 is split into 17a (RED, the acceptance suite) and 17b (GREEN, the wiring), so FR-REC-04's test is frozen before the code it verifies exists.** FR-REC-04 also **moves out of packets 09–10**, where the prompt index had put it. | **FR-REC-04 is the load-bearing requirement of the project, it is Core, and it is verified by TEST** — *"add a conflicting commitment over a recommended workout and assert it is re-placed."* **The index put it in Ryan's 09–10 pair; the build order put the wiring in Patrick's 17. Both readings were live, and one of them resolves to the same person writing the wiring and the test that says the wiring works** — the exact failure the RED/GREEN split exists to prevent, landing on the one requirement §2.7.1 says the project would rather lose everything else than lose. **It would be green on 31 July and would prove only that the code agrees with itself.** *Rejected: leaving it in Ryan's 09 RED, which is the strongest freeze ordering available but loads the critical path — he is blocked on OPEN-01, has never run the method, and the test is integration-level, spanning the engine, `RescheduleService` and task creation, none of which are his packets. Rejected: leaving 17 unsplit, which is the status quo and the failure itself.* **The split costs no new packet number and no critical-path time**: 17a can run as soon as 09 RED fixes the recommendation shape, while 17b still waits on 10 and 12. ⚠️ **17a needs a second human at its gate** like any RED freeze — by then Ryan or Miguel will have been through one. | Patrick |
| **22 Jul** | **Two service-level invariants promoted from packet 07's implementation into the SRS. → v2.17, §3.8.5.** **(1)** The busy set handed to the engine is **`PLANNED` + `COMPLETED` only**. **(2)** *"Fully elapsed"* in FR-RSC-01 means the **occurrence's** interval, not the task's **preferred window** — the requirement uses one word for two different intervals. | **Both were decided by an implementation and recorded nowhere binding, which is the failure this project keeps catching in new places** — OPEN-12's identifier, FR-SCH-03's unevaluable criterion, FR-SCH-10's missing timestamp, and now two rules living in a commit body. **Neither is pinned by a test**, so nothing would have failed when packet 08 or packet 12 answered them differently. **Promoted before the branch merged**, so `dev` never carried an implementation whose governing rules existed only in its own history. ⚠️ **(2) is a correction, not a clarification.** Read FR-RSC-01's *"window"* literally as the preferred window and **the System churns forever**: a 21:15 successor of a task whose window closed at 20:30 is elapsed the instant it is created, so every retrieval re-misses and re-places it — **and FR-RSC-06's termination argument, which rests on each successor starting strictly later, quietly stops holding while every test stays green.** **(1)** is what makes FR-RSC-09's *"frees the interval it held"* true by construction rather than by cleanup code: a `CANCELLED` row simply leaves the set. | Patrick (decided) · packet 07 GREEN agent (surfaced, having recorded them as choices no test pins) |
| **22 Jul** | **Packet 06's frozen suite re-frozen from `8b24320` to `c30e784` — one defective fixture, corrected in a RED-style commit.** `triggers.test.ts`'s three-trigger scenario asserted a `DISPLACED` it never set up: the shared `SKIP_CANDIDATES` moved Gym clear of the Advisor before the commitment landed, so nothing overlapped and nothing could be displaced. **The assertion did not move; a local fixture replaced the shared one.** | **Logged because the manifest says so.** `scripts/frozen-tests.json` carries its own instruction that *"changing or removing an entry is how you would defeat this guard, so it is a team decision with a row in TEAM-MEETING.md — exactly like editing the tests themselves."* **A re-freeze with no row is indistinguishable from a re-freeze that hid a weakened test.** ⚠️ **The valuable part is how it surfaced.** The GREEN agent could not pass the test and **refused the two rules that would have rescued it**: displacing non-`COMPLETED` rows overwrites DR-06's record that the user declared a skip, and matching overlap at task level while moving a different row leaves a lingering `SKIPPED` row that re-fires displacement on every retrieval. **Both are non-idempotent — and `idempotence.test.ts` has no skipped rows in its fixtures, so either would have stayed green while being wrong.** That is the "false, plausible, and green" shape exactly, caught because an implementer is forbidden from touching a test and therefore has nowhere to put the problem except a report. **The fix was made by the RED session, told not to read `server/src/reschedule/` and not to run the suite** — with the stub gone, a green run would have meant the fixture was tuned until the implementation liked it. | Patrick (decided) · packet 07 GREEN agent (found) · packet 06 RED agent (fixed) |
| **22 Jul** | **OPEN-18 — FR-TSK-04 is performed by a SEVENTH service method, `onTaskEdited(task, date)`, and `RescheduleTrigger` gains `EDITED`. SRS → v2.15.** Raised by Miguel while authoring packet 12. **The boundary adopted, which also settles OPEN-17: does the occurrence already exist?** An occurrence that exists is re-placed by `RescheduleService` — missed, skipped, displaced, **edited**. A task with no occurrence yet is placed by the **creation path in packet 12**, which is where a day's FR-SCH-10 ordering lives too. | **FR-TSK-04 is Essential and appeared exactly ONCE in the SRS** — no use case, no sequence diagram, no class, nothing in §3.6 answering *"the task's own attributes changed."* **An obligation with no assigned mechanism is implemented by whichever module reaches it first, or by nobody**, and this one was about to be reached by two. **Rejected: handling the edit in the API layer.** It is defensible on filing — FR-TSK-04 sits under Task Management, not FR-RSC — but filing describes the **user's action**, not the mechanism, and §3.8.4 already defines FR-RSC as *"when that same function is called again"*, which is precisely what an edited placement needs. ⚠️ **The decisive argument is the validity check, not the re-placement.** Answering *"does this placement still fit the new duration, still lie in the new window, still avoid every busy interval?"* is a **predicate**; answering *"where should it go instead?"* is the **engine's, always** (FR-RSC-03). Those two questions sit one careless step apart, and putting the first in a second owner is how the second one gets written there too. **The rule is therefore explicit: the check may only reject, never choose.** **Two things the question did not surface.** `RescheduleTrigger` was `MISSED | SKIPPED | DISPLACED` — **an edit-driven move had no value**, so DR-06 could not say what moved the placement and FR-DSH-05 could not write *"Moved to 7:00 PM — you changed how long this takes."* **E10's defect shape exactly, four hours later.** And **FR-SCH-10 does not apply** to a single edited task placed against an existing busy set, so the ordering duplication the question feared does not arise — that risk lives in the creation path, and is OPEN-17. **Settled before packet 06's freeze deliberately: a method and an enum member cost minutes now and a re-freeze afterwards.** | Patrick (decided) · Miguel (found) |
| **22 Jul** | **E1 — a missed task could never be re-placed, and the fix belongs to the caller. SRS → v2.14.** A missed task's preferred window is behind `now` by definition, and v2.12 has the service hand the engine a day starting at `now` — at which point **FR-SCH-09's last boundary row obliges the engine to return an empty result** for *"preferred window entirely outside the schedulable day."* **Every missed task, always.** Resolution: where a window has **fully elapsed**, the engine is invoked with a **derived task whose window is the remainder of the day**; the stored task is untouched. Partly-elapsed windows pass through unchanged. | **This is the defect the RED/GREEN split exists to find, and it was found three days before it would have been found on stage.** UC-05's 21:15 was unreachable, and §2.7.1's *"the schedule repairs itself"* was false for the Core's own trigger — **with 30 engine tests green the whole time**, because the engine is correct: FR-SCH-09's boundary row is right, and the service was asking it the wrong question. **Why the caller and not the engine:** the engine cannot distinguish *"the user asked for 2 AM"* from *"the caller narrowed the day past the user's window"* — the two arrive as byte-identical input. **A rule that needs information only the caller holds has to live in the caller**, which is the same argument that made `schedulableDay` a parameter instead of something the engine derives. Changing the engine would also have re-opened the suite frozen at `ac06e70` to repair a defect it does not have. ⚠️ **Two consequences recorded so they are not lost:** on a substituted call every candidate is "within the preferred window" as far as the engine can see, so **`Slot.withinPreferredWindow` must not be shown as meaning the user's stated preference** (FR-DSH-05); and where `now` is past the end of the day there is no valid `Interval` to pass, so the System **does not call the engine** and reports `DAY_FULL` — **the only case where the user-facing reason does not originate in the engine.** | Patrick (decided) · packet 06 RED agent (found) |
| **22 Jul** | **E10 + E6 — each trigger's effect on stored state is now written down, and they are deliberately not the same shape. SRS → v2.14.** **Missed and skipped:** the original occurrence is kept (`MISSED` / `SKIPPED`) and a **successor** is created. **Displaced:** the same row is **moved in place** — still `PLANNED`, `rescheduleTrigger` = `DISPLACED`, no successor. **FR-RSC-09's correction is scoped to the missed classification only.** | **`PlacementStatus` had no member describing a displaced occurrence, and §3.4's sequence diagram has always said *"Update Gym placement → 17:45"* — so the diagram and the state model disagreed, and packet 06 could not write the test without picking one.** Adopting §3.4 is right on the merits, not just on precedent: **nothing happened at the old time.** A miss or a skip is a fact about the user's day that FR-ANL must be able to read; a displacement is the day being rearranged before the fact, and inventing a `DISPLACED` status would be **recording that nothing happened.** ⚠️ **This also corrected FR-RSC-06's idempotency note written earlier the same day**, which explained idempotency by a state change displacement does not make — displacement was idempotent for a reason the SRS did not give. The note is now general: **a trigger is a no-op when the condition that fires it is no longer true of the stored schedule**, reached by leaving `PLANNED` (missed, skipped) or by no longer overlapping (displaced). **E6 falls out of the same principle:** FR-RSC-09 exists because a miss is **inferred from silence** and can be wrong with nobody having said anything, whereas a skip and a displacement are **events the user witnessed**. And after this decision a displacement leaves no earlier occurrence to withdraw at all. | Patrick (decided) · packet 06 RED agent (found) |
| **22 Jul** | **E9 + E2 — `TaskRepository` and `Clock` ratified into §3.6, and the repository is ASYNCHRONOUS. `onCommitmentAdded` gains a `date`.** §3.6 named `-repo: TaskRepository` and listed no members; packet 06 drafted seven, plus a three-method `Clock`, because the suite could not be written without them. Ratified as drafted **except** that every storage method returns a `Promise` (`nextPlacementId` stays synchronous — identity generation is local). `Clock` stays synchronous. | **Overruled packet 06's recommendation to keep the port synchronous.** Its argument was that promises would make the frozen suite about plumbing rather than rescheduling, which is true and is worth less than the alternative: **packet 12 implements this port over the official MongoDB driver (CON-08, SI-03), which is promise-based, and there is no synchronous alternative.** A port shaped for test ergonomics that **the only real implementer cannot satisfy** is the wrong port — and discovering that in week four means editing tests frozen in week three, which is the one thing the method forbids. **The cost of being wrong is asymmetric:** `await` in a test costs one keyword; a re-freeze costs the correctness argument. **E2 is the same defect one level down** — `Task` records *when in a day* and never *which day*, so `onCommitmentAdded(commitment)` silently assumed "today" and would have been wrong for any commitment added for next Tuesday. Fixed now, while six tests call it, rather than after the freeze. | Patrick (decided) · packet 06 RED agent (found + drafted) |
| **22 Jul** | **E3, E4, E5, E7 — four smaller corrections from packet 06's RED run. SRS → v2.14.** **FR-RSC-05** now reads *"missed, **skipped**, or displaced"*. **Two citations** reading UC-06 were corrected to **UC-10**. **FR-SCH-10's mixed-`createdAt` case** is specified as a single ranking key. **`.eslintrc.cjs`** gains `argsIgnorePattern: '^_'`. | **E3:** FR-RSC-08, UC-10 and UC-13 all already gave a skipped occurrence the next-day offer; only the sentence that governs it said *"missed or displaced"*. Three artifacts against one omission is not a close call. **E4:** the text quoted lives in UC-10 ("The Day Is Genuinely Full"), not UC-06 ("A New Fixed Commitment Displaces a Task") — **both citations were written into the SRS earlier the same day by this session, so the error is mine and worth naming as such.** The cost of leaving it is that the next reader cannot tell whether the edit went astray or only the pointer did. ⚠️ **E5 overrules packet 06's proposal.** It recommended *"if either task lacks `createdAt`, order that pair by `id`"* — **that comparator is not transitive and therefore defines no order at all**: with A(10:00, `a`), B(absent, `b`), C(09:00, `c`) it gives A<B, B<C and C<A, and a sort handed a cycle returns whatever its pivot choices produce — **differing between two runs of the same day, which is precisely what FR-SCH-10 exists to prevent.** Replaced with a single key, `(has an instant, the instant, id)`, total by construction. *The proposal was a reasonable reading of my own wording, which said "where `createdAt` is absent" and never said whose.* **E7:** a RED stub has no body, so every parameter is unused by construction while the packet demands zero lint errors — each RED session was going to smuggle in a file-level disable and each GREEN session was going to have to remember to remove it. One line in the config ends that for every future packet; **verified by removing packet 06's disable and re-running lint clean.** | Patrick (decided) · packet 06 RED agent (found) |
| **22 Jul** | **FR-RSC-06's idempotency is enforced by the occurrence's STATE, not by a record of past triggers. SRS → v2.13, OPEN-16(3) closed — and OPEN-16 with it.** A trigger acts only on a **`PLANNED`, incomplete** occurrence; handling it moves that occurrence out of `PLANNED`, so a second firing finds nothing to act on. **No trigger log, no dedupe collection, no cap.** | **FR-RSC-10 re-evaluates on every schedule retrieval, so this is not a corner case** — a user refreshing five times fires the missed check five times, and exactly one placement must result. **Rejected: a stored processed-trigger record.** It is a second source of truth about what already happened, and it must be unwound by hand when FR-RSC-09 cancels a reschedule — **the same drift failure rejected an hour earlier for `UNPLACED`**, and in both cases the divergence between the two copies is invisible until someone notices the schedule is wrong. The occurrence's status already carries the fact; storing it twice only creates the chance for the copies to disagree. **Termination is proved, not capped:** the day handed to the engine begins at `now`, so each successor starts **strictly later** than what it replaces — a strictly increasing sequence bounded by the end of the day terminates, and FR-RSC-05 ends the chain when the day runs out. ⚠️ **An "at most N reschedules per day" cap was specifically rejected**: nothing in the SRS names a number, it needs a refusal path with no user-facing story, and **when it fires it silently denies a legitimate reschedule while everything still looks correct** — a guard against a failure this design cannot have, bought with a failure it otherwise would not. | Patrick (decided) · Patrick + AI (framed) |
| **22 Jul** | **An unplaceable task is represented by the ABSENCE of a placement, and FR-RSC-05's offer is actionable. SRS → v2.13, OPEN-16(2) closed.** No `UNPLACED` member added to `PlacementStatus`; the reason is **recomputed on retrieval** from the engine rather than stored; accepting the offer re-invokes **the same engine** for the next day via a new `RescheduleService.moveToNextDay` (§3.6). **Shipping: inform + accept-next-day. Not shipping: discard-for-the-day, and rearranging today to make room.** | **UC-06 already required more than FR-RSC-05 said** — *"the task remains visible and retrievable in an **unplaced state**"* — and **`PlacementStatus` had no such state**, so UC-06 named something the System could not represent: OPEN-15's defect shape one layer down. **Rejected: adding `UNPLACED` and storing a row.** A stored flag can drift out of step with the schedule; an absence cannot. And a `Placement` that is not placed anywhere would still carry a `start` and an `end` — a row asserting *"this is where it is"* while being nowhere, which DR-03 makes worse rather than better. **The derived form also self-heals:** delete the meeting that filled the day and the next retrieval simply places the task, offer gone, nothing to clean up. *Recomputing costs one pure engine call under 200 ms (NFR-PERF-01), which is cheaper than the class of bug stale state produces.* **On the affordances, asked directly and bounded on purpose:** *discard-for-the-day* looks free but is not — FR-RSC-08's skip **re-invokes the engine by definition**, so "discard without re-placing" is a *different* obligation needing its own requirement and an FR-ANL-06 note, and nothing Essential asks for it. *Rearranging today to fit the task* **is FR-SCH-07 displacement** — Conditional, barred by §2.7.1 until every Essential requirement is verified, and with 06/07, 08–10, 12, 13–15 and the FR-REC-04 wiring all still unbuilt, it is the option that costs the demo (`CLAUDE.md` §5, §6). | Patrick (decided) · Patrick + AI (framed) |
| **22 Jul** | **An automatic reschedule takes the engine's RANK-1 candidate — for all three triggers. SRS → v2.12, OPEN-16(1) closed.** *"The remainder of the day"* is expressed by the service handing the engine a **`schedulableDay` that begins at the current time**, so every candidate returned is already in the remainder — the service shapes the day, the engine ranks within it. | **FR-RSC-01 says *"the next valid slot remaining that day"*; FR-SCH-02 returns up to THREE ranked candidates. Nothing said which one the automatic path took.** **The rejected reading — literal "earliest remaining" — is indistinguishable for a missed task**, because the preferred window has elapsed and FR-SCH-03's proximity criterion therefore already yields the earliest remaining slot (UC-05's 21:15 is unchanged under either rule). **It bites only on a skip declared in advance or a displacement**, where the preferred window is still ahead: a 17:00 gym session skipped at 15:00 goes to 15:30 under "earliest" and to the slot nearest 17:00 under rank 1 — sooner, and further from what the user actually asked for. ⚠️ **The structural reason, which outlives this particular case: taking anything but rank 1 means the service applies its own criterion to a list the engine has already ranked.** That is not yet a second placement function, but it is the first line of one, and FR-RSC-03 is the requirement it erodes. **One rule for all three triggers also keeps FR-RSC-08's *"exactly as FR-RSC-01 does"* literally true** rather than approximately true. | Patrick (decided) · Patrick + AI (framed) |
| **22 Jul** | **`Task.createdAt` added to the contract — FR-SCH-10's tiebreak had nothing to read. SRS → v2.11, OPEN-15 closed.** An **ISO 8601 UTC instant** (new `IsoTimestamp` alias), **optional in the type**, set at the API boundary. An absent or identical instant falls through to **ascending `id`**, so the order is total in every case. | **FR-SCH-10 broke a priority tie on the "earlier-created" task, and no type recorded when a task was created** — the same defect as FR-SCH-03(b) in v2.7: a criterion with no field behind it. **Two alternatives were rejected, both for failing silently.** *Tiebreak on `id` alone* is total and repeatable today, and free if identifiers turn out to be Mongo ObjectIds (whose hex order **is** creation order) — but it stops meaning "earlier-created" the instant OPEN-12 picks a random UUID, and **nothing goes red when it does.** *A stable sort on whatever order the repository supplies* costs nothing now and moves the requirement's truth into a layer that does not exist yet: a forgotten `sort` in packet 12 makes FR-SCH-10 false while packet 06's tests stay green. **An instant, not `IsoDate`:** two tasks created eleven minutes apart on one day are the ordinary case, and a calendar date would tie them again — closing the hole in the way that reopens it. **Optional in the type on purpose:** the 30 engine tests frozen at `ac06e70` construct `Task` literals, so a **required** field breaks their typecheck and forces a re-freeze — real ceremony (§8.3) spent on a field **the engine never reads.** ⚠️ **The purity question, answered once so it is not re-asked:** a timestamp on a type the engine receives does not weaken FR-SCH-05. Purity forbids the engine **consulting** a clock; the caller **passing** it data is what the whole design already does with `schedulableDay`. *Verified immediately after the edit: typecheck clean, 30/30 frozen tests green, `guard:tests-frozen` passing.* Both copies changed identically — `shared/src/contract.ts` and `prompts/foundation/03-shared-contract-types.md`, per §4.7. | Patrick (decided + authorised) · Patrick + AI (found + applied) |
| **22 Jul** | **Source folders are named for the MODULE, never for its owner.** `server/src/` is laid out by what the code *is* — **`reschedule/` (06–07), `wearable/` (08), `recommendation/` (09–10), `catalog/` (11), `api/` + `db/` (12)** — with tests mirroring at `server/test/<module>/`. **`RescheduleService` therefore lives at `server/src/reschedule/`**, and it receives **both the engine and the current time by injection**: the engine typed `FindCandidateSlots`, and `now`/the date as data (`Minute` / `IsoDate`), never read from a clock inside the service. Written into packet 06. | **Patrick's call, asked directly: "there is no ownership of folders in this project — place it where it makes the most sense and keep consistency, so things can be found when they are looked for."** A tree organised by owner answers a question nobody asks at 11pm; a tree organised by module answers the one everybody asks. It also mirrors §3.6's class clusters, so the diagram and the directory listing read the same way, and it matches the `engine/src` ↔ `engine/test` shape already committed. ⚠️ **Note the distinction from `prompts/`, which IS grouped by owner (§3)** — a packet is a unit of human review, a source folder is a unit of subject matter, and conflating them is what produced the earlier draft of this row. **It cannot live in `engine/`** regardless: `engine/package.json` has a permanently empty `dependencies` block (§4.7) and the service needs a store, and §3.8.4 draws the line explicitly between *the pure decision* and *the stateful policy that decides when to re-decide*. **Injecting the engine is what makes FR-RSC-03 testable rather than only inspectable** — a recording double proves the service *asked* for a placement instead of computing one, one stage earlier than packet 16's guard. **Injecting the time is forced by FR-RSC-10's own verification wording** — *"with the clock advanced past a placed occurrence's window … with no background process running"* — which a test cannot do to a service that calls `Date.now()` itself; the suite would also become time-of-day dependent and fail at midnight for a reason nobody diagnoses on 30 July. *Same reasoning that made `Minute` the engine's time type.* **Ryan and Miguel need this before packets 08 and 12, not for permission but so their trees match.** | Patrick (decided) · Patrick + AI (framed) |
| **22 Jul** | **Packets 06–17 are authored by the human who owns them, immediately before they are run** — not written up front in a planning pass. | Restates `CLAUDE.md` §4.11 as an operating instruction now that building has started: **a prompt authored before its module is designed encodes guesses, and an agent implements a guess as faithfully as a requirement.** The practical consequence for the build order: **authoring a packet is the first half of its own work item**, not overhead that can be batched or skipped. | Team |
| **22 Jul** | **`Task.recurrence` added to the contract — `{ frequency: 'DAILY' \| 'WEEKLY'; daysOfWeek?: readonly number[] }`, optional.** SRS → v2.19. | Found authoring packet 12: FR-TSK-01's own attribute table and §5's Data Requirements table have always listed `recurrence` as a Task attribute, and FR-TSK-05 (Essential, Core) requires expanding a recurring task into one placement per matching day — but the frozen contract had no field to read, the identical defect shape as `Task.createdAt` (OPEN-15) and FR-SCH-03's removed criterion (b). **Rejected: scoping packet 12 to non-recurring tasks and deferring FR-TSK-05** — it sits inside §2.7.1's Core Demonstrable Capability, so deferring only moves the same decision closer to 31 Jul under worse time pressure. Optional in the type for the same reason `createdAt` was: absence means "no recurrence" (FR-TSK-01's own default) and the 30 engine tests frozen at `ac06e70` need no re-freeze. **The engine never reads it** — FR-TSK-05's expansion is packet 12's, before anything reaches the engine, mirroring FR-CAL-06's identical rule for calendar-sourced recurrence. Verified: typecheck clean, 184/184 tests green. | Miguel (asked) · user (authorised, standing in for the §4.7 gate) + AI |
| **22 Jul** | **OPEN-12 closed — the user identifier is the Mongo `_id` of the `User` document, stringified**, minted at account creation and carried in the session; email is used only to look it up at login. **Ownership lives at the API/persistence boundary** — `shared/src/contract.ts` gains no `userId` field. | `TaskRepository.ownerOfTask(taskId)` (§3.6, ratified at v2.14) is a repository-level lookup, which already meant ownership could not live on `Task` itself — the "domain types vs. boundary" half of OPEN-12 was effectively answered before this decision named it. **The remaining question was what the identifier itself is.** Rejected the email address as the identifier: it would put a PII string on every `Task`, `Placement`, and `Metric`-owning document for no benefit an opaque id doesn't already give, and it ties identity to a field (email) the System never promises stays fixed. SRS → **v2.18**: FR-USR-04's note, Appendix C's OPEN-12 row. | Miguel (owns the answer, per FR-USR-04's note) + AI |
| **22 Jul** | **OPEN-19 closed — packet 12's `TaskRepository` tests run against `mongodb-memory-server`**, a throwaway in-process Mongo spun up per test run. Added to `server/package.json` as a **devDependency**. | It exercises the real driver and real queries, so a broken query fails the test that's supposed to catch it — a hand-rolled in-memory fake was rejected for the opposite reason: it could pass while the same query fails against real Mongo. A real local `mongod` was rejected because it makes the suite environment-dependent — every machine and CI would need one running, plus manual cleanup between runs, for a guarantee `mongodb-memory-server` gives for free. ⚠️ **This governs the test suite only.** The running server (dev, demo) still needs a real MongoDB — a local `mongod` or an Atlas connection string in `.env` — nothing about this decision starts one for you. | Miguel (owns the answer, §8.4) + AI |

---

# 🔓 OPEN ISSUES

*Mirrors Appendix C of the SRS. Close them here as they're resolved.*

| ID | Issue | Owner | Due | Status |
|---|---|---|---|---|
| **OPEN-01** | ✅ **CLOSED 22 Jul — both metrics exist, as published fields.** Ryan pulled the full Garmin GDPR export (38 top-level folders) and read it. **Exactly two files matter**, and everything else in the export maps to no requirement: **(1)** `DI_CONNECT/DI-Connect-Aggregator/UDSFile_*.json` → **`activeKilocalories`** (kcal, float), separate from `bmrKilocalories` and `totalKilocalories` — **84 of 85 days in 2026**; **(2)** `DI_CONNECT/DI-Connect-Wellness/2023-12-16_2024-10-04_118195747_sleepData.json` → **`sleepScores.overallScore`** (integer 0–100). **Both files key on `calendarDate` in `"YYYY-MM-DD"` form**, which is already the contract's `IsoDate` and is the join key. *Original question:* ~~Pull a real Garmin export; confirm it has a sleep score AND active calories.~~ | **Ryan Woosley** | ~~16 Jul~~ **done 22 Jul** | ✅ **Closed** |
| **OPEN-02** | ✅ **CLOSED 22 Jul — no derivation formula is needed; Garmin publishes the score.** `sleepScores.overallScore` is an integer 0–100 that maps directly onto FR-REC-01's tiers. **This question existed only in case the export lacked a single published score. It does not.** ⚠️ **Two properties of the real data that a fixture must preserve:** the `overallScore` **key is ABSENT** on a record that otherwise exists and is marked `ENHANCED_CONFIRMED_FINAL` (2024-01-11, 2024-09-19) — the `sleepScores` object is present but carries only `{feedback, insight}`, with no `overallScore` field *(corrected 25 Jul from an earlier "can be `null`" — the real shape is a missing key, not a null value; verified against the export)*. That is FR-WER-06's unavailable branch occurring naturally, and an adapter doing `sleepScores.overallScore ?? 0` would silently recommend a recovery session for a night that was never measured — the guard must key on **presence** (`'overallScore' in sleepScores`); and the 22 real scores **span all three tiers (14 → 83) and include an exact 75**, one of the four boundary values FR-REC-01's verification note names. | **Ryan Woosley** | ~~16 Jul~~ **done 22 Jul** | ✅ **Closed** |
| **OPEN-26** | ✅ **CLOSED 25 Jul — the real export has no intra-file duplicate `calendarDate`s** (checked both files: unique in each). The winner-rule does not occur in this data, so *last-write-wins per `(date, metric)`* stays only as a defensive default consistent with FR-WER-09's upsert, and 08b GREEN needs no duplicate-resolution logic. Raised by packet 08 RED (E2); the "2026-04-01 twice" it cited was its synthetic fixture, not the real export (that date is in a gap in the real UDSFile). | **Ryan Woosley** | ~~Before 08b GREEN~~ **done 25 Jul** | ✅ **Closed** |
| **OPEN-20** | ✅ **CLOSED 25 Jul — the discriminator is the presence of the `totalSteps` key**, now written into packet 08 as a stated constraint (decision log, 25 Jul). `activeKilocalories: 0.0` means two unrelated things — the watch was not worn (**no data**, FR-WER-06 → unavailable) or it was worn and the user barely moved (**a real measured zero**, 2026-03-17 has 26 steps). Garmin publishes no explicit flag. **`totalSteps` present → measured (a `0.0` is a real zero); `totalSteps` absent → unavailable** — correct on all 88 days checked. `includesActivityData` was **rejected** (2026-02-13 carries 721 active kcal with `includesActivityData: false`). ⚠️ **Inferred from the data, not documented by Garmin — so it is a team decision, given to the adapter, never derived by it.** Getting it wrong is invisible: the value is a valid number either way and no test fails. | **Ryan Woosley** | ~~Before packet 08~~ **done 25 Jul** | ✅ **Closed** |
| **OPEN-03** | ~~Calendar source.~~ **CLOSED 12 Jul.** Import declined; **`.ics` export adopted** (SI-06 / FR-CAL-07, Conditional). Fixed commitments come from **manual entry only**. | Miguel | — | ✅ **Closed** |
| **OPEN-09** | ✅ **CLOSED 23 Jul — Ryan takes views 14 and 15; Miguel keeps packet 12 (backend) and packet 13 (the Core schedule dashboard, FR-DSH-01/05).** This balances the load and keeps the Core dashboard with the person who built its backend. *Rejected: Ryan takes only view 15 — lighter, but leaves Miguel with three; and leaving all three views with Miguel — four packets against §7.3's "reviewing is the bottleneck", on the person who also owns the demo's whole UI. The original fix (Patrick takes the backend) died when 06/07/16/17a/17b filled Patrick's queue.* *Original:* ~~⚠️ DIRECTION SET 22 Jul, pending Ryan and Miguel's agreement.~~ | Patrick (directed) · Ryan + Miguel (agreed) | ✅ **Closed** | ✅ **Closed** |
| **OPEN-10** | **Questions for the professor** — (1) is AI-generated prompting acceptable? (2) does he expect tests to be human-written? *(This one has teeth: NFR-MNT-01's 90% coverage and NFR-COR-01's property tests stop meaning what they appear to mean if one agent writes both the tests and the code.)* (3) deliverable format for the SRS? | Patrick | Before 16 Jul | 🔴 Open |
| **OPEN-11** | **Scope cuts not yet ratified** — team didn't recall them at Meeting 1. FR-REC-10 and NFR-MNT-02 → Conditional. Re-stated at the top of this file. | Patrick | **Meeting 2** | 🔴 Open |
| **OPEN-04** | ✅ **CLOSED 27 Jul — both halves done (packet 11b GREEN, merged to `dev`).** Exercise dataset: `yuhonas/free-exercise-db`, The Unlicense (public domain), vendored + attributed (FR-LIB-10); FR-LIB-04 tier mapping + per-tier durations implemented and frozen-tested; **the meal library is now hand-authored** (`server/src/catalog/mealLibrary.ts`) with calorie counts + dietary flags, satisfying FR-LIB-06/07 (the ±10% sweep for every dietary combination is green). Full dataset/mapping/duration reasoning: decision log, 27 Jul. | **Ryan Woosley** | ~~20 Jul~~ done 27 Jul | ✅ **Closed** |
| **OPEN-28** | **Two `Catalog` interfaces coexist and must be reconciled when the real catalog is wired in.** Packet 17b left a **minimal** port in `server/src/recommendation/RecommendationScheduler.ts` (`findWorkouts(tier, count)`, with a comment deferring to packet 11) so its acceptance suite could run against a `Catalog` double; packet 11 now defines the **full** server-local `Catalog` (`findWorkouts(tier, prefs, n): CatalogResult<Workout>`, `findMeals(...): CatalogResult<Meal>`). When 11b GREEN lands the real seeded catalog and it is wired into the recommendation/scheduler path, 17b's minimal port must be **replaced** by packet 11's interface — not left as a second, diverging shape. Surfaced by the packet 11 RED gate (27 Jul). **Not a freeze blocker; a wiring/integration item for whoever connects the real catalog to FR-REC-03/04.** | **Ryan Woosley** *(raise with Patrick — 17b is his)* | Before the §6 rehearsals | 🔴 Open |
| **OPEN-30** | ✅ **RESOLVED 28 Jul — "do what the SRS says" (Ryan, owner): NO FR-ANL-01 amendment; the occurrence reading is what the SRS already compels, and it is now pinned.** The literal "consecutive completed *days*" seemed to point at calendar days (a weekly habit's streak = 1), but **ratified FR-ANL-07 (v2.31) — a `SUPERSEDED` occurrence does not break a streak — already rules a strict calendar-day reading out**: it would give the pinned SUPERSEDED test (day0 COMPLETED, day1 SUPERSEDED, day2 COMPLETED) a streak of **1, not the asserted 2.** And once a *System-excused* day cannot break a streak, a day the habit was **never scheduled** cannot either — otherwise the System replacing a workout would *beat* a rest day, which is incoherent. So the SRS read **whole** compels a streak over **resolved occurrences** (weekly = **3**) with **no wording change**. Now pinned by a new 15a test (`a weekly habit … streak of 3, not 1`). The earlier proposed amendment is **withdrawn**, and the cross-ref move (UC-11 line 727 "consecutive-day streak") is **moot** — no SRS text changes. Full reasoning: `docs/P15A-RED-REPORT.md`, item ②. *Original: FR-ANL-01's "days" vs the suite's "occurrences" diverge for a weekly habit; a wording change was drafted and held for Patrick.* | **Ryan Woosley** *(owner; decided)* | ✅ **Resolved 28 Jul** | ✅ **Closed** |
| **OPEN-05** | Whether to attempt a live wearable API at all. **Do not open until all Essential work is verified.** | | 27 Jul | ⚪ Deferred |
| **OPEN-06** | Whether priority displacement (FR-SCH-07/08) is in scope. ⚠️ **Re-examined 22 Jul and deliberately left deferred.** The need that prompted it — *priority should decide which flexible task wins a contested slot* — **is now met by FR-SCH-10 (Essential)**. What is left in FR-SCH-07 is only **eviction of a task already placed**, which appears nowhere in the acceptance demo. **The design is now written down** (SRS FR-SCH-07 design note: service-located, three engine calls, revert on failure), so picking it up later is ~a day, not a week. **Do not start it before every Essential requirement is verified.** | Patrick | After Essential verified | ⚪ Deferred |
| **OPEN-07** | ~~Garmin Developer Program application.~~ | — | — | ✅ **Closed — excluded** |
| **OPEN-08** | **Team roles.** Blocks the SRS's annotated TOC and signature page. | Patrick | **12 Jul** | 🔴 Open |
| **OPEN-13** | ✅ **CLOSED 22 Jul — closest fitting position to the preferred window; earliest breaks a tie. Now FR-SCH-02 (SRS v2.9).** Inside the preferred window, earliest governs (every position is equally close). **The frozen engine tests were checked against the decision and all 30 remain correct** — every asserted position sits in an interval either exactly the task's length or after the preferred window. *Original question:* ~~**Where a candidate slot starts inside a free interval longer than the task.**~~ Two plausible rules — **earliest start in the interval**, or **start closest to the preferred window**. ⚠️ **Appendix A cannot tell them apart**: in all three of its ranked candidates they give the same answer, so the example that reads like an oracle is silent here. They diverge whenever an over-long free interval sits *before* the preferred window (free 07:00–10:00, 60-min task, 10:00 preferred → 07:00 vs 09:00). **The frozen engine suite deliberately asserts no start position in a case where they disagree**, so either answer can be adopted without touching a test — but if nobody adopts one, **packet 05 settles it silently** and it becomes an unstated requirement living only in the implementation. *(SRS Appendix C OPEN-13; raised by the packet 04 RED agent.)* | **Patrick Rucker** | **Before packet 05** | ✅ **Closed** |
| **OPEN-18** | ✅ **CLOSED 22 Jul — `RescheduleService.onTaskEdited(task, date)`; `RescheduleTrigger` gains `EDITED` (SRS v2.15).** Boundary: **an occurrence that already exists is re-placed by the service; a task with no occurrence yet is placed by packet 12's creation path.** Rejected handling it in the API layer — it would put the validity check in a second owner, and *"is this still legal?"* is one step from *"where should it go?"* (FR-RSC-03). *Original:* ~~**FR-TSK-04 obliges a re-evaluation and re-placement when a user changes a task's duration or preferred window, and no class in §3.6 performs it** — the service handled missed, skipped, displaced and completed, and nothing answered "the task's own attributes changed."~~ **Raised by Miguel while authoring packet 12; unblocked the same day.** | **Patrick Rucker** | — | ✅ **Closed** |
| **OPEN-19** | ✅ **CLOSED 22 Jul — `TaskRepository` tests run against `mongodb-memory-server`**, added as a devDependency in `server/package.json`. Rejected a real local `mongod` (environment-dependent, needs cleanup between runs) and a thin in-memory fake (doesn't exercise the real driver — a broken query could pass the fake). **The running server still needs a real MongoDB** — a local `mongod` or an Atlas string in `.env` — this closes the *test* strategy only. *Original:* ~~**How is the Mongo-backed `TaskRepository` tested?** Packet 06's repository double was in-memory; packet 12 implements the **real async repository over the Mongo driver**, and its tests need something to run against. **No runtime dependency is missing** — packet 02 pinned Express, the `mongodb` driver, bcrypt, jsonwebtoken, cors and dotenv, and all are installed (bcrypt's native build works on Patrick's machine). **What is missing is a test strategy**: a throwaway Mongo (`mongodb-memory-server`, the usual choice, **not installed**), a real local `mongod`, or a repository kept thin enough to test against an in-memory fake.~~ | **Miguel Alvarez** | — | ✅ **Closed** |
| **OPEN-21** | ✅ **CLOSED 22 Jul — `triggerFromHistory` no longer defaults; it returns `undefined` where the store holds no evidence of a reschedule, and the placement is written with no trigger at all. SRS v2.20 (FR-RSC-05 re-attempt note + DR-06 prohibition).** **Fixed at the root rather than at the report Miguel found it in.** The suggested shape — `sweepElapsed` passing `{ stamped: false }` on empty history — was right about the sweep but would have left **a second live instance**: `moveToNextDay` calls the same `triggerFromHistory`, and packet 12's API returns that outcome over the wire from `POST /tasks/:id/move-to-next-day`, so a never-placed task accepted onto tomorrow would still have reported `MISSED`. Removing the default fixes both paths and makes a third impossible. **`RescheduleOutcome.trigger` is now optional** on `RESCHEDULED` and `UNPLACEABLE` — FR-RSC-04's three facts are the facts of a *reschedule*, and a first placement is not one; the four genuine triggers still take a literal union and cannot produce an absence. `{ stamped: false }` is retained and is **not** the same thing: it means *"a real reschedule, but the row being written is a fresh placement"* (the accepted next-day offer, which still reports what it descends from). **Verified**: `npm run verify` green — 235/235, both freeze guards intact, nothing under `server/test/reschedule/` or `engine/` touched. ⚠️ **The case is specified but NOT YET PINNED by a test — see OPEN-22.** *Original:* ~~⚠️ **A brand-new, never-placed flexible task's FIRST placement comes back stamped `rescheduleTrigger: 'MISSED'`.**~~ | **Patrick Rucker** | ✅ **Closed 22 Jul** |
| **OPEN-22** | ⚠️ **SRS v2.20's re-attempt provenance rule is specified but has no test.** OPEN-21's fix is in, verified by hand and by temporary tests that failed on the old code and passed on the new — **but those tests were deleted rather than committed, on purpose.** §4.6 forbids one agent writing both a test and the code it tests, and the session that wrote this fix had read the implementation first; a test it authored would encode what the code *does* rather than what v2.20 *says*, go green, and look reasonable. **It is also not addable to packet 06's suite without a re-freeze**, which `scripts/frozen-tests.json` makes a team decision needing a second human (`AGENTIC-TDD-WORKFLOW.md` §6). **Proposed owner: packet 17a**, the acceptance RED suite — its agent sees the SRS and no implementation, which is exactly the clean-room the case needs, and v2.20 now states the rule in binding form for it to read. **Two cases to pin**: (1) a task with no placement history is placed with **no** `rescheduleTrigger`, and (2) a re-attempt that *does* descend from a `MISSED`/`SKIPPED` row still records it. *Until then this is a regression window: nothing would fail if the default came back.* ✅ **CLOSED 26 Jul — both cases pinned by packet 17a's frozen acceptance suite (`server/test/acceptance/reattempt-provenance.test.ts`).** Case 1 (no history → no trigger) and case 2 (MISSED/SKIPPED row → re-attempt records it) are asserted against the real `sweepElapsed`; both are GREEN against current code, so they are **regression guards** — the freeze is what closes the window (the convention "a RED freeze may hold a green regression guard" is recorded in the decision log, 26 Jul). *(Case 1's honest-reason companion is OPEN-24, which stays open — pinned RED, fix pending 17b.)* | **Patrick Rucker** | ✅ **Pinned + frozen 26 Jul (17a)** | ✅ **Closed** |
| **OPEN-23** | ✅ **CLOSED 22 Jul — `wp-12-backend-api` merged `origin/dev`'s two commits (`ad96c8c`, `22950a3`, Ryan's OPEN-01/02 closure), keeping both sides in all three colliding regions**, per the rule below. `npm run verify` checked green (235/235, both freeze guards intact) both immediately before and immediately after resolving — confirming the merge was the docs-only operation it was expected to be. **(1)** `CLAUDE.md`'s *"Last updated"* line: Patrick's OPEN-21/v2.20 paragraph now leads (it's the later edit, dated 22:29 vs. Ryan's 21:09), followed by Ryan's OPEN-01/02/§4.1 paragraph, followed by the shared historical tail once — not duplicated. **(2)** `CLAUDE.md` §9's status block auto-merged with no conflict — the two edits landed on non-overlapping lines. **(3)** `docs/TEAM-MEETING.md`'s decision log: both pairs of new rows kept, in sequence. *Original question below.* ~~`wp-12-backend-api` is two commits behind `origin/dev`, and the reconciliation is a KNOWN, BOUNDED doc merge — resolve it before the branch merges to `dev`.~~ | **Miguel Alvarez** | ✅ **Closed** |
| **OPEN-25** | ✅ **CLOSED 23 Jul — confirmed and fixed, not just hypothesized.** Re-ran `npx jest --coverage` back to back several times to force a repeat; the third run reproduced it with a much more specific error than the original: `Port "52703" already in use`, thrown by `mongodb-memory-server-core`'s `MongoInstance` itself (`server/test/api/app.test.ts`, a different test than the first failure — confirms it's not specific to one test, it's the shared Mongo-instance infrastructure). **Root cause confirmed**: every server test spins up its own `mongodb-memory-server` instance per `beforeEach`/`it` (`server/test/support/testApp.ts`), and the library's ephemeral-port selection has a known race when many instances start at once — with no `maxWorkers` cap, Jest runs the 5 server test files in parallel workers, occasionally landing two instances on the same port. **Fixed**: `jest.retryTimes(2, { logErrorsBeforeRetry: true })` in a new `server/test/support/jest.setup.ts`, wired via `setupFilesAfterEnv` in `server/jest.config.cjs` — Jest's own built-in mechanism for exactly this class of infrastructure flake (jest-circus only, which this project already uses by default). **Rejected: capping `maxWorkers`/`--runInBand`** — would serialize all 5 server test files for every future `npm run verify`, a permanent slowdown to eliminate an intermittent third-party library race; a bounded retry costs nothing on the common case (no failure) and absorbs the rare one. **Verified**: 7 full `npx jest --coverage` runs after the fix (4 dedicated + a fresh `npm run verify` + 2 more), all green at 243/243. *Original, superseded below.* ~~`server/test/api/app.test.ts`'s FR-TSK-07/DR-01 deletion test is intermittently flaky~~ | **Miguel Alvarez** | ✅ **Closed 23 Jul** | ✅ **Closed** |
| **OPEN-24** | ⚠️ **`reattemptReason`'s wording is inaccurate for a brand-new task's first-ever placement, though nothing renders it today.** Found authoring packet 13. `server/src/reschedule/reasons.ts`'s `reattemptReason` — *"placed at X — your day had no room for it earlier today"* — is used **unconditionally** by `sweepElapsed`'s reattempt branch (`RescheduleService.ts`, the same branch OPEN-21/v2.20 fixed for `rescheduleTrigger`), for both a task that genuinely failed to place before **and** a task that has simply never been placed at all. For the second case the sentence is false: nothing was tried and failed "earlier today" — this is the task's first attempt. **Not urgent and not blocking**: packet 13 gates the dashboard's reason line (ⓘ) and reschedule marker (↻) on `Placement.rescheduleTrigger`'s presence, matching the SRS §3.7.1 wireframe (an ordinary "Lunch" block carries no ⓘ) — so this string is **stored but never displayed** for the case where it's wrong. Flagged because storing an inaccurate sentence is a latent risk for anything that later reads `placementReason` unconditionally (FR-ANL, a future `.ics` export, an admin view) — DR-03 says a placement records why it's where it is, and this one would be recording something false, just not saying it out loud yet. **Same fix shape as OPEN-21**: distinguish "history exists but nothing planned" from "history is empty" inside `sweepElapsed`, and give the empty case its own, honest sentence. **Assigned to packet 17a (24 Jul), folded in with OPEN-22** — the fix is a behaviour change in the reschedule module, so §4.6 bars the session that wrote that module from also writing its test, and packet 06's suite is frozen (a new test there needs a re-freeze and a second human). 17a's RED gate is the clean-room both need: its agent reads the SRS and no implementation. **It stays dormant until then** — nothing renders `placementReason` for a first placement (gated on `rescheduleTrigger`'s presence), so deferral loses nothing; the risk lands only when a future unconditional reader (FR-ANL, `.ics` export) arrives, which 17a precedes. 🟡 **PINNED 26 Jul — packet 17a's frozen suite now asserts it (`reattempt-provenance.test.ts`, the "no false claim of an earlier failed attempt" test), and it is RED against current code** (`reattemptReason` stores *"…your day had no room for it earlier today"* on a first-ever placement — the real bug). **The FIX lands in 17b GREEN** (distinguish empty history from "history exists but nothing planned" in `sweepElapsed`, and give the empty case an honest sentence); the test is a *negative* property (`not /no room/i`, `not /earlier today/i`) so 17b is free to word it. **Closes when 17b turns it green.** ✅ **CLOSED 26 Jul — 17b GREEN turned it green.** `sweepElapsed`'s re-attempt branch now stores an honest `firstPlacementReason` on a first-ever placement (`server/src/reschedule/reasons.ts`, `RescheduleService.ts`); the frozen 17a test passes; the false "no room earlier today" sentence is gone for the empty-history case. | **Patrick Rucker** | ✅ **Fixed 26 Jul (17b)** | ✅ **Closed** |
| **OPEN-27** | ⚠️ **A recommendation-replaced workout does not stay replaced across a schedule sweep — demo-blocking.** Found by packet 17b GREEN. `RecommendationScheduler.applyWorkoutRecommendation` vacates the above-tier run it replaces by marking its `Placement` **`CANCELLED`** (the only status that frees the slot and preserves history — but a semantic overload: DR-06/the contract define `CANCELLED` as an FR-RSC-09 *withdrawal*). The replaced task then has **no `PLANNED` placement**, and `sweepElapsed`'s re-attempt branch (FR-RSC-05) re-places *any* such task on the next retrieval — so the replaced run **resurrects** on the very next `GET /schedule`, landing beside the recovery session and undoing the replacement. **Not caught by 17a's frozen suite** (it drives services directly and never sweeps the replaced task's own resurrection — a coverage hole, the v2.22 lesson again), so 17b passes 8/8 while the integrated behaviour is wrong. **The root cause is that the domain has no durable "set aside" concept** — no way to suppress a task's occurrence for a day without deleting the task. Options for the fix (a design decision, not mechanical): (a) a durable set-aside marker the sweep honours; (b) a new terminal `PlacementStatus` (e.g. `SUPERSEDED`) excluded from re-attempt (a contract touch); (c) model the replacement as an *edit* of the user's own workout task rather than a distinct `SYSTEM` task — but 17a froze `source: 'SYSTEM'`, so this contradicts the frozen suite and would need a re-freeze. **Proper fix = a clean-room RED test added to 17a (re-freeze + second human) pinning "the replacement survives a sweep," then the mechanism.** ⚠️ **Must land before the ≥3 §6 demo rehearsals** (build-order item 7) — a rehearsal through the app hits the second `GET /schedule` and the run reappears. 🟡 **DESIGN CHOSEN 26 Jul — option (b), a new terminal `PlacementStatus` `SUPERSEDED`** (decision log, 26 Jul); authored as packet `prompts/verification/17c-replacement-durability.md`, which runs RED (own frozen dir `server/test/replacement/`) → second-human gate → GREEN. ✅ **CLOSED 26 Jul — 17c GREEN turned the frozen durability test green.** `SUPERSEDED` added to `PlacementStatus` (human-authored amendment, transcribed verbatim, §4.7); `RecommendationScheduler.applyWorkoutRecommendation` now vacates the replaced run as `SUPERSEDED` (not `CANCELLED`); `RescheduleService.sweepElapsed` excludes a task whose date-occurrence is `SUPERSEDED` from FR-RSC-05's re-attempt branch — so the replaced run no longer resurrects on any sweep, across two idempotent sweeps (FR-RSC-06). `SUPERSEDED` is not in `OCCUPIES_TIME`, so it frees the slot for the recovery exactly as `CANCELLED` did. FR-RSC-03 held (`guard:single-placement` green, no second placement fn); `npm run verify` **319 green** (318 + the 17c test), engine 100%; no frozen test edited (`git diff` of every frozen dir empty); SRS → v2.28. | **Patrick Rucker** | **Before demo rehearsals (packet 17c)** | ✅ **Closed 26 Jul (17c GREEN)** |
| **OPEN-17** | ✅ **CLOSED 22 Jul — no new placement-adjacent function was needed.** `TaskRepository.tasksForDate` decides which tasks are candidates for a date (visibility: does a never-placed or recurring task count); `sweepElapsed`, wired into `GET /schedule` (FR-RSC-10's own retrieval point), places everything still unplaced for a date **together, in one correctly-ordered pass** — the frozen `placement-order.test.ts` suite already proves that ordering. Verified with a test creating three tasks out of priority order across separate requests and checking the final schedule, not just call order. *(Superseded in part by packet 13's UC-02/UC-03 direct-call design — a flexible task's FIRST placement now goes straight through `findCandidateSlots`, not deferred to `sweepElapsed` — but that changed how a single task's live creation is placed, not FR-SCH-10's genuine batch ordering, which `sweepElapsed` still owns; OPEN-17 was about the latter and stays closed. See `docs/P12-REPORT.md`.)* *Original:* ~~**FR-SCH-10 binds a path no class owns — placing a day's flexible tasks in the FIRST place.** `RescheduleService` covers the reschedule paths and packet 06 tests them; initial day planning is on no class in §3.6.~~ | **Miguel Alvarez** | ✅ **Closed 22 Jul** | ✅ **Closed** |
| **OPEN-15** | ✅ **CLOSED 22 Jul — `Task.createdAt` added (ISO 8601 UTC instant, optional in the type); absent or identical instants fall through to ascending `id`. FR-SCH-10 note added, SRS v2.11.** Rejected `id`-only and repository-supplied-order, both of which fail silently — see the decision-log row above. Frozen engine suite verified unaffected. *Original below.* ~~**FR-SCH-10's "earlier-created" tiebreak has nothing to read.**~~ The requirement says equal-priority tasks are placed earlier-created first *"so that the order is total and repeatable"* — but `Task` in the frozen contract carries **no creation timestamp**: `id`, `title`, `type`, `durationMinutes`, `priority`, `preferredWindow`, `flexibility`, `source`, `intensityTier`, and nothing else. **As written, the tiebreak cannot be evaluated.** ⚠️ **Structurally identical to the FR-SCH-03(b) defect closed this morning** — a criterion the code has no field to read — and the available workarounds are all silent: tiebreaking on `id`, on insertion order, or on whatever Mongo returns would satisfy every test while making *"total and repeatable"* false. **Adding a field to `contract.ts` is a §4.7 team decision**, so packet 06's RED agent is instructed to escalate rather than guess. *(Found while authoring packet 06.)* | **Patrick Rucker** | **Before packet 06's gate** | ✅ **Closed** |
| **OPEN-16** | ✅ **CLOSED 22 Jul — all three parts, before packet 06 ran.** **(1)** rank 1, all three triggers, day narrowed to start at `now` (v2.12). **(2)** unplaced is the absence of a placement; offer acted on via `moveToNextDay` (v2.13). **(3)** idempotency by occurrence state, no trigger log, termination proved from strictly-later successors (v2.13). *Originals below.* ~~**(1) FR-RSC-01** says the missed task goes in *"the next valid slot remaining that day"* — but FR-SCH-02 returns **up to three ranked candidates**, and the SRS never says whether the automatic path takes the engine's **rank 1** or the **earliest remaining**; they are not always the same slot.~~ ~~**(2) FR-RSC-05** says the System *"shall offer to move it to the next day"* — whether the service **creates** a next-day placement or **surfaces an offer** the user must accept produces different stored state, different task counts, and therefore different tests.~~ **(3) FR-RSC-06** requires *"the same trigger processed twice"* to be idempotent, without defining what makes two triggers the same one — placement? trigger kind? occurrence? — and idempotency cannot be tested without that answer. **All three are frozen into executable form by packet 06**, so an invented answer becomes a requirement nobody agreed to and packet 07 implements it faithfully. *(Found while authoring packet 06; listed in its escalation clause.)* | **Patrick Rucker** | **Before packet 06's gate** | ✅ **Closed** |
| **OPEN-14** | ✅ **CLOSED 22 Jul — corrected in both copies to `(FR-SCH-10 placement order; FR-SCH-07 displacement.)`.** Comment only; no type, field or behaviour changed. Authorised by Patrick, per §4.7. *Original:* ~~**`shared/src/contract.ts` line 71 documents `priority` as "(FR-SCH-03 ranking tiebreak)" — that is now false.** FR-SCH-03 no longer uses priority (SRS v2.7). The field itself is correct and unchanged; **only the comment's justification is stale**. It should now read **`(FR-SCH-10 placement order; FR-SCH-07 displacement.)`** — FR-SCH-10 being priority's Essential consumer as of v2.8. ⚠️ **Not edited by the agent on purpose:** §4.7 makes `contract.ts` human-owned, and an agent that edits it — even a comment — has crossed the line the rule draws. The same sentence appears in `prompts/foundation/03-shared-contract-types.md` line 121 and must change with it, or the two disagree. **A one-line fix, but it is Patrick's line to change.** | **Patrick Rucker** | **Before packet 05** | ✅ **Closed** |

---

# ✅ ACTION ITEMS

| # | Action | Owner | Due | Done |
|---|---|---|---|---|
| 1 | **Pull the Garmin export and report what fields it actually contains.** ⚠️ **Critical path** — the sleep-score formula and the meal library's calorie assumptions are guesses until this is done. | **Ryan** | **16 Jul** | ☐ |
| 2 | ~~Send role assignments~~ — **done. SRS cover page, all 49 TOC annotations, and the signature block are updated.** | Patrick | 12 Jul | ✅ |
| 3 | **Read `AGENTIC-TDD-WORKFLOW.md`** — all three of us, before any code is written. It is self-contained; it assumes you've seen none of this. **§2 is the argument** (the three ways agentic TDD fails silently — **read the worked example in §2.1**, it's the whole thing in one code block). **§6 is the checklist you will personally run.** | All | 13 Jul | ☐ |
| 4 | **Ratify the contract** (`prompts/foundation/03-shared-contract-types.md`) — read it line by line, together. **This is the hour that lets us work in parallel.** Once agents are running, changing it is expensive. | All | 13 Jul | ☐ |
| 5 | Create the repo — **run `prompts/foundation/01-project-scaffold.md`** | **Patrick** | 13 Jul | ☐ |
| 6 | **Engine RED** — run `prompts/engine/04-engine-tests-RED.md`. Output is a **failing** suite. *(A green suite means the agent wrote the engine and the packet failed.)* | **Patrick** | 14 Jul | ☐ |
| 7 | **HUMAN GATE** — read every engine test against the requirement it cites. Not *"do they pass"* — **"are they right."** ⚠️ **Do not skip this. It is the review that carries the project; the code is checked by the tests, and the tests are checked by nothing but us.** | **Patrick + 1 other** | 14 Jul | ☐ |
| 8 | **Engine GREEN** — run `prompts/engine/05-engine-implementation-GREEN.md`. Agent **may not edit a test**; escalations come to a human. | **Patrick** | 15 Jul | ☐ |
| 9 | **Mongo collections** — **one document per metric per date**, *not* one field per metric (DR-05). ⚠️ **A schema-free store permits the wrong shape; DR-05 still forbids it.** | **Miguel** | 15 Jul | ☐ |
| 10 | Frontend scaffolding against a **mocked** API, typed from the contract so it can't drift. **Do not wait for the backend.** | **Miguel** | 17 Jul | ☐ |
| 11 | Write the (I)-requirement CI guards: **one-placement-function** (FR-RSC-03), **no-network-in-recommendation** (FR-LIB-02) | **Patrick** | 18 Jul | ☐ |
| 12 | **Ask the professor the three questions in OPEN-10** (AI-written prompts? AI-written tests? deliverable format?) | Patrick | Before 16 Jul | ☐ |
| 13 | ~~Resolve the workload imbalance~~ (OPEN-09) — Miguel currently owns frontend *and* backend | Patrick | Meeting 2 | ✅ |
| 14 | **Ratify the scope cuts** (OPEN-11) — FR-REC-10 and NFR-MNT-02 → Conditional | Patrick | Meeting 2 | ☐ |
| 15 | ~~`.ics` export (FR-CAL-07) — Conditional. Do not start until all Essential work is verified.~~ **Done 24 Jul on `wp-cal-export`, started ahead of this row's own instruction — see the 24 Jul decision-log row for the reasoning (scoped, Conditional, no collision with anyone else's queue).** | Miguel | after 27 Jul | ✅ |
| | | | | ☐ |
